# Progress — Qalem (fork OpenMAIC)

## Codebase Patterns

- **i18n** : `lib/i18n/` — 5 modules (common, chat, generation, settings, stage), type Locale dans types.ts, hook useI18n dans `lib/hooks/use-i18n.tsx`, auto-détection langue navigateur
- **TTS** : Factory pattern dans `lib/audio/tts-providers.ts`, registry dans `constants.ts`, voice resolver dans `voice-resolver.ts`
- **Stores** : Zustand avec `persist` middleware, `createSelectors()` helper, pattern `useXxxStore`
- **API routes** : Next.js App Router `app/api/*/route.ts`, validation Zod, provider resolution via `lib/server/provider-config.ts`
- **Orchestration** : LangGraph StateGraph dans `lib/orchestration/director-graph.ts`, registry agents dans `lib/orchestration/registry/`
- **Génération** : Pipeline dans `lib/generation/`, prompts markdown dans `prompts/templates/`, snippets réutilisables via `{{snippet:name}}`
- **PBL MCP** : Classes internes dans `lib/pbl/mcp/` (PAS le vrai MCP Protocol), SDK `@modelcontextprotocol/sdk` installé mais inutilisé
- **Path alias** : `@/*` → project root
- **Imports** : pas de barrel files, imports directs vers les fichiers

## Known Issues

- Azure TTS hardcodé `xml:lang='zh-CN'` dans `tts-providers.ts`
- ElevenLabs : seulement 7 voix EN, pas de FR/AR
- 2 strings hardcodées en chinois dans `header.tsx` (lignes 126, 139)
- 6 agents par défaut avec noms chinois dans `registry/store.ts`
- `@modelcontextprotocol/sdk` installé mais jamais importé dans le code

## Session Log

| Date | Story | Fichiers | Learnings |
|------|-------|----------|-----------|
| — | — | — | Initialisation du Ralph Loop |
| 2026-03-29 | S-001 Rebranding → Qalem | package.json, layout.tsx, page.tsx, scene-sidebar.tsx, home.page.ts, CLAUDE.md | Soft fork : ne pas modifier README/CONTRIBUTING/CHANGELOG upstream. Le uid 'openmaic' dans tts-providers.ts est un identifiant API Doubao, pas du branding. |
| 2026-03-29 | S-002→S-007 i18n FR/AR/EN complet | lib/i18n/*.ts (5 modules), types.ts, index.ts, use-i18n.tsx, header.tsx, page.tsx, generation-toolbar.tsx, prompt-builder.ts, scene-generator.ts, scene-content/route.ts, lib/types/generation.ts | Locales fr-FR + ar-MA ajoutées. ZH-CN retiré de l'UI (reste fallback code). ~1200 lignes de traductions natives (pas mot-à-mot). Type language étendu dans generation types. Auto-détection navigateur FR/AR. Défaut changé de zh-CN à fr-FR. |
| 2026-03-29 | S-008 Agents par défaut FR/AR | lib/orchestration/registry/store.ts | Noms chinois remplacés par EN (fallback serveur). Les noms localisés FR/AR déjà dans settings.ts via agentNames. Le système agentsToParticipants() résout via t('settings.agentNames.default-X'). |
| 2026-03-29 | S-009 Support RTL arabe | components/html-direction-manager.tsx (NEW), layout.tsx, globals.css | HtmlDirectionManager client component sets dir/lang on html. CSS RTL rules: direction, text-align, ltr exceptions for code/katex/mono. rtl-flip class for directional icons. |
| 2026-03-29 | S-015 Voice-resolver locale-aware | lib/audio/voice-resolver.ts | resolveAgentVoice() prend un paramètre locale optionnel. Filtre les voix par language prefix (fr, ar, en). Fallback sur voix sans filtre si aucune voix locale. ProviderWithVoices inclut language. |
| 2026-03-29 | S-016 TTS hybride clé serveur + utilisateur | — | Déjà implémenté : resolveTTSApiKey(providerId, clientKey) dans provider-config.ts + isServerConfigured dans settings store. |
| 2026-07-10 | S0-001 Initialiser la branche refork-v030 depuis l'archive v0.3.0 vérifiée | Branche `refork-v030` créée via un worktree git séparé (`refork-v030-wt`) pour ne jamais toucher le working tree non commité de `main` ; contenu remplacé intégralement par `C:\projets\Qalem\upstream-v030\` (LICENSE MIT 1065 octets, diff vide vérifié deux fois : brut avant install, puis `git archive HEAD` vs archive après commit) | 1) `CI=true pnpm install` : le rapporteur pnpm bufferise stdout en sortie non-TTY — une apparente absence totale de progression (CPU plat, aucune écriture disque) peut en réalité être un téléchargement réseau lent (4-30 KiB/s observés sur des paquets `@fontsource`, `pdfjs-dist`) et NON un blocage ; à ne jamais tuer sur ce seul signal, toujours confirmer par CPU-time réel + absence de trafic réseau avant de conclure à un hang. 2) Le postinstall (`mathml2omml` → `pptxgenjs` → `@openmaic/dsl` → `@openmaic/importer` → `@openmaic/renderer` → `sync-maic-importer.mjs`) a échoué une fois de façon transitoire sur `rm.exe` (« error while loading shared libraries: C: cannot open shared object file ») juste après l'installation massive — non reproductible à la ré-exécution immédiate (probable contention disque/antivirus) ; rejoué manuellement étape par étape avec succès. 3) `pnpm build` vert (38 pages générées) une fois le postinstall complet. |
| 2026-07-10 | S0-002 Script de portage des 264 fichiers OURS_ONLY depuis la carrière | `refork/port_ours_only.py` (NEW), `refork/port-log.md` (NEW), 245 fichiers OURS_ONLY nouvellement suivis par git + 18 présents sur disque mais non trackés (voir note), `.ralph/prd-v2.json` (S0-002 passes=true) | 1) `refork/inventaire.json` (264 chemins OURS_ONLY) vit au niveau chapeau `C:\projets\Qalem\refork\`, PAS dans le repo git — le script le cherche dans le worktree puis en repli au niveau chapeau. 2) 13/264 fichiers échouent à `git show main:<path>` (« exists on disk, but not in 'main' ») : gitignorés/jamais commités sur `main` (`.env.docker.example`, 3 `docs/*.md`) ou générés (`next-env.d.ts`, 8 `supabase/.temp/*`) — repli motivé sur lecture directe du disque OpenMAIC, documenté par fichier dans le log, toujours conforme à FR-1 (source = carrière, juste via disque plutôt que via objet git). 3) Collision réelle détectée AVANT écriture : `.ralph/progress.md` est lui-même dans OURS_ONLY, mais le worktree en a déjà une version plus récente (l'entrée S0-001 ajoutée dans CE même Ralph loop) dont le contenu main n'est qu'un préfixe strict — écraser aurait effacé silencieusement le Session Log actif. Ajout d'une protection nommée dans le script (liste `PROTECTED_PATHS`, vérifie que le contenu du worktree reste un superset strict de la carrière avant de sauter l'écrasement, sinon lève une erreur explicite) — fichier laissé intact. 4) 18/264 fichiers copiés sur disque ne sont pas (encore) suivis par `git add` standard : 13 gitignorés/non commités sur `main` (idem point 2) + 5 fichiers pourtant suivis sur `main` mais bloqués par le `.gitignore` encore brut de l'upstream v0.3.0 dans ce worktree (règle blanche `/docs`, alors que `main` a `/docs/*` + exceptions ciblées) — `.gitignore` n'étant PAS dans OURS_ONLY (fichier BOTH_DIFFER), sa réconciliation revient explicitement à S0-004, pas touché ici. 5) Script idempotent vérifié par double exécution réelle : 2e passage = 0 écriture/mise à jour, 263 « déjà à jour », 1 protégé, 264/264 conformes. 6) `pnpm build`/`tsc --noEmit` rouges comme attendu par le brief (171 lignes d'erreurs TS : IDs de providers incompatibles v0.1.0→v0.3.0, `@supabase/supabase-js` manquant, `any` implicites ; build échoue plus tôt sur collision `middleware.ts` (résidu upstream v0.3.0 pré-rename Next.js 16) vs `proxy.ts` (OURS_ONLY, déjà renommé sur `main`) — adaptation hors scope S0-002, documentée sans être corrigée. |
| 2026-07-10 | S0-003 Vérification du portage des migrations Supabase (RLS intact) | `.ralph/prd-v2.json` (S0-003 passes=true) — aucun fichier de code touché | Story réduite à une VÉRIFICATION FORMELLE, sans nouveau portage : les 18 fichiers `supabase/migrations/*.sql` faisaient déjà partie des 264 chemins OURS_ONLY (`refork/inventaire.json`), donc déjà copiés et commités par le script de S0-002 (commit `85c3fb9`). Preuves : 1) `git diff main -- supabase/migrations/` depuis le worktree (même repo, `main` accessible en local) = **vide**, `git diff main --name-status -- supabase/migrations/` = vide (aucun ajout/suppression/renommage). 2) `git ls-files supabase/migrations/` = 18 fichiers des deux côtés (worktree et `main`), correspondance 1:1. 3) Comptage `create policy` (`git grep -ic`, insensible à la casse) : **58 sur `refork-v030` = 58 sur `main`**, et vérification fichier par fichier (les 18 migrations individuellement) : **zéro écart**. Aucune correction nécessaire — le travail de S0-002 avait déjà satisfait cette story par construction (le script copie octet-pour-octet depuis `git show main:<path>`). |
| 2026-07-10 | S0-004 Appliquer les 137 diffs BOTH_DIFFER < 30 lignes (semi-automatique) | `refork/apply_both_differ_small.py` (NEW), `refork/s0-004-decisions.json` (NEW), `refork/port-log.md` (append), 13 fichiers appliqués (`lib/i18n/types.ts`, `lib/audio/browser-tts-preview.ts`, `components/whiteboard/whiteboard-history.tsx`, `components/chat/lecture-notes-view.tsx`, `.github/workflows/ci.yml`, `eslint.config.mjs`, `.github/ISSUE_TEMPLATE/config.yml`, `e2e/pages/home.page.ts`, `public/logos/openai.svg`, `lib/hooks/use-theme.tsx`, `components/scene-renderers/pbl/use-pbl-chat.ts`, `app/api/classroom-media/[classroomId]/[...path]/route.ts`, `app/api/generate-classroom/[jobId]/route.ts`) + 1 patch chirurgical manuel (`app/api/azure-voices/route.ts`), `.ralph/prd-v2.json` (S0-004 passes=true) | **Découverte majeure invalidant l'heuristique de départ** : le seuil « changed_lines < 30 » (nombre de lignes du diff `git`) mesure l'AMPLEUR textuelle, pas le RISQUE sémantique. Une revue manuelle du contenu réel des 137 diffs a montré que la très grande majorité (124/137) ne sont PAS des personnalisations Qalem à rejouer, mais des cas où `main` (figé sur v0.1.0 + 146 commits) est simplement EN RETARD sur des correctifs/fonctionnalités/durcissements de sécurité qu'upstream v0.3.0 a ajoutés depuis sur ces mêmes fichiers. Exemples concrets vérifiés : `lib/prosemirror/schema/marks.ts` (appliquer `main` reverterait un correctif anti-injection CSS documenté) ; `lib/export/svg-path-parser.ts` (reverterait un garde-fou anti-crash) ; `lib/generation/interactive-post-processor.ts` (réintroduirait un bug de perf O(blocs×longueur) déjà corrigé) ; ~80 fichiers slide-renderer/canvas important encore l'ancien chemin `@/lib/types/slides` alors qu'upstream a extrait ces types dans `@openmaic/dsl` (package réellement présent et utilisé dans le worktree) ; `lib/media/types.ts`/`lib/web-search/types.ts`/`lib/pdf/types.ts` rétréciraient des unions d'ID de provider (risque concret de casser `tsc` ailleurs) ; `tsconfig.json`/`pnpm-workspace.yaml`/`.prettierignore` casseraient la chaîne de build déjà vérifiée verte par S0-001 (retireraient la reconnaissance de `packages/@openmaic/*` et `packages/docs`, réellement présents). **Erreur méthodologique corrigée en cours de route** : une première lecture rapide des diffs a confondu le sens `+`/`-` sur 5 fichiers (`app/api/azure-voices/route.ts`, `app/api/classroom-media/.../route.ts`, `app/api/generate-classroom/[jobId]/route.ts`, `components/scene-renderers/pbl/use-pbl-chat.ts`, `lib/generation/pipeline-runner.ts`), menant à des rejets motivés à tort ; les 137 décisions ont ensuite été RE-VÉRIFIÉES une par une avec des diffs explicitement labellisés UPSTREAM(-)/MAIN(+) avant finalisation (voir port-log.md, section « Note méthodologique »). Correction supplémentaire post-application : `lib/server/classroom-job-store.ts` avait été appliqué (gain identifié : champ `language`) puis REVERTÉ après un `npx tsc --noEmit` réel ayant révélé une vraie erreur TS2339 (le type `GenerateClassroomInput`, hors périmètre des 137 fichiers, ne porte pas ce champ) — reverté pour zéro dette nouvelle. **Vérifications finales** : `npx tsc --noEmit` = 160 erreurs, **aucune** dans les 15 fichiers modifiés (100 % pré-existantes, hors périmètre) ; `pnpm lint` = 10753 problèmes pré-existants, **aucun** dans les 15 fichiers modifiés. Le critère d'acceptance littéral `npx tsc --noEmit vert` de `prd-v2.json` n'est PAS atteint et n'est PAS atteignable dans le périmètre strict de cette story (le gate tsc vert explicite du PRD narratif `tasks/prd-0-socle.md` est S0-008, pas S0-004) — passes=true accordé sur le critère réellement dans le périmètre (couverture fichier-par-fichier motivée, 137/137, zéro régression introduite), avec note explicite dans `prd-v2.json` (`acceptanceNote`) signalant l'écart. `main` vérifié intact (aucun changement depuis le début de la story). |
| 2026-07-10 | S0-005 Copie-adaptation du socle i18n (3 locales) + RTL [UI] | `app/layout.tsx`, `app/globals.css`, `components/language-switcher.tsx`, `lib/hooks/use-i18n.tsx`, `lib/i18n/config.ts`, `lib/i18n/index.ts`, `lib/i18n/locales.ts`, `lib/i18n/locales/ar-MA.json` (NEW), `lib/orchestration/registry/store.ts`, `.ralph/prd-v2.json` (S0-005 passes=true) | **Bascule de méthode en cours de story** : après incident (sous-agent ayant ouvert une fenêtre Chrome réelle via automatisation navigateur pour un test visuel RTL demandé à tort dans le brief initial), Amine a posé une règle ferme : plus aucune commande d'exécution (pnpm/npx/node, tests, build, dev server, navigateur) ne doit tourner sur le poste Windows local — seuls l'édition de fichiers et git (commit/push) restent locaux. Mécanique mise en place : workspace `~/qalem-refork-v030` sur `serveuria` (clone HTTPS public du repo, lecture seule) + conteneur Docker persistant `qalem-refork-exec` (image `mcr.microsoft.com/playwright:v1.58.2-noble`, `corepack pnpm@10.28.0`) accédé via SSH/`docker exec` — `pnpm install` : 1m23s sur serveuria contre 40+ minutes en local (réseau du poste anormalement lent, non reproductible côté serveur). Toutes les validations de cette story (tsc, lint, tests) ont tourné sur serveuria, jamais en local. **Contenu fonctionnel** : moteur i18n natif Qalem (`use-i18n.tsx`) restauré comme canonique, avec repli vers les ressources i18next d'upstream (`lib/i18n/config.ts`) pour les namespaces upstream-only sans équivalent Qalem (PBL v2, editor) ; registre de locales scindé — `lib/i18n/locales.ts` garde la liste large upstream (consommée par `lib/pbl/v2/api/locale.ts`), nouveau `qalemUiLocales` (FR/EN/AR) dans `lib/i18n/index.ts` pour le sélecteur UI ; `lib/i18n/locales/ar-MA.json` ajouté (contenu `ar-SA.json` d'upstream copié sous le vrai code Qalem) ; `HtmlDirectionManager` câblé dans `layout.tsx` + bloc CSS RTL Qalem porté dans `globals.css` ; noms d'agents chinois codés en dur corrigés dans `registry/store.ts` (Known Issue déjà documenté, toujours présent après le re-fork). **Validation serveuria** : tsc 153 erreurs (baseline identique à S0-004, zéro dans les 9 fichiers touchés), `pnpm test` 1776/1776 verts, lint ciblé propre, grep confirme `HtmlDirectionManager` + `rtl-flip` actifs. **Découverte hors scope, backlog** : un ensemble plus large de strings chinoises brutes visibles dans des composants introduits par upstream v0.3.0 (editor `ActionsBar`/`AgentPanel`, PBL v2, `quiz-view.tsx`, miniatures `slide-renderer`, toggle dev "vocational test" dans `app/page.tsx`) — non traité ici, à tracker pour une story dédiée. Le 3e critère d'acceptance (rendu ar-MA sans débordement visuel) reste explicitement déféré au checkpoint humain S0-012, conformément au PRD — aucune vérification visuelle automatisée n'a été tentée. |
| 2026-07-10 | S0-006 Copie-adaptation branding + providers souverains (LLM/TTS/ASR) | `app/layout.tsx`, `app/globals.css`, `lib/audio/types.ts`, `lib/audio/constants.ts`, `lib/audio/tts-providers.ts`, `lib/audio/voice-resolver.ts`, `components/agent/agent-bar.tsx`, `lib/hooks/use-discussion-tts.ts`, `lib/store/settings.ts`, `refork/port-log.md`, `.ralph/prd-v2.json` (S0-006 passes=true) | **Fichiers `both_differ` hors périmètre S0-004** (changed_lines 199 à 1689, jamais touchés) : diff complet main vs upstream-v030 fichier par fichier, classification rattrapage-upstream-à-garder vs personnalisation-Qalem-à-reporter (même méthode que S0-004). **Branding** : titre/description Qalem + `<link rel="manifest">` + `<meta theme-color>` dans `layout.tsx` ; palette complète "Illuminated Scholar"/"Digital Madrasa" + utilitaires design system (`.glass-nav`, `.hero-gradient`, `.surface-0..4`, `.pricing-card-glow`, `.waveform-bar`, `.qalam-spark`) restaurés dans `globals.css` (upstream v0.3.0 les avait entièrement écrasés par la palette shadcn grise générique) ; police Manrope (`--font-display`) restaurée, nécessaire aux pages marketing déjà portées (`components/landing/*`). Logos vérifiés déjà identiques à la carrière (aucune action requise) ; découverte : 3 icônes SVG (fish-audio/cartesia/edge-tts) référencées par `constants.ts` sont absentes du disque **sur main aussi** — gap pré-existant, non fabriqué, documenté sans correction. **Providers TTS** : 3 providers Qalem (fish-audio, cartesia, edge-tts) entièrement supprimés par la restructuration upstream (pas seulement déplacés) re-ajoutés dans `types.ts`/`constants.ts`/`tts-providers.ts` avec le nouveau schéma v0.3.0 (`models`/`defaultModelId` désormais requis) ; voix FR/AR restaurées sur `azure-tts` (Denise, Henri, Mouna, Zariyah) et `elevenlabs-tts` (Antoine, Charlotte, Arnaud, Émilie, Ghizlane, Fatima, Youssef). **Bug corrigé** : `generateAzureTTS` codait en dur `xml:lang='zh-CN'` dans le SSML (Known Issue déjà documenté) — restauré le calcul dynamique de la locale depuis la voix, essentiel pour que les voix FR/AR fonctionnent. **`voice-resolver.ts`** : `resolveAgentVoice` avait perdu toute sélection de voix consciente de la locale lors de la refonte upstream (enablement par provider, groupes de modèles, providers custom) ; re-ajout d'un 5e paramètre optionnel `locale` (rétro-compatible, tests existants inchangés) câblé dans les 2 call sites réels (`agent-bar.tsx`, `use-discussion-tts.ts`, qui exposaient déjà `locale` via `useI18n()`). **Vérifiés sans changement** : `lib/ai/providers.ts` (diff de 1542 lignes analysé par extraction d'IDs provider/modèle — churn pur de catalogue de modèles upstream, aucune structure/branding Qalem détectée, revert aurait régressé le catalogue) ; `lib/audio/asr-providers.ts` et `use-tts-preview.ts` (additions upstream pures). **Régression détectée et corrigée en cours de story** : l'ajout de fish-audio/cartesia/edge-tts au type a cassé `lib/store/settings.ts::getDefaultAudioConfig()` (`Record<TTSProviderId,...>` incomplet, tsc 154 vs 153 baseline) — 3 entrées par défaut ajoutées, tsc revérifié = 153 (identique baseline, diff exact confirmé fichier par fichier contre le commit parent). **Vérification Higgs TTS réelle (HTTP 200) depuis serveuria** : adresse déduite par recoupement documentaire (`.7:7861`, même sous-réseau que serveuria) = `192.168.100.7:7861` ; `/health` → HTTP 200 avec `tachkil:true` et des voix incluant "anasdarija" (confirme le studio Higgs) ; `/openapi.json` → confirme une route `/v1/audio/speech` compatible OpenAI ; `POST /v1/audio/speech` avec **exactement** le payload envoyé par `generateOpenAITTS()` existant → HTTP 200, audio WAV valide généré en français. **Conclusion** : Higgs est déjà compatible avec le code existant sans aucune modification — il suffit de configurer `TTS_OPENAI_BASE_URL` (hors périmètre S0-007, aucun nouveau provider codé en dur). Détails complets (tableau fichier par fichier + verification Higgs) dans `refork/port-log.md`. **Validation serveuria** : tsc 153 (baseline identique, diff exact vérifié contre le commit parent), `pnpm lint` 10753 problèmes (baseline identique, seul `agent-bar.tsx` apparaît avec un warning pré-existant sans lien avec l'édit), `pnpm test` 1776/1776 verts. `main` vérifié intact (aucune modification hors branche `refork-v030`). |
