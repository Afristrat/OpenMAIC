# Port log — S0-002 (OURS_ONLY → refork-v030)

Journal d'execution de `refork/port_ours_only.py`. Chaque execution reelle
(hors `--dry-run`) ajoute une section horodatee ci-dessous.

## Execution 2026-07-10T13:38:56

- Inventaire : `C:\projets\Qalem\refork\inventaire.json`
- Repo carriere (main) : `C:\projets\Qalem\OpenMAIC`
- Worktree cible : `C:\projets\Qalem\refork-v030-wt`
- Total OURS_ONLY attendu : 264
- Source `git show main:<path>` : 251
- Source repli filesystem (fichier non suivi sur main — gitignore/genere) : 13
  - Fichiers concernes par le repli filesystem (motif FR-1 : `git show` echoue car fichier absent de l'historique `main`, present seulement sur le disque du repo carriere) :
    - `.env.docker.example`
    - `docs/bundle-optimization-todo.md`
    - `docs/np-cadrage.md`
    - `docs/self-hosted.md`
    - `next-env.d.ts`
    - `supabase/.temp/cli-latest`
    - `supabase/.temp/gotrue-version`
    - `supabase/.temp/pooler-url`
    - `supabase/.temp/postgres-version`
    - `supabase/.temp/project-ref`
    - `supabase/.temp/rest-version`
    - `supabase/.temp/storage-migration`
    - `supabase/.temp/storage-version`
- Fichiers proteges (non ecrases, superset strict deja present dans le worktree) : 1
  - `.ralph/progress.md` : suivi actif du Ralph loop de re-fork (deja modifie par S0-001 dans ce worktree) ; le contenu main est un prefixe exact du contenu worktree (superset strict) -> ecraser detruirait l'entree Session Log S0-001
- Nouveaux fichiers ecrits : 262
- Fichiers mis a jour : 1
- Fichiers deja a jour (idempotence) : 0
- Erreurs : 0
- Verification post-copie : 264/264 conformes, ecarts = 0
- Hash sha256 de l'ensemble des 264 chemins (contenu concatene, ordre inventaire) : b8531895e4815528ad68ce9603e371f5a19667e7bb6bd0bb2a33f414472404b0

### Note sur le suivi git des 264 fichiers (analyse manuelle post-execution)

Les 264 fichiers sont tous presents et verifies octet-pour-octet sur le disque du
worktree (cf. verification ci-dessus). Cote suivi `git`, 18 d'entre eux ne seront
PAS inclus par un `git add -A`/`git add .` standard (git les ignore silencieusement) :

- **13 fichiers** non suivis sur `main` non plus (repli filesystem, cf. section
  ci-dessus) : `.env.docker.example`, `next-env.d.ts`, `docs/bundle-optimization-todo.md`,
  `docs/np-cadrage.md`, `docs/self-hosted.md`, et les 8 `supabase/.temp/*` (cache local
  du CLI Supabase, ephemere). Reproduire fidelement l'etat reel de la carriere, y
  compris son absence de suivi git, est conforme a FR-1 — aucune decision editoriale
  prise ici pour forcer leur ajout.
- **5 fichiers** suivis sur `main` (`git show main:<path>` reussit) mais bloques par
  le `.gitignore` de `refork-v030` : `docs/demo-video-script.md`, `docs/features-showcase.md`,
  `docs/pilot-preparation.md`, `docs/quick-start.md`, `docs/stitch-prompts.md`. Cause
  racine identifiee : `.gitignore` n'est PAS dans OURS_ONLY (c'est un fichier BOTH_DIFFER,
  hors perimetre S0-002) ; le worktree porte encore la regle blanche `/docs` heritee brute
  de l'upstream v0.3.0, alors que `main` a une regle affinee (`/docs/*` + exceptions
  `!/docs/np-cadrage.md` et `!/docs/foundation/`). Reconciliation explicitement du ressort
  de S0-004 (application des diffs BOTH_DIFFER, dont `.gitignore`) — PAS touche ici pour
  respecter le perimetre strict de cette story (copie brute uniquement, zero adaptation).

Consequence pratique : le commit `[S0-002]` de cette story ajoute les 245 fichiers
nouveaux non ignores (`.ralph/progress.md` reste inchange, deja commite depuis S0-001).
Les 18 chemins ci-dessus restent presents sur le disque du worktree (l'acceptance
"264 fichiers presents sur refork-v030" est remplie au sens working-tree, verifiee par
le script lui-meme) mais pas encore dans l'historique git tant que S0-004 n'a pas
reconcilie le `.gitignore`. Point signale explicitement pour ne pas etre perdu de vue.

## Execution 2026-07-10T13:39:19

- Inventaire : `C:\projets\Qalem\refork\inventaire.json`
- Repo carriere (main) : `C:\projets\Qalem\OpenMAIC`
- Worktree cible : `C:\projets\Qalem\refork-v030-wt`
- Total OURS_ONLY attendu : 264
- Source `git show main:<path>` : 251
- Source repli filesystem (fichier non suivi sur main — gitignore/genere) : 13
  - Fichiers concernes par le repli filesystem (motif FR-1 : `git show` echoue car fichier absent de l'historique `main`, present seulement sur le disque du repo carriere) :
    - `.env.docker.example`
    - `docs/bundle-optimization-todo.md`
    - `docs/np-cadrage.md`
    - `docs/self-hosted.md`
    - `next-env.d.ts`
    - `supabase/.temp/cli-latest`
    - `supabase/.temp/gotrue-version`
    - `supabase/.temp/pooler-url`
    - `supabase/.temp/postgres-version`
    - `supabase/.temp/project-ref`
    - `supabase/.temp/rest-version`
    - `supabase/.temp/storage-migration`
    - `supabase/.temp/storage-version`
- Fichiers proteges (non ecrases, superset strict deja present dans le worktree) : 1
  - `.ralph/progress.md` : suivi actif du Ralph loop de re-fork (deja modifie par S0-001 dans ce worktree) ; le contenu main est un prefixe exact du contenu worktree (superset strict) -> ecraser detruirait l'entree Session Log S0-001
- Nouveaux fichiers ecrits : 0
- Fichiers mis a jour : 0
- Fichiers deja a jour (idempotence) : 263
- Erreurs : 0
- Verification post-copie : 264/264 conformes, ecarts = 0
- Hash sha256 de l'ensemble des 264 chemins (contenu concatene, ordre inventaire) : b8531895e4815528ad68ce9603e371f5a19667e7bb6bd0bb2a33f414472404b0

## Execution S0-004 (revue manuelle) 2026-07-10T14:23:36

Application semi-automatique des divergences BOTH_DIFFER < 30 lignes, apres REVUE MANUELLE DU CONTENU de chacune (pas seulement du nombre de lignes — voir docstring de `refork/apply_both_differ_small.py` et `refork/s0-004-decisions.json` pour la justification complete).

- Inventaire : `C:\projets\Qalem\refork\inventaire.json`
- Decisions manuelles : `C:\projets\Qalem\refork-v030-wt\refork\s0-004-decisions.json`
- Repo carriere (main) : `C:\projets\Qalem\OpenMAIC`
- Archive upstream v0.3.0 : `C:\projets\Qalem\upstream-v030`
- Worktree cible : `C:\projets\Qalem\refork-v030-wt`
- Total fichiers BOTH_DIFFER < 30 lignes : 137
- Appliques : 10
- Rejetes : 127
- No-op (deja a jour) : 0

### Detail fichier par fichier

| Fichier | Lignes diff | Decision | Motif |
|---|---|---|---|
| .github/ISSUE_TEMPLATE/config.yml | 4 | applied | Lien Discord mis a jour -- cosmetique, zero risque technique. (source=git, aucune derive detectee) |
| .github/workflows/ci.yml | 28 | applied | Config CI uniquement (n'affecte aucune commande locale tsc/lint/test/build). Retire un step 'i18n Key Alignment' + 'Unit Tests (importer)' absents de main, ajoute un step Build -- cosmetique, zero risque sur le code source. (source=git, aucune derive detectee) |
| .prettierignore | 10 | rejected | Meme famille que pnpm-workspace.yaml/tsconfig.json : retire les ignores de packages/@openmaic/*/dist, importer/src1 (vendor) et packages/docs sans les remplacer par un ignore plus large -- regression par rapport a la structure v0.3.0 reelle du worktree. |
| CONTRIBUTING.md | 11 | rejected | FR-3 / convention soft-fork Qalem : README/CONTRIBUTING/CHANGELOG herites de l'upstream ne sont JAMAIS modifies (regle absolue, independante du seuil de lignes). |
| app/api/azure-voices/route.ts | 15 | rejected | Le diff reintroduit un import depuis l'ancien chemin @/lib/types/slides alors qu'upstream v0.3.0 a extrait ces types dans le package @openmaic/dsl (reellement present et utilise ailleurs dans le worktree). Appliquer romprait la coherence d'architecture avec le reste du code v0.3.0 non touche par cette story. Echantillon verifie manuellement (6 fichiers) : chacun perd aussi une fonctionnalite ou un correctif upstream reel au-dela du seul import (ex. SpotlightOverlay perd domIdPrefix + un correctif Tailwind ; BaseImageElement revient a un pattern de resolution media plus ancien perdant une correction documentee de contamination cross-course ; chartOption.ts perd des verifications defensives ; element-fingerprint.ts perd le cas 'code' (risque de rupture d'exhaustivite de switch). |
| app/api/classroom-media/[classroomId]/[...path]/route.ts | 13 | rejected | Le diff reintroduit un import depuis l'ancien chemin @/lib/types/slides alors qu'upstream v0.3.0 a extrait ces types dans le package @openmaic/dsl (reellement present et utilise ailleurs dans le worktree). Appliquer romprait la coherence d'architecture avec le reste du code v0.3.0 non touche par cette story. Echantillon verifie manuellement (6 fichiers) : chacun perd aussi une fonctionnalite ou un correctif upstream reel au-dela du seul import (ex. SpotlightOverlay perd domIdPrefix + un correctif Tailwind ; BaseImageElement revient a un pattern de resolution media plus ancien perdant une correction documentee de contamination cross-course ; chartOption.ts perd des verifications defensives ; element-fingerprint.ts perd le cas 'code' (risque de rupture d'exhaustivite de switch). |
| app/api/generate-classroom/[jobId]/route.ts | 12 | rejected | Le diff reintroduit un import depuis l'ancien chemin @/lib/types/slides alors qu'upstream v0.3.0 a extrait ces types dans le package @openmaic/dsl (reellement present et utilise ailleurs dans le worktree). Appliquer romprait la coherence d'architecture avec le reste du code v0.3.0 non touche par cette story. Echantillon verifie manuellement (6 fichiers) : chacun perd aussi une fonctionnalite ou un correctif upstream reel au-dela du seul import (ex. SpotlightOverlay perd domIdPrefix + un correctif Tailwind ; BaseImageElement revient a un pattern de resolution media plus ancien perdant une correction documentee de contamination cross-course ; chartOption.ts perd des verifications defensives ; element-fingerprint.ts perd le cas 'code' (risque de rupture d'exhaustivite de switch). |
| app/api/health/route.ts | 4 | rejected | Regression mineure de precision (verifie Object.keys().length au lieu de values().some(!disabled)) -- pas de gain identifie, rejet par coherence de politique (main en retard, aucune necessite Qalem). |
| app/api/server-providers/route.ts | 6 | rejected | Perd le champ 'generation.parallelSceneConcurrency' ajoute par upstream -- risque de casser un type client qui l'attendrait. |
| app/generation-preview/types.ts | 23 | rejected | Perd previewPhase, languageDirective, courseTitle, taskEngineMode (cluster de fonctionnalites upstream plus recentes) et reordonne le tableau d'etapes -- perte de structure, pas une simple divergence textuelle. |
| components/agent/agent-config-panel.tsx | 8 | rejected | Perd la fonctionnalite PublishAgentDialog (publication d'agent) ajoutee par upstream. |
| components/ai-elements/prompt-input.tsx | 7 | rejected | RISQUE CONCRET DE CASSER TSC : le commentaire upstream explique que le '?' optionnel sur Window.SpeechRecognition doit rester identique aux modificateurs de l'augmentation globale de @assistant-ui/core (declaration merging TypeScript exige des modificateurs identiques). Le retirer peut provoquer un conflit de declaration globale. |
| components/canvas/canvas-area.tsx | 21 | rejected | Perd la fonctionnalite ClassroomCompletePageConnected / isCourseComplete (ecran de fin de cours) ajoutee par upstream. |
| components/chat/inline-action-tag.tsx | 6 | rejected | Perd les icones/labels wb_draw_code et wb_edit_code -- meme cluster que registry/types.ts et tool-schemas.ts. |
| components/chat/lecture-notes-view.tsx | 4 | applied | Meme raison que whiteboard-history.tsx : interpolation par .replace('{n}',...) necessaire pour les traductions Qalem a accolade simple. (source=git, aucune derive detectee) |
| components/chat/session-list.tsx | 6 | rejected | Perd le cas SessionStatus='error' (icone AlertCircle) -- coherent avec le rejet de lib/types/chat.ts (meme enum). |
| components/scene-renderers/pbl/use-pbl-chat.ts | 24 | rejected | Melange : les 2 interpolations t()->.replace() sont necessaires (meme raison que whiteboard-history.tsx) MAIS le fichier ajoute aussi un en-tete 'x-requires-api-key' cote upstream absent de main -- traite chirurgicalement hors de ce remplacement mecanique (voir note dediee dans port-log.md). |
| components/settings/provider-list.tsx | 8 | rejected | Perd la logique MONO_LOGO_PROVIDERS (inversion dark-mode des logos monochromes) ajoutee par upstream. |
| components/slide-renderer/Editor/Canvas/EditableElement.tsx | 4 | rejected | Le diff reintroduit un import depuis l'ancien chemin @/lib/types/slides alors qu'upstream v0.3.0 a extrait ces types dans le package @openmaic/dsl (reellement present et utilise ailleurs dans le worktree). Appliquer romprait la coherence d'architecture avec le reste du code v0.3.0 non touche par cette story. Echantillon verifie manuellement (6 fichiers) : chacun perd aussi une fonctionnalite ou un correctif upstream reel au-dela du seul import (ex. SpotlightOverlay perd domIdPrefix + un correctif Tailwind ; BaseImageElement revient a un pattern de resolution media plus ancien perdant une correction documentee de contamination cross-course ; chartOption.ts perd des verifications defensives ; element-fingerprint.ts perd le cas 'code' (risque de rupture d'exhaustivite de switch). |
| components/slide-renderer/Editor/Canvas/ElementCreateSelection.tsx | 10 | rejected | Perd la gestion dediee de creation d'un element texte (comportement de taille par defaut adapte) ajoutee par upstream. |
| components/slide-renderer/Editor/Canvas/GridLines.tsx | 4 | rejected | Le diff reintroduit un import depuis l'ancien chemin @/lib/types/slides alors qu'upstream v0.3.0 a extrait ces types dans le package @openmaic/dsl (reellement present et utilise ailleurs dans le worktree). Appliquer romprait la coherence d'architecture avec le reste du code v0.3.0 non touche par cette story. Echantillon verifie manuellement (6 fichiers) : chacun perd aussi une fonctionnalite ou un correctif upstream reel au-dela du seul import (ex. SpotlightOverlay perd domIdPrefix + un correctif Tailwind ; BaseImageElement revient a un pattern de resolution media plus ancien perdant une correction documentee de contamination cross-course ; chartOption.ts perd des verifications defensives ; element-fingerprint.ts perd le cas 'code' (risque de rupture d'exhaustivite de switch). |
| components/slide-renderer/Editor/Canvas/Operate/CommonElementOperate.tsx | 4 | rejected | Le diff reintroduit un import depuis l'ancien chemin @/lib/types/slides alors qu'upstream v0.3.0 a extrait ces types dans le package @openmaic/dsl (reellement present et utilise ailleurs dans le worktree). Appliquer romprait la coherence d'architecture avec le reste du code v0.3.0 non touche par cette story. Echantillon verifie manuellement (6 fichiers) : chacun perd aussi une fonctionnalite ou un correctif upstream reel au-dela du seul import (ex. SpotlightOverlay perd domIdPrefix + un correctif Tailwind ; BaseImageElement revient a un pattern de resolution media plus ancien perdant une correction documentee de contamination cross-course ; chartOption.ts perd des verifications defensives ; element-fingerprint.ts perd le cas 'code' (risque de rupture d'exhaustivite de switch). |
| components/slide-renderer/Editor/Canvas/Operate/ImageElementOperate.tsx | 4 | rejected | Le diff reintroduit un import depuis l'ancien chemin @/lib/types/slides alors qu'upstream v0.3.0 a extrait ces types dans le package @openmaic/dsl (reellement present et utilise ailleurs dans le worktree). Appliquer romprait la coherence d'architecture avec le reste du code v0.3.0 non touche par cette story. Echantillon verifie manuellement (6 fichiers) : chacun perd aussi une fonctionnalite ou un correctif upstream reel au-dela du seul import (ex. SpotlightOverlay perd domIdPrefix + un correctif Tailwind ; BaseImageElement revient a un pattern de resolution media plus ancien perdant une correction documentee de contamination cross-course ; chartOption.ts perd des verifications defensives ; element-fingerprint.ts perd le cas 'code' (risque de rupture d'exhaustivite de switch). |
| components/slide-renderer/Editor/Canvas/Operate/LineElementOperate.tsx | 4 | rejected | Le diff reintroduit un import depuis l'ancien chemin @/lib/types/slides alors qu'upstream v0.3.0 a extrait ces types dans le package @openmaic/dsl (reellement present et utilise ailleurs dans le worktree). Appliquer romprait la coherence d'architecture avec le reste du code v0.3.0 non touche par cette story. Echantillon verifie manuellement (6 fichiers) : chacun perd aussi une fonctionnalite ou un correctif upstream reel au-dela du seul import (ex. SpotlightOverlay perd domIdPrefix + un correctif Tailwind ; BaseImageElement revient a un pattern de resolution media plus ancien perdant une correction documentee de contamination cross-course ; chartOption.ts perd des verifications defensives ; element-fingerprint.ts perd le cas 'code' (risque de rupture d'exhaustivite de switch). |
| components/slide-renderer/Editor/Canvas/Operate/MultiSelectOperate.tsx | 4 | rejected | Le diff reintroduit un import depuis l'ancien chemin @/lib/types/slides alors qu'upstream v0.3.0 a extrait ces types dans le package @openmaic/dsl (reellement present et utilise ailleurs dans le worktree). Appliquer romprait la coherence d'architecture avec le reste du code v0.3.0 non touche par cette story. Echantillon verifie manuellement (6 fichiers) : chacun perd aussi une fonctionnalite ou un correctif upstream reel au-dela du seul import (ex. SpotlightOverlay perd domIdPrefix + un correctif Tailwind ; BaseImageElement revient a un pattern de resolution media plus ancien perdant une correction documentee de contamination cross-course ; chartOption.ts perd des verifications defensives ; element-fingerprint.ts perd le cas 'code' (risque de rupture d'exhaustivite de switch). |
| components/slide-renderer/Editor/Canvas/Operate/ShapeElementOperate.tsx | 4 | rejected | Le diff reintroduit un import depuis l'ancien chemin @/lib/types/slides alors qu'upstream v0.3.0 a extrait ces types dans le package @openmaic/dsl (reellement present et utilise ailleurs dans le worktree). Appliquer romprait la coherence d'architecture avec le reste du code v0.3.0 non touche par cette story. Echantillon verifie manuellement (6 fichiers) : chacun perd aussi une fonctionnalite ou un correctif upstream reel au-dela du seul import (ex. SpotlightOverlay perd domIdPrefix + un correctif Tailwind ; BaseImageElement revient a un pattern de resolution media plus ancien perdant une correction documentee de contamination cross-course ; chartOption.ts perd des verifications defensives ; element-fingerprint.ts perd le cas 'code' (risque de rupture d'exhaustivite de switch). |
| components/slide-renderer/Editor/Canvas/Operate/TableElementOperate.tsx | 4 | rejected | Le diff reintroduit un import depuis l'ancien chemin @/lib/types/slides alors qu'upstream v0.3.0 a extrait ces types dans le package @openmaic/dsl (reellement present et utilise ailleurs dans le worktree). Appliquer romprait la coherence d'architecture avec le reste du code v0.3.0 non touche par cette story. Echantillon verifie manuellement (6 fichiers) : chacun perd aussi une fonctionnalite ou un correctif upstream reel au-dela du seul import (ex. SpotlightOverlay perd domIdPrefix + un correctif Tailwind ; BaseImageElement revient a un pattern de resolution media plus ancien perdant une correction documentee de contamination cross-course ; chartOption.ts perd des verifications defensives ; element-fingerprint.ts perd le cas 'code' (risque de rupture d'exhaustivite de switch). |
| components/slide-renderer/Editor/Canvas/Operate/index.tsx | 4 | rejected | Le diff reintroduit un import depuis l'ancien chemin @/lib/types/slides alors qu'upstream v0.3.0 a extrait ces types dans le package @openmaic/dsl (reellement present et utilise ailleurs dans le worktree). Appliquer romprait la coherence d'architecture avec le reste du code v0.3.0 non touche par cette story. Echantillon verifie manuellement (6 fichiers) : chacun perd aussi une fonctionnalite ou un correctif upstream reel au-dela du seul import (ex. SpotlightOverlay perd domIdPrefix + un correctif Tailwind ; BaseImageElement revient a un pattern de resolution media plus ancien perdant une correction documentee de contamination cross-course ; chartOption.ts perd des verifications defensives ; element-fingerprint.ts perd le cas 'code' (risque de rupture d'exhaustivite de switch). |
| components/slide-renderer/Editor/Canvas/Ruler.tsx | 4 | rejected | Le diff reintroduit un import depuis l'ancien chemin @/lib/types/slides alors qu'upstream v0.3.0 a extrait ces types dans le package @openmaic/dsl (reellement present et utilise ailleurs dans le worktree). Appliquer romprait la coherence d'architecture avec le reste du code v0.3.0 non touche par cette story. Echantillon verifie manuellement (6 fichiers) : chacun perd aussi une fonctionnalite ou un correctif upstream reel au-dela du seul import (ex. SpotlightOverlay perd domIdPrefix + un correctif Tailwind ; BaseImageElement revient a un pattern de resolution media plus ancien perdant une correction documentee de contamination cross-course ; chartOption.ts perd des verifications defensives ; element-fingerprint.ts perd le cas 'code' (risque de rupture d'exhaustivite de switch). |
| components/slide-renderer/Editor/Canvas/ShapeCreateCanvas.tsx | 4 | rejected | Le diff reintroduit un import depuis l'ancien chemin @/lib/types/slides alors qu'upstream v0.3.0 a extrait ces types dans le package @openmaic/dsl (reellement present et utilise ailleurs dans le worktree). Appliquer romprait la coherence d'architecture avec le reste du code v0.3.0 non touche par cette story. Echantillon verifie manuellement (6 fichiers) : chacun perd aussi une fonctionnalite ou un correctif upstream reel au-dela du seul import (ex. SpotlightOverlay perd domIdPrefix + un correctif Tailwind ; BaseImageElement revient a un pattern de resolution media plus ancien perdant une correction documentee de contamination cross-course ; chartOption.ts perd des verifications defensives ; element-fingerprint.ts perd le cas 'code' (risque de rupture d'exhaustivite de switch). |
| components/slide-renderer/Editor/Canvas/ViewportBackground.tsx | 4 | rejected | Le diff reintroduit un import depuis l'ancien chemin @/lib/types/slides alors qu'upstream v0.3.0 a extrait ces types dans le package @openmaic/dsl (reellement present et utilise ailleurs dans le worktree). Appliquer romprait la coherence d'architecture avec le reste du code v0.3.0 non touche par cette story. Echantillon verifie manuellement (6 fichiers) : chacun perd aussi une fonctionnalite ou un correctif upstream reel au-dela du seul import (ex. SpotlightOverlay perd domIdPrefix + un correctif Tailwind ; BaseImageElement revient a un pattern de resolution media plus ancien perdant une correction documentee de contamination cross-course ; chartOption.ts perd des verifications defensives ; element-fingerprint.ts perd le cas 'code' (risque de rupture d'exhaustivite de switch). |
| components/slide-renderer/Editor/Canvas/hooks/useDragElement.ts | 4 | rejected | Le diff reintroduit un import depuis l'ancien chemin @/lib/types/slides alors qu'upstream v0.3.0 a extrait ces types dans le package @openmaic/dsl (reellement present et utilise ailleurs dans le worktree). Appliquer romprait la coherence d'architecture avec le reste du code v0.3.0 non touche par cette story. Echantillon verifie manuellement (6 fichiers) : chacun perd aussi une fonctionnalite ou un correctif upstream reel au-dela du seul import (ex. SpotlightOverlay perd domIdPrefix + un correctif Tailwind ; BaseImageElement revient a un pattern de resolution media plus ancien perdant une correction documentee de contamination cross-course ; chartOption.ts perd des verifications defensives ; element-fingerprint.ts perd le cas 'code' (risque de rupture d'exhaustivite de switch). |
| components/slide-renderer/Editor/Canvas/hooks/useDragLineElement.ts | 4 | rejected | Le diff reintroduit un import depuis l'ancien chemin @/lib/types/slides alors qu'upstream v0.3.0 a extrait ces types dans le package @openmaic/dsl (reellement present et utilise ailleurs dans le worktree). Appliquer romprait la coherence d'architecture avec le reste du code v0.3.0 non touche par cette story. Echantillon verifie manuellement (6 fichiers) : chacun perd aussi une fonctionnalite ou un correctif upstream reel au-dela du seul import (ex. SpotlightOverlay perd domIdPrefix + un correctif Tailwind ; BaseImageElement revient a un pattern de resolution media plus ancien perdant une correction documentee de contamination cross-course ; chartOption.ts perd des verifications defensives ; element-fingerprint.ts perd le cas 'code' (risque de rupture d'exhaustivite de switch). |
| components/slide-renderer/Editor/Canvas/hooks/useMouseSelection.ts | 4 | rejected | Le diff reintroduit un import depuis l'ancien chemin @/lib/types/slides alors qu'upstream v0.3.0 a extrait ces types dans le package @openmaic/dsl (reellement present et utilise ailleurs dans le worktree). Appliquer romprait la coherence d'architecture avec le reste du code v0.3.0 non touche par cette story. Echantillon verifie manuellement (6 fichiers) : chacun perd aussi une fonctionnalite ou un correctif upstream reel au-dela du seul import (ex. SpotlightOverlay perd domIdPrefix + un correctif Tailwind ; BaseImageElement revient a un pattern de resolution media plus ancien perdant une correction documentee de contamination cross-course ; chartOption.ts perd des verifications defensives ; element-fingerprint.ts perd le cas 'code' (risque de rupture d'exhaustivite de switch). |
| components/slide-renderer/Editor/Canvas/hooks/useMoveShapeKeypoint.ts | 4 | rejected | Le diff reintroduit un import depuis l'ancien chemin @/lib/types/slides alors qu'upstream v0.3.0 a extrait ces types dans le package @openmaic/dsl (reellement present et utilise ailleurs dans le worktree). Appliquer romprait la coherence d'architecture avec le reste du code v0.3.0 non touche par cette story. Echantillon verifie manuellement (6 fichiers) : chacun perd aussi une fonctionnalite ou un correctif upstream reel au-dela du seul import (ex. SpotlightOverlay perd domIdPrefix + un correctif Tailwind ; BaseImageElement revient a un pattern de resolution media plus ancien perdant une correction documentee de contamination cross-course ; chartOption.ts perd des verifications defensives ; element-fingerprint.ts perd le cas 'code' (risque de rupture d'exhaustivite de switch). |
| components/slide-renderer/Editor/Canvas/hooks/useRotateElement.ts | 4 | rejected | Le diff reintroduit un import depuis l'ancien chemin @/lib/types/slides alors qu'upstream v0.3.0 a extrait ces types dans le package @openmaic/dsl (reellement present et utilise ailleurs dans le worktree). Appliquer romprait la coherence d'architecture avec le reste du code v0.3.0 non touche par cette story. Echantillon verifie manuellement (6 fichiers) : chacun perd aussi une fonctionnalite ou un correctif upstream reel au-dela du seul import (ex. SpotlightOverlay perd domIdPrefix + un correctif Tailwind ; BaseImageElement revient a un pattern de resolution media plus ancien perdant une correction documentee de contamination cross-course ; chartOption.ts perd des verifications defensives ; element-fingerprint.ts perd le cas 'code' (risque de rupture d'exhaustivite de switch). |
| components/slide-renderer/Editor/Canvas/hooks/useScaleElement.ts | 9 | rejected | Le diff reintroduit un import depuis l'ancien chemin @/lib/types/slides alors qu'upstream v0.3.0 a extrait ces types dans le package @openmaic/dsl (reellement present et utilise ailleurs dans le worktree). Appliquer romprait la coherence d'architecture avec le reste du code v0.3.0 non touche par cette story. Echantillon verifie manuellement (6 fichiers) : chacun perd aussi une fonctionnalite ou un correctif upstream reel au-dela du seul import (ex. SpotlightOverlay perd domIdPrefix + un correctif Tailwind ; BaseImageElement revient a un pattern de resolution media plus ancien perdant une correction documentee de contamination cross-course ; chartOption.ts perd des verifications defensives ; element-fingerprint.ts perd le cas 'code' (risque de rupture d'exhaustivite de switch). |
| components/slide-renderer/Editor/Canvas/hooks/useSelectElement.ts | 4 | rejected | Le diff reintroduit un import depuis l'ancien chemin @/lib/types/slides alors qu'upstream v0.3.0 a extrait ces types dans le package @openmaic/dsl (reellement present et utilise ailleurs dans le worktree). Appliquer romprait la coherence d'architecture avec le reste du code v0.3.0 non touche par cette story. Echantillon verifie manuellement (6 fichiers) : chacun perd aussi une fonctionnalite ou un correctif upstream reel au-dela du seul import (ex. SpotlightOverlay perd domIdPrefix + un correctif Tailwind ; BaseImageElement revient a un pattern de resolution media plus ancien perdant une correction documentee de contamination cross-course ; chartOption.ts perd des verifications defensives ; element-fingerprint.ts perd le cas 'code' (risque de rupture d'exhaustivite de switch). |
| components/slide-renderer/Editor/Canvas/index.tsx | 4 | rejected | Le diff reintroduit un import depuis l'ancien chemin @/lib/types/slides alors qu'upstream v0.3.0 a extrait ces types dans le package @openmaic/dsl (reellement present et utilise ailleurs dans le worktree). Appliquer romprait la coherence d'architecture avec le reste du code v0.3.0 non touche par cette story. Echantillon verifie manuellement (6 fichiers) : chacun perd aussi une fonctionnalite ou un correctif upstream reel au-dela du seul import (ex. SpotlightOverlay perd domIdPrefix + un correctif Tailwind ; BaseImageElement revient a un pattern de resolution media plus ancien perdant une correction documentee de contamination cross-course ; chartOption.ts perd des verifications defensives ; element-fingerprint.ts perd le cas 'code' (risque de rupture d'exhaustivite de switch). |
| components/slide-renderer/Editor/HighlightOverlay.tsx | 4 | rejected | Le diff reintroduit un import depuis l'ancien chemin @/lib/types/slides alors qu'upstream v0.3.0 a extrait ces types dans le package @openmaic/dsl (reellement present et utilise ailleurs dans le worktree). Appliquer romprait la coherence d'architecture avec le reste du code v0.3.0 non touche par cette story. Echantillon verifie manuellement (6 fichiers) : chacun perd aussi une fonctionnalite ou un correctif upstream reel au-dela du seul import (ex. SpotlightOverlay perd domIdPrefix + un correctif Tailwind ; BaseImageElement revient a un pattern de resolution media plus ancien perdant une correction documentee de contamination cross-course ; chartOption.ts perd des verifications defensives ; element-fingerprint.ts perd le cas 'code' (risque de rupture d'exhaustivite de switch). |
| components/slide-renderer/Editor/ScreenCanvas.tsx | 4 | rejected | Le diff reintroduit un import depuis l'ancien chemin @/lib/types/slides alors qu'upstream v0.3.0 a extrait ces types dans le package @openmaic/dsl (reellement present et utilise ailleurs dans le worktree). Appliquer romprait la coherence d'architecture avec le reste du code v0.3.0 non touche par cette story. Echantillon verifie manuellement (6 fichiers) : chacun perd aussi une fonctionnalite ou un correctif upstream reel au-dela du seul import (ex. SpotlightOverlay perd domIdPrefix + un correctif Tailwind ; BaseImageElement revient a un pattern de resolution media plus ancien perdant une correction documentee de contamination cross-course ; chartOption.ts perd des verifications defensives ; element-fingerprint.ts perd le cas 'code' (risque de rupture d'exhaustivite de switch). |
| components/slide-renderer/Editor/ScreenElement.tsx | 6 | rejected | Le diff reintroduit un import depuis l'ancien chemin @/lib/types/slides alors qu'upstream v0.3.0 a extrait ces types dans le package @openmaic/dsl (reellement present et utilise ailleurs dans le worktree). Appliquer romprait la coherence d'architecture avec le reste du code v0.3.0 non touche par cette story. Echantillon verifie manuellement (6 fichiers) : chacun perd aussi une fonctionnalite ou un correctif upstream reel au-dela du seul import (ex. SpotlightOverlay perd domIdPrefix + un correctif Tailwind ; BaseImageElement revient a un pattern de resolution media plus ancien perdant une correction documentee de contamination cross-course ; chartOption.ts perd des verifications defensives ; element-fingerprint.ts perd le cas 'code' (risque de rupture d'exhaustivite de switch). |
| components/slide-renderer/Editor/SpotlightOverlay.tsx | 28 | rejected | Le diff reintroduit un import depuis l'ancien chemin @/lib/types/slides alors qu'upstream v0.3.0 a extrait ces types dans le package @openmaic/dsl (reellement present et utilise ailleurs dans le worktree). Appliquer romprait la coherence d'architecture avec le reste du code v0.3.0 non touche par cette story. Echantillon verifie manuellement (6 fichiers) : chacun perd aussi une fonctionnalite ou un correctif upstream reel au-dela du seul import (ex. SpotlightOverlay perd domIdPrefix + un correctif Tailwind ; BaseImageElement revient a un pattern de resolution media plus ancien perdant une correction documentee de contamination cross-course ; chartOption.ts perd des verifications defensives ; element-fingerprint.ts perd le cas 'code' (risque de rupture d'exhaustivite de switch). |
| components/slide-renderer/components/element/ChartElement/BaseChartElement.tsx | 4 | rejected | Le diff reintroduit un import depuis l'ancien chemin @/lib/types/slides alors qu'upstream v0.3.0 a extrait ces types dans le package @openmaic/dsl (reellement present et utilise ailleurs dans le worktree). Appliquer romprait la coherence d'architecture avec le reste du code v0.3.0 non touche par cette story. Echantillon verifie manuellement (6 fichiers) : chacun perd aussi une fonctionnalite ou un correctif upstream reel au-dela du seul import (ex. SpotlightOverlay perd domIdPrefix + un correctif Tailwind ; BaseImageElement revient a un pattern de resolution media plus ancien perdant une correction documentee de contamination cross-course ; chartOption.ts perd des verifications defensives ; element-fingerprint.ts perd le cas 'code' (risque de rupture d'exhaustivite de switch). |
| components/slide-renderer/components/element/ChartElement/Chart.tsx | 4 | rejected | Le diff reintroduit un import depuis l'ancien chemin @/lib/types/slides alors qu'upstream v0.3.0 a extrait ces types dans le package @openmaic/dsl (reellement present et utilise ailleurs dans le worktree). Appliquer romprait la coherence d'architecture avec le reste du code v0.3.0 non touche par cette story. Echantillon verifie manuellement (6 fichiers) : chacun perd aussi une fonctionnalite ou un correctif upstream reel au-dela du seul import (ex. SpotlightOverlay perd domIdPrefix + un correctif Tailwind ; BaseImageElement revient a un pattern de resolution media plus ancien perdant une correction documentee de contamination cross-course ; chartOption.ts perd des verifications defensives ; element-fingerprint.ts perd le cas 'code' (risque de rupture d'exhaustivite de switch). |
| components/slide-renderer/components/element/ChartElement/chartOption.ts | 25 | rejected | Le diff reintroduit un import depuis l'ancien chemin @/lib/types/slides alors qu'upstream v0.3.0 a extrait ces types dans le package @openmaic/dsl (reellement present et utilise ailleurs dans le worktree). Appliquer romprait la coherence d'architecture avec le reste du code v0.3.0 non touche par cette story. Echantillon verifie manuellement (6 fichiers) : chacun perd aussi une fonctionnalite ou un correctif upstream reel au-dela du seul import (ex. SpotlightOverlay perd domIdPrefix + un correctif Tailwind ; BaseImageElement revient a un pattern de resolution media plus ancien perdant une correction documentee de contamination cross-course ; chartOption.ts perd des verifications defensives ; element-fingerprint.ts perd le cas 'code' (risque de rupture d'exhaustivite de switch). |
| components/slide-renderer/components/element/ChartElement/index.tsx | 4 | rejected | Le diff reintroduit un import depuis l'ancien chemin @/lib/types/slides alors qu'upstream v0.3.0 a extrait ces types dans le package @openmaic/dsl (reellement present et utilise ailleurs dans le worktree). Appliquer romprait la coherence d'architecture avec le reste du code v0.3.0 non touche par cette story. Echantillon verifie manuellement (6 fichiers) : chacun perd aussi une fonctionnalite ou un correctif upstream reel au-dela du seul import (ex. SpotlightOverlay perd domIdPrefix + un correctif Tailwind ; BaseImageElement revient a un pattern de resolution media plus ancien perdant une correction documentee de contamination cross-course ; chartOption.ts perd des verifications defensives ; element-fingerprint.ts perd le cas 'code' (risque de rupture d'exhaustivite de switch). |
| components/slide-renderer/components/element/ElementOutline.tsx | 4 | rejected | Le diff reintroduit un import depuis l'ancien chemin @/lib/types/slides alors qu'upstream v0.3.0 a extrait ces types dans le package @openmaic/dsl (reellement present et utilise ailleurs dans le worktree). Appliquer romprait la coherence d'architecture avec le reste du code v0.3.0 non touche par cette story. Echantillon verifie manuellement (6 fichiers) : chacun perd aussi une fonctionnalite ou un correctif upstream reel au-dela du seul import (ex. SpotlightOverlay perd domIdPrefix + un correctif Tailwind ; BaseImageElement revient a un pattern de resolution media plus ancien perdant une correction documentee de contamination cross-course ; chartOption.ts perd des verifications defensives ; element-fingerprint.ts perd le cas 'code' (risque de rupture d'exhaustivite de switch). |
| components/slide-renderer/components/element/ImageElement/BaseImageElement.tsx | 26 | rejected | Le diff reintroduit un import depuis l'ancien chemin @/lib/types/slides alors qu'upstream v0.3.0 a extrait ces types dans le package @openmaic/dsl (reellement present et utilise ailleurs dans le worktree). Appliquer romprait la coherence d'architecture avec le reste du code v0.3.0 non touche par cette story. Echantillon verifie manuellement (6 fichiers) : chacun perd aussi une fonctionnalite ou un correctif upstream reel au-dela du seul import (ex. SpotlightOverlay perd domIdPrefix + un correctif Tailwind ; BaseImageElement revient a un pattern de resolution media plus ancien perdant une correction documentee de contamination cross-course ; chartOption.ts perd des verifications defensives ; element-fingerprint.ts perd le cas 'code' (risque de rupture d'exhaustivite de switch). |
| components/slide-renderer/components/element/ImageElement/ImageClipHandler.tsx | 4 | rejected | Le diff reintroduit un import depuis l'ancien chemin @/lib/types/slides alors qu'upstream v0.3.0 a extrait ces types dans le package @openmaic/dsl (reellement present et utilise ailleurs dans le worktree). Appliquer romprait la coherence d'architecture avec le reste du code v0.3.0 non touche par cette story. Echantillon verifie manuellement (6 fichiers) : chacun perd aussi une fonctionnalite ou un correctif upstream reel au-dela du seul import (ex. SpotlightOverlay perd domIdPrefix + un correctif Tailwind ; BaseImageElement revient a un pattern de resolution media plus ancien perdant une correction documentee de contamination cross-course ; chartOption.ts perd des verifications defensives ; element-fingerprint.ts perd le cas 'code' (risque de rupture d'exhaustivite de switch). |
| components/slide-renderer/components/element/ImageElement/ImageOutline/image-ellipse-outline.tsx | 4 | rejected | Le diff reintroduit un import depuis l'ancien chemin @/lib/types/slides alors qu'upstream v0.3.0 a extrait ces types dans le package @openmaic/dsl (reellement present et utilise ailleurs dans le worktree). Appliquer romprait la coherence d'architecture avec le reste du code v0.3.0 non touche par cette story. Echantillon verifie manuellement (6 fichiers) : chacun perd aussi une fonctionnalite ou un correctif upstream reel au-dela du seul import (ex. SpotlightOverlay perd domIdPrefix + un correctif Tailwind ; BaseImageElement revient a un pattern de resolution media plus ancien perdant une correction documentee de contamination cross-course ; chartOption.ts perd des verifications defensives ; element-fingerprint.ts perd le cas 'code' (risque de rupture d'exhaustivite de switch). |
| components/slide-renderer/components/element/ImageElement/ImageOutline/image-polygon-outline.tsx | 4 | rejected | Le diff reintroduit un import depuis l'ancien chemin @/lib/types/slides alors qu'upstream v0.3.0 a extrait ces types dans le package @openmaic/dsl (reellement present et utilise ailleurs dans le worktree). Appliquer romprait la coherence d'architecture avec le reste du code v0.3.0 non touche par cette story. Echantillon verifie manuellement (6 fichiers) : chacun perd aussi une fonctionnalite ou un correctif upstream reel au-dela du seul import (ex. SpotlightOverlay perd domIdPrefix + un correctif Tailwind ; BaseImageElement revient a un pattern de resolution media plus ancien perdant une correction documentee de contamination cross-course ; chartOption.ts perd des verifications defensives ; element-fingerprint.ts perd le cas 'code' (risque de rupture d'exhaustivite de switch). |
| components/slide-renderer/components/element/ImageElement/ImageOutline/image-rect-outline.tsx | 4 | rejected | Le diff reintroduit un import depuis l'ancien chemin @/lib/types/slides alors qu'upstream v0.3.0 a extrait ces types dans le package @openmaic/dsl (reellement present et utilise ailleurs dans le worktree). Appliquer romprait la coherence d'architecture avec le reste du code v0.3.0 non touche par cette story. Echantillon verifie manuellement (6 fichiers) : chacun perd aussi une fonctionnalite ou un correctif upstream reel au-dela du seul import (ex. SpotlightOverlay perd domIdPrefix + un correctif Tailwind ; BaseImageElement revient a un pattern de resolution media plus ancien perdant une correction documentee de contamination cross-course ; chartOption.ts perd des verifications defensives ; element-fingerprint.ts perd le cas 'code' (risque de rupture d'exhaustivite de switch). |
| components/slide-renderer/components/element/ImageElement/ImageOutline/index.tsx | 4 | rejected | Le diff reintroduit un import depuis l'ancien chemin @/lib/types/slides alors qu'upstream v0.3.0 a extrait ces types dans le package @openmaic/dsl (reellement present et utilise ailleurs dans le worktree). Appliquer romprait la coherence d'architecture avec le reste du code v0.3.0 non touche par cette story. Echantillon verifie manuellement (6 fichiers) : chacun perd aussi une fonctionnalite ou un correctif upstream reel au-dela du seul import (ex. SpotlightOverlay perd domIdPrefix + un correctif Tailwind ; BaseImageElement revient a un pattern de resolution media plus ancien perdant une correction documentee de contamination cross-course ; chartOption.ts perd des verifications defensives ; element-fingerprint.ts perd le cas 'code' (risque de rupture d'exhaustivite de switch). |
| components/slide-renderer/components/element/ImageElement/index.tsx | 15 | rejected | Le diff reintroduit un import depuis l'ancien chemin @/lib/types/slides alors qu'upstream v0.3.0 a extrait ces types dans le package @openmaic/dsl (reellement present et utilise ailleurs dans le worktree). Appliquer romprait la coherence d'architecture avec le reste du code v0.3.0 non touche par cette story. Echantillon verifie manuellement (6 fichiers) : chacun perd aussi une fonctionnalite ou un correctif upstream reel au-dela du seul import (ex. SpotlightOverlay perd domIdPrefix + un correctif Tailwind ; BaseImageElement revient a un pattern de resolution media plus ancien perdant une correction documentee de contamination cross-course ; chartOption.ts perd des verifications defensives ; element-fingerprint.ts perd le cas 'code' (risque de rupture d'exhaustivite de switch). |
| components/slide-renderer/components/element/ImageElement/useClipImage.ts | 4 | rejected | Le diff reintroduit un import depuis l'ancien chemin @/lib/types/slides alors qu'upstream v0.3.0 a extrait ces types dans le package @openmaic/dsl (reellement present et utilise ailleurs dans le worktree). Appliquer romprait la coherence d'architecture avec le reste du code v0.3.0 non touche par cette story. Echantillon verifie manuellement (6 fichiers) : chacun perd aussi une fonctionnalite ou un correctif upstream reel au-dela du seul import (ex. SpotlightOverlay perd domIdPrefix + un correctif Tailwind ; BaseImageElement revient a un pattern de resolution media plus ancien perdant une correction documentee de contamination cross-course ; chartOption.ts perd des verifications defensives ; element-fingerprint.ts perd le cas 'code' (risque de rupture d'exhaustivite de switch). |
| components/slide-renderer/components/element/ImageElement/useFilter.ts | 4 | rejected | Le diff reintroduit un import depuis l'ancien chemin @/lib/types/slides alors qu'upstream v0.3.0 a extrait ces types dans le package @openmaic/dsl (reellement present et utilise ailleurs dans le worktree). Appliquer romprait la coherence d'architecture avec le reste du code v0.3.0 non touche par cette story. Echantillon verifie manuellement (6 fichiers) : chacun perd aussi une fonctionnalite ou un correctif upstream reel au-dela du seul import (ex. SpotlightOverlay perd domIdPrefix + un correctif Tailwind ; BaseImageElement revient a un pattern de resolution media plus ancien perdant une correction documentee de contamination cross-course ; chartOption.ts perd des verifications defensives ; element-fingerprint.ts perd le cas 'code' (risque de rupture d'exhaustivite de switch). |
| components/slide-renderer/components/element/LatexElement/BaseLatexElement.tsx | 4 | rejected | Le diff reintroduit un import depuis l'ancien chemin @/lib/types/slides alors qu'upstream v0.3.0 a extrait ces types dans le package @openmaic/dsl (reellement present et utilise ailleurs dans le worktree). Appliquer romprait la coherence d'architecture avec le reste du code v0.3.0 non touche par cette story. Echantillon verifie manuellement (6 fichiers) : chacun perd aussi une fonctionnalite ou un correctif upstream reel au-dela du seul import (ex. SpotlightOverlay perd domIdPrefix + un correctif Tailwind ; BaseImageElement revient a un pattern de resolution media plus ancien perdant une correction documentee de contamination cross-course ; chartOption.ts perd des verifications defensives ; element-fingerprint.ts perd le cas 'code' (risque de rupture d'exhaustivite de switch). |
| components/slide-renderer/components/element/LatexElement/index.tsx | 4 | rejected | Le diff reintroduit un import depuis l'ancien chemin @/lib/types/slides alors qu'upstream v0.3.0 a extrait ces types dans le package @openmaic/dsl (reellement present et utilise ailleurs dans le worktree). Appliquer romprait la coherence d'architecture avec le reste du code v0.3.0 non touche par cette story. Echantillon verifie manuellement (6 fichiers) : chacun perd aussi une fonctionnalite ou un correctif upstream reel au-dela du seul import (ex. SpotlightOverlay perd domIdPrefix + un correctif Tailwind ; BaseImageElement revient a un pattern de resolution media plus ancien perdant une correction documentee de contamination cross-course ; chartOption.ts perd des verifications defensives ; element-fingerprint.ts perd le cas 'code' (risque de rupture d'exhaustivite de switch). |
| components/slide-renderer/components/element/LineElement/BaseLineElement.tsx | 4 | rejected | Le diff reintroduit un import depuis l'ancien chemin @/lib/types/slides alors qu'upstream v0.3.0 a extrait ces types dans le package @openmaic/dsl (reellement present et utilise ailleurs dans le worktree). Appliquer romprait la coherence d'architecture avec le reste du code v0.3.0 non touche par cette story. Echantillon verifie manuellement (6 fichiers) : chacun perd aussi une fonctionnalite ou un correctif upstream reel au-dela du seul import (ex. SpotlightOverlay perd domIdPrefix + un correctif Tailwind ; BaseImageElement revient a un pattern de resolution media plus ancien perdant une correction documentee de contamination cross-course ; chartOption.ts perd des verifications defensives ; element-fingerprint.ts perd le cas 'code' (risque de rupture d'exhaustivite de switch). |
| components/slide-renderer/components/element/LineElement/LinePointMarker.tsx | 4 | rejected | Le diff reintroduit un import depuis l'ancien chemin @/lib/types/slides alors qu'upstream v0.3.0 a extrait ces types dans le package @openmaic/dsl (reellement present et utilise ailleurs dans le worktree). Appliquer romprait la coherence d'architecture avec le reste du code v0.3.0 non touche par cette story. Echantillon verifie manuellement (6 fichiers) : chacun perd aussi une fonctionnalite ou un correctif upstream reel au-dela du seul import (ex. SpotlightOverlay perd domIdPrefix + un correctif Tailwind ; BaseImageElement revient a un pattern de resolution media plus ancien perdant une correction documentee de contamination cross-course ; chartOption.ts perd des verifications defensives ; element-fingerprint.ts perd le cas 'code' (risque de rupture d'exhaustivite de switch). |
| components/slide-renderer/components/element/LineElement/index.tsx | 4 | rejected | Le diff reintroduit un import depuis l'ancien chemin @/lib/types/slides alors qu'upstream v0.3.0 a extrait ces types dans le package @openmaic/dsl (reellement present et utilise ailleurs dans le worktree). Appliquer romprait la coherence d'architecture avec le reste du code v0.3.0 non touche par cette story. Echantillon verifie manuellement (6 fichiers) : chacun perd aussi une fonctionnalite ou un correctif upstream reel au-dela du seul import (ex. SpotlightOverlay perd domIdPrefix + un correctif Tailwind ; BaseImageElement revient a un pattern de resolution media plus ancien perdant une correction documentee de contamination cross-course ; chartOption.ts perd des verifications defensives ; element-fingerprint.ts perd le cas 'code' (risque de rupture d'exhaustivite de switch). |
| components/slide-renderer/components/element/ShapeElement/BaseShapeElement.tsx | 4 | rejected | Le diff reintroduit un import depuis l'ancien chemin @/lib/types/slides alors qu'upstream v0.3.0 a extrait ces types dans le package @openmaic/dsl (reellement present et utilise ailleurs dans le worktree). Appliquer romprait la coherence d'architecture avec le reste du code v0.3.0 non touche par cette story. Echantillon verifie manuellement (6 fichiers) : chacun perd aussi une fonctionnalite ou un correctif upstream reel au-dela du seul import (ex. SpotlightOverlay perd domIdPrefix + un correctif Tailwind ; BaseImageElement revient a un pattern de resolution media plus ancien perdant une correction documentee de contamination cross-course ; chartOption.ts perd des verifications defensives ; element-fingerprint.ts perd le cas 'code' (risque de rupture d'exhaustivite de switch). |
| components/slide-renderer/components/element/ShapeElement/GradientDefs.tsx | 4 | rejected | Le diff reintroduit un import depuis l'ancien chemin @/lib/types/slides alors qu'upstream v0.3.0 a extrait ces types dans le package @openmaic/dsl (reellement present et utilise ailleurs dans le worktree). Appliquer romprait la coherence d'architecture avec le reste du code v0.3.0 non touche par cette story. Echantillon verifie manuellement (6 fichiers) : chacun perd aussi une fonctionnalite ou un correctif upstream reel au-dela du seul import (ex. SpotlightOverlay perd domIdPrefix + un correctif Tailwind ; BaseImageElement revient a un pattern de resolution media plus ancien perdant une correction documentee de contamination cross-course ; chartOption.ts perd des verifications defensives ; element-fingerprint.ts perd le cas 'code' (risque de rupture d'exhaustivite de switch). |
| components/slide-renderer/components/element/ShapeElement/index.tsx | 4 | rejected | Le diff reintroduit un import depuis l'ancien chemin @/lib/types/slides alors qu'upstream v0.3.0 a extrait ces types dans le package @openmaic/dsl (reellement present et utilise ailleurs dans le worktree). Appliquer romprait la coherence d'architecture avec le reste du code v0.3.0 non touche par cette story. Echantillon verifie manuellement (6 fichiers) : chacun perd aussi une fonctionnalite ou un correctif upstream reel au-dela du seul import (ex. SpotlightOverlay perd domIdPrefix + un correctif Tailwind ; BaseImageElement revient a un pattern de resolution media plus ancien perdant une correction documentee de contamination cross-course ; chartOption.ts perd des verifications defensives ; element-fingerprint.ts perd le cas 'code' (risque de rupture d'exhaustivite de switch). |
| components/slide-renderer/components/element/TableElement/BaseTableElement.tsx | 4 | rejected | Le diff reintroduit un import depuis l'ancien chemin @/lib/types/slides alors qu'upstream v0.3.0 a extrait ces types dans le package @openmaic/dsl (reellement present et utilise ailleurs dans le worktree). Appliquer romprait la coherence d'architecture avec le reste du code v0.3.0 non touche par cette story. Echantillon verifie manuellement (6 fichiers) : chacun perd aussi une fonctionnalite ou un correctif upstream reel au-dela du seul import (ex. SpotlightOverlay perd domIdPrefix + un correctif Tailwind ; BaseImageElement revient a un pattern de resolution media plus ancien perdant une correction documentee de contamination cross-course ; chartOption.ts perd des verifications defensives ; element-fingerprint.ts perd le cas 'code' (risque de rupture d'exhaustivite de switch). |
| components/slide-renderer/components/element/TableElement/StaticTable.tsx | 4 | rejected | Le diff reintroduit un import depuis l'ancien chemin @/lib/types/slides alors qu'upstream v0.3.0 a extrait ces types dans le package @openmaic/dsl (reellement present et utilise ailleurs dans le worktree). Appliquer romprait la coherence d'architecture avec le reste du code v0.3.0 non touche par cette story. Echantillon verifie manuellement (6 fichiers) : chacun perd aussi une fonctionnalite ou un correctif upstream reel au-dela du seul import (ex. SpotlightOverlay perd domIdPrefix + un correctif Tailwind ; BaseImageElement revient a un pattern de resolution media plus ancien perdant une correction documentee de contamination cross-course ; chartOption.ts perd des verifications defensives ; element-fingerprint.ts perd le cas 'code' (risque de rupture d'exhaustivite de switch). |
| components/slide-renderer/components/element/TableElement/index.tsx | 4 | rejected | Le diff reintroduit un import depuis l'ancien chemin @/lib/types/slides alors qu'upstream v0.3.0 a extrait ces types dans le package @openmaic/dsl (reellement present et utilise ailleurs dans le worktree). Appliquer romprait la coherence d'architecture avec le reste du code v0.3.0 non touche par cette story. Echantillon verifie manuellement (6 fichiers) : chacun perd aussi une fonctionnalite ou un correctif upstream reel au-dela du seul import (ex. SpotlightOverlay perd domIdPrefix + un correctif Tailwind ; BaseImageElement revient a un pattern de resolution media plus ancien perdant une correction documentee de contamination cross-course ; chartOption.ts perd des verifications defensives ; element-fingerprint.ts perd le cas 'code' (risque de rupture d'exhaustivite de switch). |
| components/slide-renderer/components/element/TableElement/tableUtils.ts | 4 | rejected | Le diff reintroduit un import depuis l'ancien chemin @/lib/types/slides alors qu'upstream v0.3.0 a extrait ces types dans le package @openmaic/dsl (reellement present et utilise ailleurs dans le worktree). Appliquer romprait la coherence d'architecture avec le reste du code v0.3.0 non touche par cette story. Echantillon verifie manuellement (6 fichiers) : chacun perd aussi une fonctionnalite ou un correctif upstream reel au-dela du seul import (ex. SpotlightOverlay perd domIdPrefix + un correctif Tailwind ; BaseImageElement revient a un pattern de resolution media plus ancien perdant une correction documentee de contamination cross-course ; chartOption.ts perd des verifications defensives ; element-fingerprint.ts perd le cas 'code' (risque de rupture d'exhaustivite de switch). |
| components/slide-renderer/components/element/TextElement/BaseTextElement.tsx | 4 | rejected | Le diff reintroduit un import depuis l'ancien chemin @/lib/types/slides alors qu'upstream v0.3.0 a extrait ces types dans le package @openmaic/dsl (reellement present et utilise ailleurs dans le worktree). Appliquer romprait la coherence d'architecture avec le reste du code v0.3.0 non touche par cette story. Echantillon verifie manuellement (6 fichiers) : chacun perd aussi une fonctionnalite ou un correctif upstream reel au-dela du seul import (ex. SpotlightOverlay perd domIdPrefix + un correctif Tailwind ; BaseImageElement revient a un pattern de resolution media plus ancien perdant une correction documentee de contamination cross-course ; chartOption.ts perd des verifications defensives ; element-fingerprint.ts perd le cas 'code' (risque de rupture d'exhaustivite de switch). |
| components/slide-renderer/components/element/TextElement/index.tsx | 4 | rejected | Le diff reintroduit un import depuis l'ancien chemin @/lib/types/slides alors qu'upstream v0.3.0 a extrait ces types dans le package @openmaic/dsl (reellement present et utilise ailleurs dans le worktree). Appliquer romprait la coherence d'architecture avec le reste du code v0.3.0 non touche par cette story. Echantillon verifie manuellement (6 fichiers) : chacun perd aussi une fonctionnalite ou un correctif upstream reel au-dela du seul import (ex. SpotlightOverlay perd domIdPrefix + un correctif Tailwind ; BaseImageElement revient a un pattern de resolution media plus ancien perdant une correction documentee de contamination cross-course ; chartOption.ts perd des verifications defensives ; element-fingerprint.ts perd le cas 'code' (risque de rupture d'exhaustivite de switch). |
| components/slide-renderer/components/element/VideoElement/index.tsx | 4 | rejected | Le diff reintroduit un import depuis l'ancien chemin @/lib/types/slides alors qu'upstream v0.3.0 a extrait ces types dans le package @openmaic/dsl (reellement present et utilise ailleurs dans le worktree). Appliquer romprait la coherence d'architecture avec le reste du code v0.3.0 non touche par cette story. Echantillon verifie manuellement (6 fichiers) : chacun perd aussi une fonctionnalite ou un correctif upstream reel au-dela du seul import (ex. SpotlightOverlay perd domIdPrefix + un correctif Tailwind ; BaseImageElement revient a un pattern de resolution media plus ancien perdant une correction documentee de contamination cross-course ; chartOption.ts perd des verifications defensives ; element-fingerprint.ts perd le cas 'code' (risque de rupture d'exhaustivite de switch). |
| components/slide-renderer/components/element/hooks/useElementFill.ts | 4 | rejected | Le diff reintroduit un import depuis l'ancien chemin @/lib/types/slides alors qu'upstream v0.3.0 a extrait ces types dans le package @openmaic/dsl (reellement present et utilise ailleurs dans le worktree). Appliquer romprait la coherence d'architecture avec le reste du code v0.3.0 non touche par cette story. Echantillon verifie manuellement (6 fichiers) : chacun perd aussi une fonctionnalite ou un correctif upstream reel au-dela du seul import (ex. SpotlightOverlay perd domIdPrefix + un correctif Tailwind ; BaseImageElement revient a un pattern de resolution media plus ancien perdant une correction documentee de contamination cross-course ; chartOption.ts perd des verifications defensives ; element-fingerprint.ts perd le cas 'code' (risque de rupture d'exhaustivite de switch). |
| components/slide-renderer/components/element/hooks/useElementOutline.ts | 4 | rejected | Le diff reintroduit un import depuis l'ancien chemin @/lib/types/slides alors qu'upstream v0.3.0 a extrait ces types dans le package @openmaic/dsl (reellement present et utilise ailleurs dans le worktree). Appliquer romprait la coherence d'architecture avec le reste du code v0.3.0 non touche par cette story. Echantillon verifie manuellement (6 fichiers) : chacun perd aussi une fonctionnalite ou un correctif upstream reel au-dela du seul import (ex. SpotlightOverlay perd domIdPrefix + un correctif Tailwind ; BaseImageElement revient a un pattern de resolution media plus ancien perdant une correction documentee de contamination cross-course ; chartOption.ts perd des verifications defensives ; element-fingerprint.ts perd le cas 'code' (risque de rupture d'exhaustivite de switch). |
| components/slide-renderer/components/element/hooks/useElementShadow.ts | 4 | rejected | Le diff reintroduit un import depuis l'ancien chemin @/lib/types/slides alors qu'upstream v0.3.0 a extrait ces types dans le package @openmaic/dsl (reellement present et utilise ailleurs dans le worktree). Appliquer romprait la coherence d'architecture avec le reste du code v0.3.0 non touche par cette story. Echantillon verifie manuellement (6 fichiers) : chacun perd aussi une fonctionnalite ou un correctif upstream reel au-dela du seul import (ex. SpotlightOverlay perd domIdPrefix + un correctif Tailwind ; BaseImageElement revient a un pattern de resolution media plus ancien perdant une correction documentee de contamination cross-course ; chartOption.ts perd des verifications defensives ; element-fingerprint.ts perd le cas 'code' (risque de rupture d'exhaustivite de switch). |
| components/stage/scene-renderer.tsx | 17 | rejected | Perd le support du type de scene 'plugin' (PluginRenderer) ajoute par upstream -- perte de fonctionnalite entiere, pas une divergence cosmetique. |
| components/ui/popover.tsx | 8 | rejected | Retire les exports PopoverAnchor/PopoverClose ajoutes par upstream -- risque de casser d'autres fichiers (hors perimetre) qui les importeraient. |
| components/whiteboard/whiteboard-canvas.tsx | 14 | rejected | Le diff reintroduit un import depuis l'ancien chemin @/lib/types/slides alors qu'upstream v0.3.0 a extrait ces types dans le package @openmaic/dsl (reellement present et utilise ailleurs dans le worktree). Appliquer romprait la coherence d'architecture avec le reste du code v0.3.0 non touche par cette story. Echantillon verifie manuellement (6 fichiers) : chacun perd aussi une fonctionnalite ou un correctif upstream reel au-dela du seul import (ex. SpotlightOverlay perd domIdPrefix + un correctif Tailwind ; BaseImageElement revient a un pattern de resolution media plus ancien perdant une correction documentee de contamination cross-course ; chartOption.ts perd des verifications defensives ; element-fingerprint.ts perd le cas 'code' (risque de rupture d'exhaustivite de switch). |
| components/whiteboard/whiteboard-history.tsx | 7 | applied | Coherence i18n : les traductions Qalem deja portees (lib/i18n/stage.ts) utilisent des placeholders a accolade simple '{n}' (pas '{{n}}' i18next). Le t(key,{n}) upstream ne les interpolerait pas (react-i18next attend '{{n}}') -- le .replace('{n}',...) de main est necessaire pour un affichage correct. (source=git, aucune derive detectee) |
| configs/animation.ts | 4 | rejected | Le diff reintroduit un import depuis l'ancien chemin @/lib/types/slides alors qu'upstream v0.3.0 a extrait ces types dans le package @openmaic/dsl (reellement present et utilise ailleurs dans le worktree). Appliquer romprait la coherence d'architecture avec le reste du code v0.3.0 non touche par cette story. Echantillon verifie manuellement (6 fichiers) : chacun perd aussi une fonctionnalite ou un correctif upstream reel au-dela du seul import (ex. SpotlightOverlay perd domIdPrefix + un correctif Tailwind ; BaseImageElement revient a un pattern de resolution media plus ancien perdant une correction documentee de contamination cross-course ; chartOption.ts perd des verifications defensives ; element-fingerprint.ts perd le cas 'code' (risque de rupture d'exhaustivite de switch). |
| configs/chart.ts | 4 | rejected | Le diff reintroduit un import depuis l'ancien chemin @/lib/types/slides alors qu'upstream v0.3.0 a extrait ces types dans le package @openmaic/dsl (reellement present et utilise ailleurs dans le worktree). Appliquer romprait la coherence d'architecture avec le reste du code v0.3.0 non touche par cette story. Echantillon verifie manuellement (6 fichiers) : chacun perd aussi une fonctionnalite ou un correctif upstream reel au-dela du seul import (ex. SpotlightOverlay perd domIdPrefix + un correctif Tailwind ; BaseImageElement revient a un pattern de resolution media plus ancien perdant une correction documentee de contamination cross-course ; chartOption.ts perd des verifications defensives ; element-fingerprint.ts perd le cas 'code' (risque de rupture d'exhaustivite de switch). |
| configs/lines.ts | 4 | rejected | Le diff reintroduit un import depuis l'ancien chemin @/lib/types/slides alors qu'upstream v0.3.0 a extrait ces types dans le package @openmaic/dsl (reellement present et utilise ailleurs dans le worktree). Appliquer romprait la coherence d'architecture avec le reste du code v0.3.0 non touche par cette story. Echantillon verifie manuellement (6 fichiers) : chacun perd aussi une fonctionnalite ou un correctif upstream reel au-dela du seul import (ex. SpotlightOverlay perd domIdPrefix + un correctif Tailwind ; BaseImageElement revient a un pattern de resolution media plus ancien perdant une correction documentee de contamination cross-course ; chartOption.ts perd des verifications defensives ; element-fingerprint.ts perd le cas 'code' (risque de rupture d'exhaustivite de switch). |
| configs/shapes.ts | 4 | rejected | Le diff reintroduit un import depuis l'ancien chemin @/lib/types/slides alors qu'upstream v0.3.0 a extrait ces types dans le package @openmaic/dsl (reellement present et utilise ailleurs dans le worktree). Appliquer romprait la coherence d'architecture avec le reste du code v0.3.0 non touche par cette story. Echantillon verifie manuellement (6 fichiers) : chacun perd aussi une fonctionnalite ou un correctif upstream reel au-dela du seul import (ex. SpotlightOverlay perd domIdPrefix + un correctif Tailwind ; BaseImageElement revient a un pattern de resolution media plus ancien perdant une correction documentee de contamination cross-course ; chartOption.ts perd des verifications defensives ; element-fingerprint.ts perd le cas 'code' (risque de rupture d'exhaustivite de switch). |
| configs/theme.ts | 4 | rejected | Le diff reintroduit un import depuis l'ancien chemin @/lib/types/slides alors qu'upstream v0.3.0 a extrait ces types dans le package @openmaic/dsl (reellement present et utilise ailleurs dans le worktree). Appliquer romprait la coherence d'architecture avec le reste du code v0.3.0 non touche par cette story. Echantillon verifie manuellement (6 fichiers) : chacun perd aussi une fonctionnalite ou un correctif upstream reel au-dela du seul import (ex. SpotlightOverlay perd domIdPrefix + un correctif Tailwind ; BaseImageElement revient a un pattern de resolution media plus ancien perdant une correction documentee de contamination cross-course ; chartOption.ts perd des verifications defensives ; element-fingerprint.ts perd le cas 'code' (risque de rupture d'exhaustivite de switch). |
| e2e/fixtures/mock-api.ts | 27 | rejected | Upstream a ajoute l'extraction automatique de stageId depuis le corps de requete (robustesse) que main n'a pas encore -- fichier de test uniquement mais retrait d'une fonctionnalite reelle sans necessite identifiee ; rejet par coherence de politique. |
| e2e/fixtures/test-data/scene-content.ts | 6 | rejected | Le diff reintroduit un import depuis l'ancien chemin @/lib/types/slides alors qu'upstream v0.3.0 a extrait ces types dans le package @openmaic/dsl (reellement present et utilise ailleurs dans le worktree). Appliquer romprait la coherence d'architecture avec le reste du code v0.3.0 non touche par cette story. Echantillon verifie manuellement (6 fichiers) : chacun perd aussi une fonctionnalite ou un correctif upstream reel au-dela du seul import (ex. SpotlightOverlay perd domIdPrefix + un correctif Tailwind ; BaseImageElement revient a un pattern de resolution media plus ancien perdant une correction documentee de contamination cross-course ; chartOption.ts perd des verifications defensives ; element-fingerprint.ts perd le cas 'code' (risque de rupture d'exhaustivite de switch). |
| e2e/fixtures/test-data/settings.ts | 3 | rejected | Perd le champ reviewOutlineEnabled ajoute par upstream (fixture de test). |
| e2e/pages/home.page.ts | 4 | applied | Branding : selecteur de test cherche alt='Qalem' au lieu de alt='OpenMAIC' -- necessaire pour que les e2e trouvent le logo une fois le rebranding applique (deja fait sur layout.tsx/page.tsx via OURS_ONLY). (source=git, aucune derive detectee) |
| eslint.config.mjs | 15 | applied | Le remplacement des ignores granulaires par 'packages/**' est un SUR-ensemble (ignore AU MOINS autant, y compris packages/@openmaic et packages/docs) -- reduit le perimetre de lint, ne peut pas introduire de nouvelle erreur. (source=git, aucune derive detectee) |
| lib/api/stage-api-canvas.ts | 4 | rejected | Le diff reintroduit un import depuis l'ancien chemin @/lib/types/slides alors qu'upstream v0.3.0 a extrait ces types dans le package @openmaic/dsl (reellement present et utilise ailleurs dans le worktree). Appliquer romprait la coherence d'architecture avec le reste du code v0.3.0 non touche par cette story. Echantillon verifie manuellement (6 fichiers) : chacun perd aussi une fonctionnalite ou un correctif upstream reel au-dela du seul import (ex. SpotlightOverlay perd domIdPrefix + un correctif Tailwind ; BaseImageElement revient a un pattern de resolution media plus ancien perdant une correction documentee de contamination cross-course ; chartOption.ts perd des verifications defensives ; element-fingerprint.ts perd le cas 'code' (risque de rupture d'exhaustivite de switch). |
| lib/api/stage-api-element.ts | 4 | rejected | Le diff reintroduit un import depuis l'ancien chemin @/lib/types/slides alors qu'upstream v0.3.0 a extrait ces types dans le package @openmaic/dsl (reellement present et utilise ailleurs dans le worktree). Appliquer romprait la coherence d'architecture avec le reste du code v0.3.0 non touche par cette story. Echantillon verifie manuellement (6 fichiers) : chacun perd aussi une fonctionnalite ou un correctif upstream reel au-dela du seul import (ex. SpotlightOverlay perd domIdPrefix + un correctif Tailwind ; BaseImageElement revient a un pattern de resolution media plus ancien perdant une correction documentee de contamination cross-course ; chartOption.ts perd des verifications defensives ; element-fingerprint.ts perd le cas 'code' (risque de rupture d'exhaustivite de switch). |
| lib/api/stage-api-types.ts | 4 | rejected | Le diff reintroduit un import depuis l'ancien chemin @/lib/types/slides alors qu'upstream v0.3.0 a extrait ces types dans le package @openmaic/dsl (reellement present et utilise ailleurs dans le worktree). Appliquer romprait la coherence d'architecture avec le reste du code v0.3.0 non touche par cette story. Echantillon verifie manuellement (6 fichiers) : chacun perd aussi une fonctionnalite ou un correctif upstream reel au-dela du seul import (ex. SpotlightOverlay perd domIdPrefix + un correctif Tailwind ; BaseImageElement revient a un pattern de resolution media plus ancien perdant une correction documentee de contamination cross-course ; chartOption.ts perd des verifications defensives ; element-fingerprint.ts perd le cas 'code' (risque de rupture d'exhaustivite de switch). |
| lib/api/stage-api-whiteboard.ts | 4 | rejected | Le diff reintroduit un import depuis l'ancien chemin @/lib/types/slides alors qu'upstream v0.3.0 a extrait ces types dans le package @openmaic/dsl (reellement present et utilise ailleurs dans le worktree). Appliquer romprait la coherence d'architecture avec le reste du code v0.3.0 non touche par cette story. Echantillon verifie manuellement (6 fichiers) : chacun perd aussi une fonctionnalite ou un correctif upstream reel au-dela du seul import (ex. SpotlightOverlay perd domIdPrefix + un correctif Tailwind ; BaseImageElement revient a un pattern de resolution media plus ancien perdant une correction documentee de contamination cross-course ; chartOption.ts perd des verifications defensives ; element-fingerprint.ts perd le cas 'code' (risque de rupture d'exhaustivite de switch). |
| lib/audio/browser-tts-preview.ts | 16 | applied | Additif pur : main ajoute la detection de langue arabe (ar-MA) et francaise (fr-FR) a inferPreviewLang(), sans retirer aucune capacite upstream. Coeur de la mission FR/AR de Qalem. (source=git, aucune derive detectee) |
| lib/audio/use-tts-preview.ts | 6 | rejected | Perd modelId et providerOptions (options TTS avancees) ajoutes par upstream. |
| lib/export/svg-path-parser.ts | 24 | rejected | Upstream a ajoute une gestion defensive des chemins SVG malformes (try/catch, retour [] documente comme corrigeant un crash reel cause par des hallucinations LLM). Appliquer main REINTRODUIRAIT ce risque de crash. |
| lib/generation/generation-pipeline.ts | 6 | rejected | Renomme buildLanguageText en getLanguageName/getLanguageQualityRules (meme cluster languageDirective que scene-builder.ts/pipeline-runner.ts) et retire un re-export de types (SceneContentOptions/SceneActionsOptions) potentiellement utilise ailleurs. |
| lib/generation/interactive-post-processor.ts | 25 | rejected | Upstream corrige un bug de performance documente (O(blocs x longueur) avec l'ancienne boucle indexOf+substring, remplace par un replacer fonction qui est deja immunise contre les caracteres '$' -- le commentaire de main affirme a tort le contraire). Appliquer main REINTRODUIRAIT ce bug de perf deja corrige. |
| lib/generation/pipeline-runner.ts | 25 | rejected | Melange : la de-sinicisation des messages de statut (souhaitable) est indissociable ici de la perte de la destructuration 'languageDirective' (fonctionnalite upstream plus recente, cf. cluster scene-builder.ts/generation-preview/types.ts). Un remplacement integral casserait la coherence avec le reste du pipeline non touche par cette story. Necessite un patch chirurgical hors perimetre mecanique. |
| lib/generation/scene-builder.ts | 27 | rejected | Le diff reintroduit un import depuis l'ancien chemin @/lib/types/slides alors qu'upstream v0.3.0 a extrait ces types dans le package @openmaic/dsl (reellement present et utilise ailleurs dans le worktree). Appliquer romprait la coherence d'architecture avec le reste du code v0.3.0 non touche par cette story. Echantillon verifie manuellement (6 fichiers) : chacun perd aussi une fonctionnalite ou un correctif upstream reel au-dela du seul import (ex. SpotlightOverlay perd domIdPrefix + un correctif Tailwind ; BaseImageElement revient a un pattern de resolution media plus ancien perdant une correction documentee de contamination cross-course ; chartOption.ts perd des verifications defensives ; element-fingerprint.ts perd le cas 'code' (risque de rupture d'exhaustivite de switch). |
| lib/hooks/use-browser-asr.ts | 13 | rejected | Le diff reintroduit un import depuis l'ancien chemin @/lib/types/slides alors qu'upstream v0.3.0 a extrait ces types dans le package @openmaic/dsl (reellement present et utilise ailleurs dans le worktree). Appliquer romprait la coherence d'architecture avec le reste du code v0.3.0 non touche par cette story. Echantillon verifie manuellement (6 fichiers) : chacun perd aussi une fonctionnalite ou un correctif upstream reel au-dela du seul import (ex. SpotlightOverlay perd domIdPrefix + un correctif Tailwind ; BaseImageElement revient a un pattern de resolution media plus ancien perdant une correction documentee de contamination cross-course ; chartOption.ts perd des verifications defensives ; element-fingerprint.ts perd le cas 'code' (risque de rupture d'exhaustivite de switch). |
| lib/hooks/use-canvas-operations.ts | 8 | rejected | Le diff reintroduit un import depuis l'ancien chemin @/lib/types/slides alors qu'upstream v0.3.0 a extrait ces types dans le package @openmaic/dsl (reellement present et utilise ailleurs dans le worktree). Appliquer romprait la coherence d'architecture avec le reste du code v0.3.0 non touche par cette story. Echantillon verifie manuellement (6 fichiers) : chacun perd aussi une fonctionnalite ou un correctif upstream reel au-dela du seul import (ex. SpotlightOverlay perd domIdPrefix + un correctif Tailwind ; BaseImageElement revient a un pattern de resolution media plus ancien perdant une correction documentee de contamination cross-course ; chartOption.ts perd des verifications defensives ; element-fingerprint.ts perd le cas 'code' (risque de rupture d'exhaustivite de switch). |
| lib/hooks/use-order-element.ts | 6 | rejected | Le diff reintroduit un import depuis l'ancien chemin @/lib/types/slides alors qu'upstream v0.3.0 a extrait ces types dans le package @openmaic/dsl (reellement present et utilise ailleurs dans le worktree). Appliquer romprait la coherence d'architecture avec le reste du code v0.3.0 non touche par cette story. Echantillon verifie manuellement (6 fichiers) : chacun perd aussi une fonctionnalite ou un correctif upstream reel au-dela du seul import (ex. SpotlightOverlay perd domIdPrefix + un correctif Tailwind ; BaseImageElement revient a un pattern de resolution media plus ancien perdant une correction documentee de contamination cross-course ; chartOption.ts perd des verifications defensives ; element-fingerprint.ts perd le cas 'code' (risque de rupture d'exhaustivite de switch). |
| lib/hooks/use-slide-background-style.ts | 4 | rejected | Le diff reintroduit un import depuis l'ancien chemin @/lib/types/slides alors qu'upstream v0.3.0 a extrait ces types dans le package @openmaic/dsl (reellement present et utilise ailleurs dans le worktree). Appliquer romprait la coherence d'architecture avec le reste du code v0.3.0 non touche par cette story. Echantillon verifie manuellement (6 fichiers) : chacun perd aussi une fonctionnalite ou un correctif upstream reel au-dela du seul import (ex. SpotlightOverlay perd domIdPrefix + un correctif Tailwind ; BaseImageElement revient a un pattern de resolution media plus ancien perdant une correction documentee de contamination cross-course ; chartOption.ts perd des verifications defensives ; element-fingerprint.ts perd le cas 'code' (risque de rupture d'exhaustivite de switch). |
| lib/hooks/use-streaming-text.ts | 17 | rejected | Le diff reintroduit un import depuis l'ancien chemin @/lib/types/slides alors qu'upstream v0.3.0 a extrait ces types dans le package @openmaic/dsl (reellement present et utilise ailleurs dans le worktree). Appliquer romprait la coherence d'architecture avec le reste du code v0.3.0 non touche par cette story. Echantillon verifie manuellement (6 fichiers) : chacun perd aussi une fonctionnalite ou un correctif upstream reel au-dela du seul import (ex. SpotlightOverlay perd domIdPrefix + un correctif Tailwind ; BaseImageElement revient a un pattern de resolution media plus ancien perdant une correction documentee de contamination cross-course ; chartOption.ts perd des verifications defensives ; element-fingerprint.ts perd le cas 'code' (risque de rupture d'exhaustivite de switch). |
| lib/hooks/use-theme.tsx | 6 | applied | Defaut de theme 'dark'/'dark' au lieu de 'system'/'light' -- choix UX cosmetique reversible, ne retire aucune capacite upstream, ne touche aucun type/API partagee. (source=git, aucune derive detectee) |
| lib/i18n/types.ts | 8 | applied | Necessaire : le systeme i18n deja porte (OURS_ONLY, use-i18n.tsx via react-i18next) depend du type Locale incluant fr-FR/ar-MA et de defaultLocale='fr-FR'. La version upstream derive Locale du nouveau registre lib/i18n/locales.ts (zh-CN/zh-TW/en-US/ja-JP/ru-RU/ar-SA/pt-BR/ko-KR, PAS fr-FR/ar-MA) -- la garder casserait la compilation du systeme i18n deja porte. (source=git, aucune derive detectee) |
| lib/media/types.ts | 22 | rejected | Retrecit drastiquement ImageProviderId/VideoProviderId (perd openai-image, minimax-image, grok-image, lemonade, minimax-video, happyhorse) -- risque concret de casser d'autres fichiers (hors perimetre des 137) qui referencent ces IDs, contraire a l'objectif tsc vert. |
| lib/orchestration/registry/store.ts | 24 | rejected | Melange : la traduction des noms d'agents par defaut (chinois -> anglais) est souhaitable (cf. Known Issues progress.md) MAIS indissociable ici de pertes reelles (type VoiceDesign, actions wb_draw_code/wb_edit_code, warm-up vocal). Renommage a refaire chirurgicalement, plus probablement dans S0-006 (branding). |
| lib/orchestration/registry/types.ts | 11 | rejected | Perd voiceDesign, modelId (config vocale par agent) et les actions wb_draw_code/wb_edit_code -- meme cluster que registry/store.ts et tool-schemas.ts/inline-action-tag.tsx. |
| lib/orchestration/stateless-generate.ts | 16 | rejected | Le diff reintroduit un import depuis l'ancien chemin @/lib/types/slides alors qu'upstream v0.3.0 a extrait ces types dans le package @openmaic/dsl (reellement present et utilise ailleurs dans le worktree). Appliquer romprait la coherence d'architecture avec le reste du code v0.3.0 non touche par cette story. Echantillon verifie manuellement (6 fichiers) : chacun perd aussi une fonctionnalite ou un correctif upstream reel au-dela du seul import (ex. SpotlightOverlay perd domIdPrefix + un correctif Tailwind ; BaseImageElement revient a un pattern de resolution media plus ancien perdant une correction documentee de contamination cross-course ; chartOption.ts perd des verifications defensives ; element-fingerprint.ts perd le cas 'code' (risque de rupture d'exhaustivite de switch). |
| lib/orchestration/tool-schemas.ts | 6 | rejected | Perd les descriptions d'outils wb_draw_code/wb_edit_code -- meme cluster que registry/types.ts et inline-action-tag.tsx. |
| lib/pbl/mcp/issueboard-mcp.ts | 12 | rejected | Renomme 'languageDirective' en 'language' -- lie au meme cluster de fonctionnalite que scene-builder.ts/pipeline-runner.ts (deja rejetes) ; incoherent d'appliquer ici isolement. |
| lib/pdf/constants.ts | 12 | rejected | Retire le provider 'mineru-cloud' entierement -- retrecissement de capacite, meme famille de risque que media/types.ts et lib/pdf/types.ts. |
| lib/pdf/types.ts | 4 | rejected | DOUBLON: voir entree ci-dessus. |
| lib/prosemirror/schema/marks.ts | 12 | rejected | SEVERITE ELEVEE : upstream a ajoute une protection anti-injection CSS (validation/echappement du nom de police avant insertion dans un style inline, avec commentaire documentant explicitement le risque). Appliquer main REVERTERAIT ce correctif de securite. |
| lib/server/api-response.ts | 6 | rejected | Perd plusieurs codes d'erreur ajoutes par upstream (PROVIDER_DISABLED, VOXCPM_AUTO_VOICE_REQUIRES_CONTEXT, TOO_MANY_REDIRECTS, RATE_LIMITED) -- risque concret de casser d'autres fichiers (hors perimetre) qui les referenceraient. |
| lib/server/classroom-job-store.ts | 4 | rejected | Perd le champ 'language' (suivi multilingue des jobs de generation) ajoute par upstream -- contraire a l'objectif FR/AR/EN de Qalem de retirer ce suivi. |
| lib/store/canvas.ts | 12 | rejected | Perd la fonctionnalite 'pickTarget' (mode timeline 'choisir un element sur le canevas') ajoutee par upstream. |
| lib/store/whiteboard-history.ts | 4 | rejected | Le diff reintroduit un import depuis l'ancien chemin @/lib/types/slides alors qu'upstream v0.3.0 a extrait ces types dans le package @openmaic/dsl (reellement present et utilise ailleurs dans le worktree). Appliquer romprait la coherence d'architecture avec le reste du code v0.3.0 non touche par cette story. Echantillon verifie manuellement (6 fichiers) : chacun perd aussi une fonctionnalite ou un correctif upstream reel au-dela du seul import (ex. SpotlightOverlay perd domIdPrefix + un correctif Tailwind ; BaseImageElement revient a un pattern de resolution media plus ancien perdant une correction documentee de contamination cross-course ; chartOption.ts perd des verifications defensives ; element-fingerprint.ts perd le cas 'code' (risque de rupture d'exhaustivite de switch). |
| lib/types/chat.ts | 19 | rejected | Melange complexe : perd ThinkingConfig, SessionStatus='error', maxTurns/currentTurn (fonctionnalites upstream) ; gagnerait 'requiresApiKey' (utile, cf. use-pbl-chat.ts) ; change aussi l'import de WhiteboardActionRecord vers un chemin obsolete (./types au lieu de ./director-prompt). Remplacement integral disproportionnellement risque pour le present perimetre mecanique. |
| lib/types/edit.ts | 9 | rejected | Le diff reintroduit un import depuis l'ancien chemin @/lib/types/slides alors qu'upstream v0.3.0 a extrait ces types dans le package @openmaic/dsl (reellement present et utilise ailleurs dans le worktree). Appliquer romprait la coherence d'architecture avec le reste du code v0.3.0 non touche par cette story. Echantillon verifie manuellement (6 fichiers) : chacun perd aussi une fonctionnalite ou un correctif upstream reel au-dela du seul import (ex. SpotlightOverlay perd domIdPrefix + un correctif Tailwind ; BaseImageElement revient a un pattern de resolution media plus ancien perdant une correction documentee de contamination cross-course ; chartOption.ts perd des verifications defensives ; element-fingerprint.ts perd le cas 'code' (risque de rupture d'exhaustivite de switch). |
| lib/types/settings.ts | 10 | rejected | Perd le champ serverBaseUrl (override d'URL cote serveur) ajoute par upstream -- risque de casser d'autres fichiers hors perimetre qui le referenceraient. |
| lib/utils/audio-player.ts | 13 | rejected | Le diff reintroduit un import depuis l'ancien chemin @/lib/types/slides alors qu'upstream v0.3.0 a extrait ces types dans le package @openmaic/dsl (reellement present et utilise ailleurs dans le worktree). Appliquer romprait la coherence d'architecture avec le reste du code v0.3.0 non touche par cette story. Echantillon verifie manuellement (6 fichiers) : chacun perd aussi une fonctionnalite ou un correctif upstream reel au-dela du seul import (ex. SpotlightOverlay perd domIdPrefix + un correctif Tailwind ; BaseImageElement revient a un pattern de resolution media plus ancien perdant une correction documentee de contamination cross-course ; chartOption.ts perd des verifications defensives ; element-fingerprint.ts perd le cas 'code' (risque de rupture d'exhaustivite de switch). |
| lib/utils/element-fingerprint.ts | 6 | rejected | Le diff reintroduit un import depuis l'ancien chemin @/lib/types/slides alors qu'upstream v0.3.0 a extrait ces types dans le package @openmaic/dsl (reellement present et utilise ailleurs dans le worktree). Appliquer romprait la coherence d'architecture avec le reste du code v0.3.0 non touche par cette story. Echantillon verifie manuellement (6 fichiers) : chacun perd aussi une fonctionnalite ou un correctif upstream reel au-dela du seul import (ex. SpotlightOverlay perd domIdPrefix + un correctif Tailwind ; BaseImageElement revient a un pattern de resolution media plus ancien perdant une correction documentee de contamination cross-course ; chartOption.ts perd des verifications defensives ; element-fingerprint.ts perd le cas 'code' (risque de rupture d'exhaustivite de switch). |
| lib/utils/element.ts | 4 | rejected | Le diff reintroduit un import depuis l'ancien chemin @/lib/types/slides alors qu'upstream v0.3.0 a extrait ces types dans le package @openmaic/dsl (reellement present et utilise ailleurs dans le worktree). Appliquer romprait la coherence d'architecture avec le reste du code v0.3.0 non touche par cette story. Echantillon verifie manuellement (6 fichiers) : chacun perd aussi une fonctionnalite ou un correctif upstream reel au-dela du seul import (ex. SpotlightOverlay perd domIdPrefix + un correctif Tailwind ; BaseImageElement revient a un pattern de resolution media plus ancien perdant une correction documentee de contamination cross-course ; chartOption.ts perd des verifications defensives ; element-fingerprint.ts perd le cas 'code' (risque de rupture d'exhaustivite de switch). |
| lib/utils/geometry.ts | 4 | rejected | Le diff reintroduit un import depuis l'ancien chemin @/lib/types/slides alors qu'upstream v0.3.0 a extrait ces types dans le package @openmaic/dsl (reellement present et utilise ailleurs dans le worktree). Appliquer romprait la coherence d'architecture avec le reste du code v0.3.0 non touche par cette story. Echantillon verifie manuellement (6 fichiers) : chacun perd aussi une fonctionnalite ou un correctif upstream reel au-dela du seul import (ex. SpotlightOverlay perd domIdPrefix + un correctif Tailwind ; BaseImageElement revient a un pattern de resolution media plus ancien perdant une correction documentee de contamination cross-course ; chartOption.ts perd des verifications defensives ; element-fingerprint.ts perd le cas 'code' (risque de rupture d'exhaustivite de switch). |
| lib/utils/model-config.ts | 15 | rejected | Le diff reintroduit un import depuis l'ancien chemin @/lib/types/slides alors qu'upstream v0.3.0 a extrait ces types dans le package @openmaic/dsl (reellement present et utilise ailleurs dans le worktree). Appliquer romprait la coherence d'architecture avec le reste du code v0.3.0 non touche par cette story. Echantillon verifie manuellement (6 fichiers) : chacun perd aussi une fonctionnalite ou un correctif upstream reel au-dela du seul import (ex. SpotlightOverlay perd domIdPrefix + un correctif Tailwind ; BaseImageElement revient a un pattern de resolution media plus ancien perdant une correction documentee de contamination cross-course ; chartOption.ts perd des verifications defensives ; element-fingerprint.ts perd le cas 'code' (risque de rupture d'exhaustivite de switch). |
| lib/web-search/types.ts | 14 | rejected | Le diff reintroduit un import depuis l'ancien chemin @/lib/types/slides alors qu'upstream v0.3.0 a extrait ces types dans le package @openmaic/dsl (reellement present et utilise ailleurs dans le worktree). Appliquer romprait la coherence d'architecture avec le reste du code v0.3.0 non touche par cette story. Echantillon verifie manuellement (6 fichiers) : chacun perd aussi une fonctionnalite ou un correctif upstream reel au-dela du seul import (ex. SpotlightOverlay perd domIdPrefix + un correctif Tailwind ; BaseImageElement revient a un pattern de resolution media plus ancien perdant une correction documentee de contamination cross-course ; chartOption.ts perd des verifications defensives ; element-fingerprint.ts perd le cas 'code' (risque de rupture d'exhaustivite de switch). |
| packages/pptxgenjs/rollup.config.mjs | 8 | rejected | Modifie la configuration de build du package pptxgenjs dont le postinstall est deja verifie vert par S0-001 -- risque de regression sur une chaine de build deja prouvee, sans benefice identifie. |
| packages/pptxgenjs/src/gen-media.ts | 4 | rejected | Retire les commentaires magiques '/* webpackIgnore: true */' presents dans la version v0.3.0 dont le build (postinstall) est deja verifie vert par S0-001 -- pas de raison de toucher a un etat deja valide. |
| packages/pptxgenjs/src/pptxgen.ts | 4 | rejected | Meme raison que gen-media.ts (meme paire de commentaires webpackIgnore, meme package deja valide par S0-001). |
| playwright.config.ts | 7 | rejected | Perd le flag NEXT_PUBLIC_MAIC_EDITOR_ENABLED necessaire aux e2e de l'Editor Pro mode ajoute par upstream -- perte de couverture e2e reelle, pas cosmetique. |
| pnpm-workspace.yaml | 4 | rejected | main ne declare pas 'packages/@openmaic/*' comme membre du workspace pnpm, alors que packages/@openmaic/{dsl,importer,renderer} existent reellement et sont deja necessaires au postinstall valide vert par S0-001 (pnpm build). Appliquer casserait cette chaine de build deja verifiee. |
| public/logos/openai.svg | 13 | applied | Asset SVG pur (fill via <style>/prefers-color-scheme au lieu de fill='#000' fixe) -- ajout d'un support dark-mode au logo, aucun impact build/type. (source=git, aucune derive detectee) |
| tsconfig.json | 4 | rejected | main exclut 'packages/docs' de tsc alors que ce dossier EXISTE reellement dans le worktree v0.3.0 (app Fumadocs avec son propre toolchain) -- retirer cette exclusion ferait typechecker un sous-projet non concu pour ca, risque concret de regression sur l'acceptance 'npx tsc --noEmit vert'. |
| vitest.config.ts | 3 | rejected | Perd setupFiles:['tests/setup-env.ts'] ajoute par upstream -- ce fichier existe reellement dans le worktree v0.3.0, le retirer changerait le comportement des tests sans necessite identifiee. |

## Execution S0-004 (revue manuelle) 2026-07-10T14:35:34

Application semi-automatique des divergences BOTH_DIFFER < 30 lignes, apres REVUE MANUELLE DU CONTENU de chacune (pas seulement du nombre de lignes — voir docstring de `refork/apply_both_differ_small.py` et `refork/s0-004-decisions.json` pour la justification complete).

- Inventaire : `C:\projets\Qalem\refork\inventaire.json`
- Decisions manuelles : `C:\projets\Qalem\refork-v030-wt\refork\s0-004-decisions.json`
- Repo carriere (main) : `C:\projets\Qalem\OpenMAIC`
- Archive upstream v0.3.0 : `C:\projets\Qalem\upstream-v030`
- Worktree cible : `C:\projets\Qalem\refork-v030-wt`
- Total fichiers BOTH_DIFFER < 30 lignes : 137
- Appliques : 4
- Rejetes : 123
- No-op (deja a jour) : 10

### Detail fichier par fichier

| Fichier | Lignes diff | Decision | Motif |
|---|---|---|---|
| .github/ISSUE_TEMPLATE/config.yml | 4 | noop | contenu main deja applique au worktree lors d'une execution precedente (source=git) — idempotent, rien a refaire. Motif d'application : Lien Discord mis a jour -- cosmetique, zero risque technique. |
| .github/workflows/ci.yml | 28 | noop | contenu main deja applique au worktree lors d'une execution precedente (source=git) — idempotent, rien a refaire. Motif d'application : Config CI uniquement (n'affecte aucune commande locale tsc/lint/test/build). Retire un step 'i18n Key Alignment' + 'Unit Tests (importer)' absents de main, ajoute un step Build -- cosmetique, zero risque sur le code source. |
| .prettierignore | 10 | rejected | Meme famille que pnpm-workspace.yaml/tsconfig.json : retire les ignores de packages/@openmaic/*/dist, importer/src1 (vendor) et packages/docs sans les remplacer par un ignore plus large -- regression par rapport a la structure v0.3.0 reelle du worktree. |
| CONTRIBUTING.md | 11 | rejected | FR-3 / convention soft-fork Qalem : README/CONTRIBUTING/CHANGELOG herites de l'upstream ne sont JAMAIS modifies (regle absolue, independante du seuil de lignes). |
| app/api/azure-voices/route.ts | 15 | rejected | MELANGE (rejete du remplacement mecanique, traite CHIRURGICALEMENT par edition manuelle directe hors de ce script) : main AJOUTE 'requireAuth(...)' (gain de securite reel, absent d'upstream) MAIS retire aussi le 'await' sur 'validateUrlForSSRF(baseUrl)' qui est une fonction ASYNC (verifie : lib/server/ssrf-guard.ts:172 'export async function validateUrlForSSRF') -- sans await, 'ssrfError' serait toujours une Promise (donc toujours truthy), cassant systematiquement la validation SSRF et donc la fonctionnalite entiere. Patch chirurgical applique manuellement : ajout de requireAuth, conservation de l'await existant. |
| app/api/classroom-media/[classroomId]/[...path]/route.ts | 13 | applied | CORRECTION apres re-verification rigoureuse du sens du diff : c'est MAIN qui AJOUTE 'requireAuth(...)' (authentification), ABSENTE d'upstream sur cette route -- gain de securite reel. Perte associee : logging d'erreur detaille (classroomId/path) -- acceptable, aucune fonctionnalite utilisateur perdue. (source=git, aucune derive detectee) |
| app/api/generate-classroom/[jobId]/route.ts | 12 | applied | CORRECTION apres re-verification rigoureuse du sens du diff : c'est MAIN qui AJOUTE 'requireAuth(...)', ABSENTE d'upstream sur cette route -- gain de securite reel. Perte associee : logging d'erreur detaille (jobId) -- acceptable. (source=git, aucune derive detectee) |
| app/api/health/route.ts | 4 | rejected | Regression mineure de precision (verifie Object.keys().length au lieu de values().some(!disabled)) -- pas de gain identifie, rejet par coherence de politique (main en retard, aucune necessite Qalem). |
| app/api/server-providers/route.ts | 6 | rejected | Perd le champ 'generation.parallelSceneConcurrency' ajoute par upstream -- risque de casser un type client qui l'attendrait. |
| app/generation-preview/types.ts | 23 | rejected | Perd previewPhase, languageDirective, courseTitle, taskEngineMode (cluster de fonctionnalites upstream plus recentes) et reordonne le tableau d'etapes -- perte de structure, pas une simple divergence textuelle. |
| components/agent/agent-config-panel.tsx | 8 | rejected | Perd la fonctionnalite PublishAgentDialog (publication d'agent) ajoutee par upstream. |
| components/ai-elements/prompt-input.tsx | 7 | rejected | RISQUE CONCRET DE CASSER TSC : le commentaire upstream explique que le '?' optionnel sur Window.SpeechRecognition doit rester identique aux modificateurs de l'augmentation globale de @assistant-ui/core (declaration merging TypeScript exige des modificateurs identiques). Le retirer peut provoquer un conflit de declaration globale. |
| components/canvas/canvas-area.tsx | 21 | rejected | Perd la fonctionnalite ClassroomCompletePageConnected / isCourseComplete (ecran de fin de cours) ajoutee par upstream. |
| components/chat/inline-action-tag.tsx | 6 | rejected | Perd les icones/labels wb_draw_code et wb_edit_code -- meme cluster que registry/types.ts et tool-schemas.ts. |
| components/chat/lecture-notes-view.tsx | 4 | noop | contenu main deja applique au worktree lors d'une execution precedente (source=git) — idempotent, rien a refaire. Motif d'application : Meme raison que whiteboard-history.tsx : interpolation par .replace('{n}',...) necessaire pour les traductions Qalem a accolade simple. |
| components/chat/session-list.tsx | 6 | rejected | Perd le cas SessionStatus='error' (icone AlertCircle) -- coherent avec le rejet de lib/types/chat.ts (meme enum). |
| components/scene-renderers/pbl/use-pbl-chat.ts | 24 | applied | CORRECTION apres re-verification rigoureuse du sens du diff (voir note methodologique) : main AJOUTE l'en-tete 'x-requires-api-key' (absent d'upstream, additif) ET bascule sur t(key).replace('{title}'/'{questions}',...) qui est NECESSAIRE pour utiliser les traductions Qalem deja portees de welcomeMessage/issueCompleteMessage (accolade simple '{title}'/'{questions}', verifie dans lib/i18n/stage.ts) -- sans ce changement ces traductions FR/AR/EN entierement redigees ne seraient jamais affichees. Aucune fonctionnalite upstream perdue ici. (source=git, aucune derive detectee) |
| components/settings/provider-list.tsx | 8 | rejected | Perd la logique MONO_LOGO_PROVIDERS (inversion dark-mode des logos monochromes) ajoutee par upstream. |
| components/slide-renderer/Editor/Canvas/EditableElement.tsx | 4 | rejected | Le diff reintroduit un import depuis l'ancien chemin @/lib/types/slides alors qu'upstream v0.3.0 a extrait ces types dans le package @openmaic/dsl (reellement present et utilise ailleurs dans le worktree). Appliquer romprait la coherence d'architecture avec le reste du code v0.3.0 non touche par cette story. Echantillon verifie manuellement (6 fichiers) : chacun perd aussi une fonctionnalite ou un correctif upstream reel au-dela du seul import (ex. SpotlightOverlay perd domIdPrefix + un correctif Tailwind ; BaseImageElement revient a un pattern de resolution media plus ancien perdant une correction documentee de contamination cross-course ; chartOption.ts perd des verifications defensives ; element-fingerprint.ts perd le cas 'code' (risque de rupture d'exhaustivite de switch). |
| components/slide-renderer/Editor/Canvas/ElementCreateSelection.tsx | 10 | rejected | Perd la gestion dediee de creation d'un element texte (comportement de taille par defaut adapte) ajoutee par upstream. |
| components/slide-renderer/Editor/Canvas/GridLines.tsx | 4 | rejected | Le diff reintroduit un import depuis l'ancien chemin @/lib/types/slides alors qu'upstream v0.3.0 a extrait ces types dans le package @openmaic/dsl (reellement present et utilise ailleurs dans le worktree). Appliquer romprait la coherence d'architecture avec le reste du code v0.3.0 non touche par cette story. Echantillon verifie manuellement (6 fichiers) : chacun perd aussi une fonctionnalite ou un correctif upstream reel au-dela du seul import (ex. SpotlightOverlay perd domIdPrefix + un correctif Tailwind ; BaseImageElement revient a un pattern de resolution media plus ancien perdant une correction documentee de contamination cross-course ; chartOption.ts perd des verifications defensives ; element-fingerprint.ts perd le cas 'code' (risque de rupture d'exhaustivite de switch). |
| components/slide-renderer/Editor/Canvas/Operate/CommonElementOperate.tsx | 4 | rejected | Le diff reintroduit un import depuis l'ancien chemin @/lib/types/slides alors qu'upstream v0.3.0 a extrait ces types dans le package @openmaic/dsl (reellement present et utilise ailleurs dans le worktree). Appliquer romprait la coherence d'architecture avec le reste du code v0.3.0 non touche par cette story. Echantillon verifie manuellement (6 fichiers) : chacun perd aussi une fonctionnalite ou un correctif upstream reel au-dela du seul import (ex. SpotlightOverlay perd domIdPrefix + un correctif Tailwind ; BaseImageElement revient a un pattern de resolution media plus ancien perdant une correction documentee de contamination cross-course ; chartOption.ts perd des verifications defensives ; element-fingerprint.ts perd le cas 'code' (risque de rupture d'exhaustivite de switch). |
| components/slide-renderer/Editor/Canvas/Operate/ImageElementOperate.tsx | 4 | rejected | Le diff reintroduit un import depuis l'ancien chemin @/lib/types/slides alors qu'upstream v0.3.0 a extrait ces types dans le package @openmaic/dsl (reellement present et utilise ailleurs dans le worktree). Appliquer romprait la coherence d'architecture avec le reste du code v0.3.0 non touche par cette story. Echantillon verifie manuellement (6 fichiers) : chacun perd aussi une fonctionnalite ou un correctif upstream reel au-dela du seul import (ex. SpotlightOverlay perd domIdPrefix + un correctif Tailwind ; BaseImageElement revient a un pattern de resolution media plus ancien perdant une correction documentee de contamination cross-course ; chartOption.ts perd des verifications defensives ; element-fingerprint.ts perd le cas 'code' (risque de rupture d'exhaustivite de switch). |
| components/slide-renderer/Editor/Canvas/Operate/LineElementOperate.tsx | 4 | rejected | Le diff reintroduit un import depuis l'ancien chemin @/lib/types/slides alors qu'upstream v0.3.0 a extrait ces types dans le package @openmaic/dsl (reellement present et utilise ailleurs dans le worktree). Appliquer romprait la coherence d'architecture avec le reste du code v0.3.0 non touche par cette story. Echantillon verifie manuellement (6 fichiers) : chacun perd aussi une fonctionnalite ou un correctif upstream reel au-dela du seul import (ex. SpotlightOverlay perd domIdPrefix + un correctif Tailwind ; BaseImageElement revient a un pattern de resolution media plus ancien perdant une correction documentee de contamination cross-course ; chartOption.ts perd des verifications defensives ; element-fingerprint.ts perd le cas 'code' (risque de rupture d'exhaustivite de switch). |
| components/slide-renderer/Editor/Canvas/Operate/MultiSelectOperate.tsx | 4 | rejected | Le diff reintroduit un import depuis l'ancien chemin @/lib/types/slides alors qu'upstream v0.3.0 a extrait ces types dans le package @openmaic/dsl (reellement present et utilise ailleurs dans le worktree). Appliquer romprait la coherence d'architecture avec le reste du code v0.3.0 non touche par cette story. Echantillon verifie manuellement (6 fichiers) : chacun perd aussi une fonctionnalite ou un correctif upstream reel au-dela du seul import (ex. SpotlightOverlay perd domIdPrefix + un correctif Tailwind ; BaseImageElement revient a un pattern de resolution media plus ancien perdant une correction documentee de contamination cross-course ; chartOption.ts perd des verifications defensives ; element-fingerprint.ts perd le cas 'code' (risque de rupture d'exhaustivite de switch). |
| components/slide-renderer/Editor/Canvas/Operate/ShapeElementOperate.tsx | 4 | rejected | Le diff reintroduit un import depuis l'ancien chemin @/lib/types/slides alors qu'upstream v0.3.0 a extrait ces types dans le package @openmaic/dsl (reellement present et utilise ailleurs dans le worktree). Appliquer romprait la coherence d'architecture avec le reste du code v0.3.0 non touche par cette story. Echantillon verifie manuellement (6 fichiers) : chacun perd aussi une fonctionnalite ou un correctif upstream reel au-dela du seul import (ex. SpotlightOverlay perd domIdPrefix + un correctif Tailwind ; BaseImageElement revient a un pattern de resolution media plus ancien perdant une correction documentee de contamination cross-course ; chartOption.ts perd des verifications defensives ; element-fingerprint.ts perd le cas 'code' (risque de rupture d'exhaustivite de switch). |
| components/slide-renderer/Editor/Canvas/Operate/TableElementOperate.tsx | 4 | rejected | Le diff reintroduit un import depuis l'ancien chemin @/lib/types/slides alors qu'upstream v0.3.0 a extrait ces types dans le package @openmaic/dsl (reellement present et utilise ailleurs dans le worktree). Appliquer romprait la coherence d'architecture avec le reste du code v0.3.0 non touche par cette story. Echantillon verifie manuellement (6 fichiers) : chacun perd aussi une fonctionnalite ou un correctif upstream reel au-dela du seul import (ex. SpotlightOverlay perd domIdPrefix + un correctif Tailwind ; BaseImageElement revient a un pattern de resolution media plus ancien perdant une correction documentee de contamination cross-course ; chartOption.ts perd des verifications defensives ; element-fingerprint.ts perd le cas 'code' (risque de rupture d'exhaustivite de switch). |
| components/slide-renderer/Editor/Canvas/Operate/index.tsx | 4 | rejected | Le diff reintroduit un import depuis l'ancien chemin @/lib/types/slides alors qu'upstream v0.3.0 a extrait ces types dans le package @openmaic/dsl (reellement present et utilise ailleurs dans le worktree). Appliquer romprait la coherence d'architecture avec le reste du code v0.3.0 non touche par cette story. Echantillon verifie manuellement (6 fichiers) : chacun perd aussi une fonctionnalite ou un correctif upstream reel au-dela du seul import (ex. SpotlightOverlay perd domIdPrefix + un correctif Tailwind ; BaseImageElement revient a un pattern de resolution media plus ancien perdant une correction documentee de contamination cross-course ; chartOption.ts perd des verifications defensives ; element-fingerprint.ts perd le cas 'code' (risque de rupture d'exhaustivite de switch). |
| components/slide-renderer/Editor/Canvas/Ruler.tsx | 4 | rejected | Le diff reintroduit un import depuis l'ancien chemin @/lib/types/slides alors qu'upstream v0.3.0 a extrait ces types dans le package @openmaic/dsl (reellement present et utilise ailleurs dans le worktree). Appliquer romprait la coherence d'architecture avec le reste du code v0.3.0 non touche par cette story. Echantillon verifie manuellement (6 fichiers) : chacun perd aussi une fonctionnalite ou un correctif upstream reel au-dela du seul import (ex. SpotlightOverlay perd domIdPrefix + un correctif Tailwind ; BaseImageElement revient a un pattern de resolution media plus ancien perdant une correction documentee de contamination cross-course ; chartOption.ts perd des verifications defensives ; element-fingerprint.ts perd le cas 'code' (risque de rupture d'exhaustivite de switch). |
| components/slide-renderer/Editor/Canvas/ShapeCreateCanvas.tsx | 4 | rejected | Le diff reintroduit un import depuis l'ancien chemin @/lib/types/slides alors qu'upstream v0.3.0 a extrait ces types dans le package @openmaic/dsl (reellement present et utilise ailleurs dans le worktree). Appliquer romprait la coherence d'architecture avec le reste du code v0.3.0 non touche par cette story. Echantillon verifie manuellement (6 fichiers) : chacun perd aussi une fonctionnalite ou un correctif upstream reel au-dela du seul import (ex. SpotlightOverlay perd domIdPrefix + un correctif Tailwind ; BaseImageElement revient a un pattern de resolution media plus ancien perdant une correction documentee de contamination cross-course ; chartOption.ts perd des verifications defensives ; element-fingerprint.ts perd le cas 'code' (risque de rupture d'exhaustivite de switch). |
| components/slide-renderer/Editor/Canvas/ViewportBackground.tsx | 4 | rejected | Le diff reintroduit un import depuis l'ancien chemin @/lib/types/slides alors qu'upstream v0.3.0 a extrait ces types dans le package @openmaic/dsl (reellement present et utilise ailleurs dans le worktree). Appliquer romprait la coherence d'architecture avec le reste du code v0.3.0 non touche par cette story. Echantillon verifie manuellement (6 fichiers) : chacun perd aussi une fonctionnalite ou un correctif upstream reel au-dela du seul import (ex. SpotlightOverlay perd domIdPrefix + un correctif Tailwind ; BaseImageElement revient a un pattern de resolution media plus ancien perdant une correction documentee de contamination cross-course ; chartOption.ts perd des verifications defensives ; element-fingerprint.ts perd le cas 'code' (risque de rupture d'exhaustivite de switch). |
| components/slide-renderer/Editor/Canvas/hooks/useDragElement.ts | 4 | rejected | Le diff reintroduit un import depuis l'ancien chemin @/lib/types/slides alors qu'upstream v0.3.0 a extrait ces types dans le package @openmaic/dsl (reellement present et utilise ailleurs dans le worktree). Appliquer romprait la coherence d'architecture avec le reste du code v0.3.0 non touche par cette story. Echantillon verifie manuellement (6 fichiers) : chacun perd aussi une fonctionnalite ou un correctif upstream reel au-dela du seul import (ex. SpotlightOverlay perd domIdPrefix + un correctif Tailwind ; BaseImageElement revient a un pattern de resolution media plus ancien perdant une correction documentee de contamination cross-course ; chartOption.ts perd des verifications defensives ; element-fingerprint.ts perd le cas 'code' (risque de rupture d'exhaustivite de switch). |
| components/slide-renderer/Editor/Canvas/hooks/useDragLineElement.ts | 4 | rejected | Le diff reintroduit un import depuis l'ancien chemin @/lib/types/slides alors qu'upstream v0.3.0 a extrait ces types dans le package @openmaic/dsl (reellement present et utilise ailleurs dans le worktree). Appliquer romprait la coherence d'architecture avec le reste du code v0.3.0 non touche par cette story. Echantillon verifie manuellement (6 fichiers) : chacun perd aussi une fonctionnalite ou un correctif upstream reel au-dela du seul import (ex. SpotlightOverlay perd domIdPrefix + un correctif Tailwind ; BaseImageElement revient a un pattern de resolution media plus ancien perdant une correction documentee de contamination cross-course ; chartOption.ts perd des verifications defensives ; element-fingerprint.ts perd le cas 'code' (risque de rupture d'exhaustivite de switch). |
| components/slide-renderer/Editor/Canvas/hooks/useMouseSelection.ts | 4 | rejected | Le diff reintroduit un import depuis l'ancien chemin @/lib/types/slides alors qu'upstream v0.3.0 a extrait ces types dans le package @openmaic/dsl (reellement present et utilise ailleurs dans le worktree). Appliquer romprait la coherence d'architecture avec le reste du code v0.3.0 non touche par cette story. Echantillon verifie manuellement (6 fichiers) : chacun perd aussi une fonctionnalite ou un correctif upstream reel au-dela du seul import (ex. SpotlightOverlay perd domIdPrefix + un correctif Tailwind ; BaseImageElement revient a un pattern de resolution media plus ancien perdant une correction documentee de contamination cross-course ; chartOption.ts perd des verifications defensives ; element-fingerprint.ts perd le cas 'code' (risque de rupture d'exhaustivite de switch). |
| components/slide-renderer/Editor/Canvas/hooks/useMoveShapeKeypoint.ts | 4 | rejected | Le diff reintroduit un import depuis l'ancien chemin @/lib/types/slides alors qu'upstream v0.3.0 a extrait ces types dans le package @openmaic/dsl (reellement present et utilise ailleurs dans le worktree). Appliquer romprait la coherence d'architecture avec le reste du code v0.3.0 non touche par cette story. Echantillon verifie manuellement (6 fichiers) : chacun perd aussi une fonctionnalite ou un correctif upstream reel au-dela du seul import (ex. SpotlightOverlay perd domIdPrefix + un correctif Tailwind ; BaseImageElement revient a un pattern de resolution media plus ancien perdant une correction documentee de contamination cross-course ; chartOption.ts perd des verifications defensives ; element-fingerprint.ts perd le cas 'code' (risque de rupture d'exhaustivite de switch). |
| components/slide-renderer/Editor/Canvas/hooks/useRotateElement.ts | 4 | rejected | Le diff reintroduit un import depuis l'ancien chemin @/lib/types/slides alors qu'upstream v0.3.0 a extrait ces types dans le package @openmaic/dsl (reellement present et utilise ailleurs dans le worktree). Appliquer romprait la coherence d'architecture avec le reste du code v0.3.0 non touche par cette story. Echantillon verifie manuellement (6 fichiers) : chacun perd aussi une fonctionnalite ou un correctif upstream reel au-dela du seul import (ex. SpotlightOverlay perd domIdPrefix + un correctif Tailwind ; BaseImageElement revient a un pattern de resolution media plus ancien perdant une correction documentee de contamination cross-course ; chartOption.ts perd des verifications defensives ; element-fingerprint.ts perd le cas 'code' (risque de rupture d'exhaustivite de switch). |
| components/slide-renderer/Editor/Canvas/hooks/useScaleElement.ts | 9 | rejected | Le diff reintroduit un import depuis l'ancien chemin @/lib/types/slides alors qu'upstream v0.3.0 a extrait ces types dans le package @openmaic/dsl (reellement present et utilise ailleurs dans le worktree). Appliquer romprait la coherence d'architecture avec le reste du code v0.3.0 non touche par cette story. Echantillon verifie manuellement (6 fichiers) : chacun perd aussi une fonctionnalite ou un correctif upstream reel au-dela du seul import (ex. SpotlightOverlay perd domIdPrefix + un correctif Tailwind ; BaseImageElement revient a un pattern de resolution media plus ancien perdant une correction documentee de contamination cross-course ; chartOption.ts perd des verifications defensives ; element-fingerprint.ts perd le cas 'code' (risque de rupture d'exhaustivite de switch). |
| components/slide-renderer/Editor/Canvas/hooks/useSelectElement.ts | 4 | rejected | Le diff reintroduit un import depuis l'ancien chemin @/lib/types/slides alors qu'upstream v0.3.0 a extrait ces types dans le package @openmaic/dsl (reellement present et utilise ailleurs dans le worktree). Appliquer romprait la coherence d'architecture avec le reste du code v0.3.0 non touche par cette story. Echantillon verifie manuellement (6 fichiers) : chacun perd aussi une fonctionnalite ou un correctif upstream reel au-dela du seul import (ex. SpotlightOverlay perd domIdPrefix + un correctif Tailwind ; BaseImageElement revient a un pattern de resolution media plus ancien perdant une correction documentee de contamination cross-course ; chartOption.ts perd des verifications defensives ; element-fingerprint.ts perd le cas 'code' (risque de rupture d'exhaustivite de switch). |
| components/slide-renderer/Editor/Canvas/index.tsx | 4 | rejected | Le diff reintroduit un import depuis l'ancien chemin @/lib/types/slides alors qu'upstream v0.3.0 a extrait ces types dans le package @openmaic/dsl (reellement present et utilise ailleurs dans le worktree). Appliquer romprait la coherence d'architecture avec le reste du code v0.3.0 non touche par cette story. Echantillon verifie manuellement (6 fichiers) : chacun perd aussi une fonctionnalite ou un correctif upstream reel au-dela du seul import (ex. SpotlightOverlay perd domIdPrefix + un correctif Tailwind ; BaseImageElement revient a un pattern de resolution media plus ancien perdant une correction documentee de contamination cross-course ; chartOption.ts perd des verifications defensives ; element-fingerprint.ts perd le cas 'code' (risque de rupture d'exhaustivite de switch). |
| components/slide-renderer/Editor/HighlightOverlay.tsx | 4 | rejected | Le diff reintroduit un import depuis l'ancien chemin @/lib/types/slides alors qu'upstream v0.3.0 a extrait ces types dans le package @openmaic/dsl (reellement present et utilise ailleurs dans le worktree). Appliquer romprait la coherence d'architecture avec le reste du code v0.3.0 non touche par cette story. Echantillon verifie manuellement (6 fichiers) : chacun perd aussi une fonctionnalite ou un correctif upstream reel au-dela du seul import (ex. SpotlightOverlay perd domIdPrefix + un correctif Tailwind ; BaseImageElement revient a un pattern de resolution media plus ancien perdant une correction documentee de contamination cross-course ; chartOption.ts perd des verifications defensives ; element-fingerprint.ts perd le cas 'code' (risque de rupture d'exhaustivite de switch). |
| components/slide-renderer/Editor/ScreenCanvas.tsx | 4 | rejected | Le diff reintroduit un import depuis l'ancien chemin @/lib/types/slides alors qu'upstream v0.3.0 a extrait ces types dans le package @openmaic/dsl (reellement present et utilise ailleurs dans le worktree). Appliquer romprait la coherence d'architecture avec le reste du code v0.3.0 non touche par cette story. Echantillon verifie manuellement (6 fichiers) : chacun perd aussi une fonctionnalite ou un correctif upstream reel au-dela du seul import (ex. SpotlightOverlay perd domIdPrefix + un correctif Tailwind ; BaseImageElement revient a un pattern de resolution media plus ancien perdant une correction documentee de contamination cross-course ; chartOption.ts perd des verifications defensives ; element-fingerprint.ts perd le cas 'code' (risque de rupture d'exhaustivite de switch). |
| components/slide-renderer/Editor/ScreenElement.tsx | 6 | rejected | Le diff reintroduit un import depuis l'ancien chemin @/lib/types/slides alors qu'upstream v0.3.0 a extrait ces types dans le package @openmaic/dsl (reellement present et utilise ailleurs dans le worktree). Appliquer romprait la coherence d'architecture avec le reste du code v0.3.0 non touche par cette story. Echantillon verifie manuellement (6 fichiers) : chacun perd aussi une fonctionnalite ou un correctif upstream reel au-dela du seul import (ex. SpotlightOverlay perd domIdPrefix + un correctif Tailwind ; BaseImageElement revient a un pattern de resolution media plus ancien perdant une correction documentee de contamination cross-course ; chartOption.ts perd des verifications defensives ; element-fingerprint.ts perd le cas 'code' (risque de rupture d'exhaustivite de switch). |
| components/slide-renderer/Editor/SpotlightOverlay.tsx | 28 | rejected | Le diff reintroduit un import depuis l'ancien chemin @/lib/types/slides alors qu'upstream v0.3.0 a extrait ces types dans le package @openmaic/dsl (reellement present et utilise ailleurs dans le worktree). Appliquer romprait la coherence d'architecture avec le reste du code v0.3.0 non touche par cette story. Echantillon verifie manuellement (6 fichiers) : chacun perd aussi une fonctionnalite ou un correctif upstream reel au-dela du seul import (ex. SpotlightOverlay perd domIdPrefix + un correctif Tailwind ; BaseImageElement revient a un pattern de resolution media plus ancien perdant une correction documentee de contamination cross-course ; chartOption.ts perd des verifications defensives ; element-fingerprint.ts perd le cas 'code' (risque de rupture d'exhaustivite de switch). |
| components/slide-renderer/components/element/ChartElement/BaseChartElement.tsx | 4 | rejected | Le diff reintroduit un import depuis l'ancien chemin @/lib/types/slides alors qu'upstream v0.3.0 a extrait ces types dans le package @openmaic/dsl (reellement present et utilise ailleurs dans le worktree). Appliquer romprait la coherence d'architecture avec le reste du code v0.3.0 non touche par cette story. Echantillon verifie manuellement (6 fichiers) : chacun perd aussi une fonctionnalite ou un correctif upstream reel au-dela du seul import (ex. SpotlightOverlay perd domIdPrefix + un correctif Tailwind ; BaseImageElement revient a un pattern de resolution media plus ancien perdant une correction documentee de contamination cross-course ; chartOption.ts perd des verifications defensives ; element-fingerprint.ts perd le cas 'code' (risque de rupture d'exhaustivite de switch). |
| components/slide-renderer/components/element/ChartElement/Chart.tsx | 4 | rejected | Le diff reintroduit un import depuis l'ancien chemin @/lib/types/slides alors qu'upstream v0.3.0 a extrait ces types dans le package @openmaic/dsl (reellement present et utilise ailleurs dans le worktree). Appliquer romprait la coherence d'architecture avec le reste du code v0.3.0 non touche par cette story. Echantillon verifie manuellement (6 fichiers) : chacun perd aussi une fonctionnalite ou un correctif upstream reel au-dela du seul import (ex. SpotlightOverlay perd domIdPrefix + un correctif Tailwind ; BaseImageElement revient a un pattern de resolution media plus ancien perdant une correction documentee de contamination cross-course ; chartOption.ts perd des verifications defensives ; element-fingerprint.ts perd le cas 'code' (risque de rupture d'exhaustivite de switch). |
| components/slide-renderer/components/element/ChartElement/chartOption.ts | 25 | rejected | Le diff reintroduit un import depuis l'ancien chemin @/lib/types/slides alors qu'upstream v0.3.0 a extrait ces types dans le package @openmaic/dsl (reellement present et utilise ailleurs dans le worktree). Appliquer romprait la coherence d'architecture avec le reste du code v0.3.0 non touche par cette story. Echantillon verifie manuellement (6 fichiers) : chacun perd aussi une fonctionnalite ou un correctif upstream reel au-dela du seul import (ex. SpotlightOverlay perd domIdPrefix + un correctif Tailwind ; BaseImageElement revient a un pattern de resolution media plus ancien perdant une correction documentee de contamination cross-course ; chartOption.ts perd des verifications defensives ; element-fingerprint.ts perd le cas 'code' (risque de rupture d'exhaustivite de switch). |
| components/slide-renderer/components/element/ChartElement/index.tsx | 4 | rejected | Le diff reintroduit un import depuis l'ancien chemin @/lib/types/slides alors qu'upstream v0.3.0 a extrait ces types dans le package @openmaic/dsl (reellement present et utilise ailleurs dans le worktree). Appliquer romprait la coherence d'architecture avec le reste du code v0.3.0 non touche par cette story. Echantillon verifie manuellement (6 fichiers) : chacun perd aussi une fonctionnalite ou un correctif upstream reel au-dela du seul import (ex. SpotlightOverlay perd domIdPrefix + un correctif Tailwind ; BaseImageElement revient a un pattern de resolution media plus ancien perdant une correction documentee de contamination cross-course ; chartOption.ts perd des verifications defensives ; element-fingerprint.ts perd le cas 'code' (risque de rupture d'exhaustivite de switch). |
| components/slide-renderer/components/element/ElementOutline.tsx | 4 | rejected | Le diff reintroduit un import depuis l'ancien chemin @/lib/types/slides alors qu'upstream v0.3.0 a extrait ces types dans le package @openmaic/dsl (reellement present et utilise ailleurs dans le worktree). Appliquer romprait la coherence d'architecture avec le reste du code v0.3.0 non touche par cette story. Echantillon verifie manuellement (6 fichiers) : chacun perd aussi une fonctionnalite ou un correctif upstream reel au-dela du seul import (ex. SpotlightOverlay perd domIdPrefix + un correctif Tailwind ; BaseImageElement revient a un pattern de resolution media plus ancien perdant une correction documentee de contamination cross-course ; chartOption.ts perd des verifications defensives ; element-fingerprint.ts perd le cas 'code' (risque de rupture d'exhaustivite de switch). |
| components/slide-renderer/components/element/ImageElement/BaseImageElement.tsx | 26 | rejected | Le diff reintroduit un import depuis l'ancien chemin @/lib/types/slides alors qu'upstream v0.3.0 a extrait ces types dans le package @openmaic/dsl (reellement present et utilise ailleurs dans le worktree). Appliquer romprait la coherence d'architecture avec le reste du code v0.3.0 non touche par cette story. Echantillon verifie manuellement (6 fichiers) : chacun perd aussi une fonctionnalite ou un correctif upstream reel au-dela du seul import (ex. SpotlightOverlay perd domIdPrefix + un correctif Tailwind ; BaseImageElement revient a un pattern de resolution media plus ancien perdant une correction documentee de contamination cross-course ; chartOption.ts perd des verifications defensives ; element-fingerprint.ts perd le cas 'code' (risque de rupture d'exhaustivite de switch). |
| components/slide-renderer/components/element/ImageElement/ImageClipHandler.tsx | 4 | rejected | Le diff reintroduit un import depuis l'ancien chemin @/lib/types/slides alors qu'upstream v0.3.0 a extrait ces types dans le package @openmaic/dsl (reellement present et utilise ailleurs dans le worktree). Appliquer romprait la coherence d'architecture avec le reste du code v0.3.0 non touche par cette story. Echantillon verifie manuellement (6 fichiers) : chacun perd aussi une fonctionnalite ou un correctif upstream reel au-dela du seul import (ex. SpotlightOverlay perd domIdPrefix + un correctif Tailwind ; BaseImageElement revient a un pattern de resolution media plus ancien perdant une correction documentee de contamination cross-course ; chartOption.ts perd des verifications defensives ; element-fingerprint.ts perd le cas 'code' (risque de rupture d'exhaustivite de switch). |
| components/slide-renderer/components/element/ImageElement/ImageOutline/image-ellipse-outline.tsx | 4 | rejected | Le diff reintroduit un import depuis l'ancien chemin @/lib/types/slides alors qu'upstream v0.3.0 a extrait ces types dans le package @openmaic/dsl (reellement present et utilise ailleurs dans le worktree). Appliquer romprait la coherence d'architecture avec le reste du code v0.3.0 non touche par cette story. Echantillon verifie manuellement (6 fichiers) : chacun perd aussi une fonctionnalite ou un correctif upstream reel au-dela du seul import (ex. SpotlightOverlay perd domIdPrefix + un correctif Tailwind ; BaseImageElement revient a un pattern de resolution media plus ancien perdant une correction documentee de contamination cross-course ; chartOption.ts perd des verifications defensives ; element-fingerprint.ts perd le cas 'code' (risque de rupture d'exhaustivite de switch). |
| components/slide-renderer/components/element/ImageElement/ImageOutline/image-polygon-outline.tsx | 4 | rejected | Le diff reintroduit un import depuis l'ancien chemin @/lib/types/slides alors qu'upstream v0.3.0 a extrait ces types dans le package @openmaic/dsl (reellement present et utilise ailleurs dans le worktree). Appliquer romprait la coherence d'architecture avec le reste du code v0.3.0 non touche par cette story. Echantillon verifie manuellement (6 fichiers) : chacun perd aussi une fonctionnalite ou un correctif upstream reel au-dela du seul import (ex. SpotlightOverlay perd domIdPrefix + un correctif Tailwind ; BaseImageElement revient a un pattern de resolution media plus ancien perdant une correction documentee de contamination cross-course ; chartOption.ts perd des verifications defensives ; element-fingerprint.ts perd le cas 'code' (risque de rupture d'exhaustivite de switch). |
| components/slide-renderer/components/element/ImageElement/ImageOutline/image-rect-outline.tsx | 4 | rejected | Le diff reintroduit un import depuis l'ancien chemin @/lib/types/slides alors qu'upstream v0.3.0 a extrait ces types dans le package @openmaic/dsl (reellement present et utilise ailleurs dans le worktree). Appliquer romprait la coherence d'architecture avec le reste du code v0.3.0 non touche par cette story. Echantillon verifie manuellement (6 fichiers) : chacun perd aussi une fonctionnalite ou un correctif upstream reel au-dela du seul import (ex. SpotlightOverlay perd domIdPrefix + un correctif Tailwind ; BaseImageElement revient a un pattern de resolution media plus ancien perdant une correction documentee de contamination cross-course ; chartOption.ts perd des verifications defensives ; element-fingerprint.ts perd le cas 'code' (risque de rupture d'exhaustivite de switch). |
| components/slide-renderer/components/element/ImageElement/ImageOutline/index.tsx | 4 | rejected | Le diff reintroduit un import depuis l'ancien chemin @/lib/types/slides alors qu'upstream v0.3.0 a extrait ces types dans le package @openmaic/dsl (reellement present et utilise ailleurs dans le worktree). Appliquer romprait la coherence d'architecture avec le reste du code v0.3.0 non touche par cette story. Echantillon verifie manuellement (6 fichiers) : chacun perd aussi une fonctionnalite ou un correctif upstream reel au-dela du seul import (ex. SpotlightOverlay perd domIdPrefix + un correctif Tailwind ; BaseImageElement revient a un pattern de resolution media plus ancien perdant une correction documentee de contamination cross-course ; chartOption.ts perd des verifications defensives ; element-fingerprint.ts perd le cas 'code' (risque de rupture d'exhaustivite de switch). |
| components/slide-renderer/components/element/ImageElement/index.tsx | 15 | rejected | Le diff reintroduit un import depuis l'ancien chemin @/lib/types/slides alors qu'upstream v0.3.0 a extrait ces types dans le package @openmaic/dsl (reellement present et utilise ailleurs dans le worktree). Appliquer romprait la coherence d'architecture avec le reste du code v0.3.0 non touche par cette story. Echantillon verifie manuellement (6 fichiers) : chacun perd aussi une fonctionnalite ou un correctif upstream reel au-dela du seul import (ex. SpotlightOverlay perd domIdPrefix + un correctif Tailwind ; BaseImageElement revient a un pattern de resolution media plus ancien perdant une correction documentee de contamination cross-course ; chartOption.ts perd des verifications defensives ; element-fingerprint.ts perd le cas 'code' (risque de rupture d'exhaustivite de switch). |
| components/slide-renderer/components/element/ImageElement/useClipImage.ts | 4 | rejected | Le diff reintroduit un import depuis l'ancien chemin @/lib/types/slides alors qu'upstream v0.3.0 a extrait ces types dans le package @openmaic/dsl (reellement present et utilise ailleurs dans le worktree). Appliquer romprait la coherence d'architecture avec le reste du code v0.3.0 non touche par cette story. Echantillon verifie manuellement (6 fichiers) : chacun perd aussi une fonctionnalite ou un correctif upstream reel au-dela du seul import (ex. SpotlightOverlay perd domIdPrefix + un correctif Tailwind ; BaseImageElement revient a un pattern de resolution media plus ancien perdant une correction documentee de contamination cross-course ; chartOption.ts perd des verifications defensives ; element-fingerprint.ts perd le cas 'code' (risque de rupture d'exhaustivite de switch). |
| components/slide-renderer/components/element/ImageElement/useFilter.ts | 4 | rejected | Le diff reintroduit un import depuis l'ancien chemin @/lib/types/slides alors qu'upstream v0.3.0 a extrait ces types dans le package @openmaic/dsl (reellement present et utilise ailleurs dans le worktree). Appliquer romprait la coherence d'architecture avec le reste du code v0.3.0 non touche par cette story. Echantillon verifie manuellement (6 fichiers) : chacun perd aussi une fonctionnalite ou un correctif upstream reel au-dela du seul import (ex. SpotlightOverlay perd domIdPrefix + un correctif Tailwind ; BaseImageElement revient a un pattern de resolution media plus ancien perdant une correction documentee de contamination cross-course ; chartOption.ts perd des verifications defensives ; element-fingerprint.ts perd le cas 'code' (risque de rupture d'exhaustivite de switch). |
| components/slide-renderer/components/element/LatexElement/BaseLatexElement.tsx | 4 | rejected | Le diff reintroduit un import depuis l'ancien chemin @/lib/types/slides alors qu'upstream v0.3.0 a extrait ces types dans le package @openmaic/dsl (reellement present et utilise ailleurs dans le worktree). Appliquer romprait la coherence d'architecture avec le reste du code v0.3.0 non touche par cette story. Echantillon verifie manuellement (6 fichiers) : chacun perd aussi une fonctionnalite ou un correctif upstream reel au-dela du seul import (ex. SpotlightOverlay perd domIdPrefix + un correctif Tailwind ; BaseImageElement revient a un pattern de resolution media plus ancien perdant une correction documentee de contamination cross-course ; chartOption.ts perd des verifications defensives ; element-fingerprint.ts perd le cas 'code' (risque de rupture d'exhaustivite de switch). |
| components/slide-renderer/components/element/LatexElement/index.tsx | 4 | rejected | Le diff reintroduit un import depuis l'ancien chemin @/lib/types/slides alors qu'upstream v0.3.0 a extrait ces types dans le package @openmaic/dsl (reellement present et utilise ailleurs dans le worktree). Appliquer romprait la coherence d'architecture avec le reste du code v0.3.0 non touche par cette story. Echantillon verifie manuellement (6 fichiers) : chacun perd aussi une fonctionnalite ou un correctif upstream reel au-dela du seul import (ex. SpotlightOverlay perd domIdPrefix + un correctif Tailwind ; BaseImageElement revient a un pattern de resolution media plus ancien perdant une correction documentee de contamination cross-course ; chartOption.ts perd des verifications defensives ; element-fingerprint.ts perd le cas 'code' (risque de rupture d'exhaustivite de switch). |
| components/slide-renderer/components/element/LineElement/BaseLineElement.tsx | 4 | rejected | Le diff reintroduit un import depuis l'ancien chemin @/lib/types/slides alors qu'upstream v0.3.0 a extrait ces types dans le package @openmaic/dsl (reellement present et utilise ailleurs dans le worktree). Appliquer romprait la coherence d'architecture avec le reste du code v0.3.0 non touche par cette story. Echantillon verifie manuellement (6 fichiers) : chacun perd aussi une fonctionnalite ou un correctif upstream reel au-dela du seul import (ex. SpotlightOverlay perd domIdPrefix + un correctif Tailwind ; BaseImageElement revient a un pattern de resolution media plus ancien perdant une correction documentee de contamination cross-course ; chartOption.ts perd des verifications defensives ; element-fingerprint.ts perd le cas 'code' (risque de rupture d'exhaustivite de switch). |
| components/slide-renderer/components/element/LineElement/LinePointMarker.tsx | 4 | rejected | Le diff reintroduit un import depuis l'ancien chemin @/lib/types/slides alors qu'upstream v0.3.0 a extrait ces types dans le package @openmaic/dsl (reellement present et utilise ailleurs dans le worktree). Appliquer romprait la coherence d'architecture avec le reste du code v0.3.0 non touche par cette story. Echantillon verifie manuellement (6 fichiers) : chacun perd aussi une fonctionnalite ou un correctif upstream reel au-dela du seul import (ex. SpotlightOverlay perd domIdPrefix + un correctif Tailwind ; BaseImageElement revient a un pattern de resolution media plus ancien perdant une correction documentee de contamination cross-course ; chartOption.ts perd des verifications defensives ; element-fingerprint.ts perd le cas 'code' (risque de rupture d'exhaustivite de switch). |
| components/slide-renderer/components/element/LineElement/index.tsx | 4 | rejected | Le diff reintroduit un import depuis l'ancien chemin @/lib/types/slides alors qu'upstream v0.3.0 a extrait ces types dans le package @openmaic/dsl (reellement present et utilise ailleurs dans le worktree). Appliquer romprait la coherence d'architecture avec le reste du code v0.3.0 non touche par cette story. Echantillon verifie manuellement (6 fichiers) : chacun perd aussi une fonctionnalite ou un correctif upstream reel au-dela du seul import (ex. SpotlightOverlay perd domIdPrefix + un correctif Tailwind ; BaseImageElement revient a un pattern de resolution media plus ancien perdant une correction documentee de contamination cross-course ; chartOption.ts perd des verifications defensives ; element-fingerprint.ts perd le cas 'code' (risque de rupture d'exhaustivite de switch). |
| components/slide-renderer/components/element/ShapeElement/BaseShapeElement.tsx | 4 | rejected | Le diff reintroduit un import depuis l'ancien chemin @/lib/types/slides alors qu'upstream v0.3.0 a extrait ces types dans le package @openmaic/dsl (reellement present et utilise ailleurs dans le worktree). Appliquer romprait la coherence d'architecture avec le reste du code v0.3.0 non touche par cette story. Echantillon verifie manuellement (6 fichiers) : chacun perd aussi une fonctionnalite ou un correctif upstream reel au-dela du seul import (ex. SpotlightOverlay perd domIdPrefix + un correctif Tailwind ; BaseImageElement revient a un pattern de resolution media plus ancien perdant une correction documentee de contamination cross-course ; chartOption.ts perd des verifications defensives ; element-fingerprint.ts perd le cas 'code' (risque de rupture d'exhaustivite de switch). |
| components/slide-renderer/components/element/ShapeElement/GradientDefs.tsx | 4 | rejected | Le diff reintroduit un import depuis l'ancien chemin @/lib/types/slides alors qu'upstream v0.3.0 a extrait ces types dans le package @openmaic/dsl (reellement present et utilise ailleurs dans le worktree). Appliquer romprait la coherence d'architecture avec le reste du code v0.3.0 non touche par cette story. Echantillon verifie manuellement (6 fichiers) : chacun perd aussi une fonctionnalite ou un correctif upstream reel au-dela du seul import (ex. SpotlightOverlay perd domIdPrefix + un correctif Tailwind ; BaseImageElement revient a un pattern de resolution media plus ancien perdant une correction documentee de contamination cross-course ; chartOption.ts perd des verifications defensives ; element-fingerprint.ts perd le cas 'code' (risque de rupture d'exhaustivite de switch). |
| components/slide-renderer/components/element/ShapeElement/index.tsx | 4 | rejected | Le diff reintroduit un import depuis l'ancien chemin @/lib/types/slides alors qu'upstream v0.3.0 a extrait ces types dans le package @openmaic/dsl (reellement present et utilise ailleurs dans le worktree). Appliquer romprait la coherence d'architecture avec le reste du code v0.3.0 non touche par cette story. Echantillon verifie manuellement (6 fichiers) : chacun perd aussi une fonctionnalite ou un correctif upstream reel au-dela du seul import (ex. SpotlightOverlay perd domIdPrefix + un correctif Tailwind ; BaseImageElement revient a un pattern de resolution media plus ancien perdant une correction documentee de contamination cross-course ; chartOption.ts perd des verifications defensives ; element-fingerprint.ts perd le cas 'code' (risque de rupture d'exhaustivite de switch). |
| components/slide-renderer/components/element/TableElement/BaseTableElement.tsx | 4 | rejected | Le diff reintroduit un import depuis l'ancien chemin @/lib/types/slides alors qu'upstream v0.3.0 a extrait ces types dans le package @openmaic/dsl (reellement present et utilise ailleurs dans le worktree). Appliquer romprait la coherence d'architecture avec le reste du code v0.3.0 non touche par cette story. Echantillon verifie manuellement (6 fichiers) : chacun perd aussi une fonctionnalite ou un correctif upstream reel au-dela du seul import (ex. SpotlightOverlay perd domIdPrefix + un correctif Tailwind ; BaseImageElement revient a un pattern de resolution media plus ancien perdant une correction documentee de contamination cross-course ; chartOption.ts perd des verifications defensives ; element-fingerprint.ts perd le cas 'code' (risque de rupture d'exhaustivite de switch). |
| components/slide-renderer/components/element/TableElement/StaticTable.tsx | 4 | rejected | Le diff reintroduit un import depuis l'ancien chemin @/lib/types/slides alors qu'upstream v0.3.0 a extrait ces types dans le package @openmaic/dsl (reellement present et utilise ailleurs dans le worktree). Appliquer romprait la coherence d'architecture avec le reste du code v0.3.0 non touche par cette story. Echantillon verifie manuellement (6 fichiers) : chacun perd aussi une fonctionnalite ou un correctif upstream reel au-dela du seul import (ex. SpotlightOverlay perd domIdPrefix + un correctif Tailwind ; BaseImageElement revient a un pattern de resolution media plus ancien perdant une correction documentee de contamination cross-course ; chartOption.ts perd des verifications defensives ; element-fingerprint.ts perd le cas 'code' (risque de rupture d'exhaustivite de switch). |
| components/slide-renderer/components/element/TableElement/index.tsx | 4 | rejected | Le diff reintroduit un import depuis l'ancien chemin @/lib/types/slides alors qu'upstream v0.3.0 a extrait ces types dans le package @openmaic/dsl (reellement present et utilise ailleurs dans le worktree). Appliquer romprait la coherence d'architecture avec le reste du code v0.3.0 non touche par cette story. Echantillon verifie manuellement (6 fichiers) : chacun perd aussi une fonctionnalite ou un correctif upstream reel au-dela du seul import (ex. SpotlightOverlay perd domIdPrefix + un correctif Tailwind ; BaseImageElement revient a un pattern de resolution media plus ancien perdant une correction documentee de contamination cross-course ; chartOption.ts perd des verifications defensives ; element-fingerprint.ts perd le cas 'code' (risque de rupture d'exhaustivite de switch). |
| components/slide-renderer/components/element/TableElement/tableUtils.ts | 4 | rejected | Le diff reintroduit un import depuis l'ancien chemin @/lib/types/slides alors qu'upstream v0.3.0 a extrait ces types dans le package @openmaic/dsl (reellement present et utilise ailleurs dans le worktree). Appliquer romprait la coherence d'architecture avec le reste du code v0.3.0 non touche par cette story. Echantillon verifie manuellement (6 fichiers) : chacun perd aussi une fonctionnalite ou un correctif upstream reel au-dela du seul import (ex. SpotlightOverlay perd domIdPrefix + un correctif Tailwind ; BaseImageElement revient a un pattern de resolution media plus ancien perdant une correction documentee de contamination cross-course ; chartOption.ts perd des verifications defensives ; element-fingerprint.ts perd le cas 'code' (risque de rupture d'exhaustivite de switch). |
| components/slide-renderer/components/element/TextElement/BaseTextElement.tsx | 4 | rejected | Le diff reintroduit un import depuis l'ancien chemin @/lib/types/slides alors qu'upstream v0.3.0 a extrait ces types dans le package @openmaic/dsl (reellement present et utilise ailleurs dans le worktree). Appliquer romprait la coherence d'architecture avec le reste du code v0.3.0 non touche par cette story. Echantillon verifie manuellement (6 fichiers) : chacun perd aussi une fonctionnalite ou un correctif upstream reel au-dela du seul import (ex. SpotlightOverlay perd domIdPrefix + un correctif Tailwind ; BaseImageElement revient a un pattern de resolution media plus ancien perdant une correction documentee de contamination cross-course ; chartOption.ts perd des verifications defensives ; element-fingerprint.ts perd le cas 'code' (risque de rupture d'exhaustivite de switch). |
| components/slide-renderer/components/element/TextElement/index.tsx | 4 | rejected | Le diff reintroduit un import depuis l'ancien chemin @/lib/types/slides alors qu'upstream v0.3.0 a extrait ces types dans le package @openmaic/dsl (reellement present et utilise ailleurs dans le worktree). Appliquer romprait la coherence d'architecture avec le reste du code v0.3.0 non touche par cette story. Echantillon verifie manuellement (6 fichiers) : chacun perd aussi une fonctionnalite ou un correctif upstream reel au-dela du seul import (ex. SpotlightOverlay perd domIdPrefix + un correctif Tailwind ; BaseImageElement revient a un pattern de resolution media plus ancien perdant une correction documentee de contamination cross-course ; chartOption.ts perd des verifications defensives ; element-fingerprint.ts perd le cas 'code' (risque de rupture d'exhaustivite de switch). |
| components/slide-renderer/components/element/VideoElement/index.tsx | 4 | rejected | Le diff reintroduit un import depuis l'ancien chemin @/lib/types/slides alors qu'upstream v0.3.0 a extrait ces types dans le package @openmaic/dsl (reellement present et utilise ailleurs dans le worktree). Appliquer romprait la coherence d'architecture avec le reste du code v0.3.0 non touche par cette story. Echantillon verifie manuellement (6 fichiers) : chacun perd aussi une fonctionnalite ou un correctif upstream reel au-dela du seul import (ex. SpotlightOverlay perd domIdPrefix + un correctif Tailwind ; BaseImageElement revient a un pattern de resolution media plus ancien perdant une correction documentee de contamination cross-course ; chartOption.ts perd des verifications defensives ; element-fingerprint.ts perd le cas 'code' (risque de rupture d'exhaustivite de switch). |
| components/slide-renderer/components/element/hooks/useElementFill.ts | 4 | rejected | Le diff reintroduit un import depuis l'ancien chemin @/lib/types/slides alors qu'upstream v0.3.0 a extrait ces types dans le package @openmaic/dsl (reellement present et utilise ailleurs dans le worktree). Appliquer romprait la coherence d'architecture avec le reste du code v0.3.0 non touche par cette story. Echantillon verifie manuellement (6 fichiers) : chacun perd aussi une fonctionnalite ou un correctif upstream reel au-dela du seul import (ex. SpotlightOverlay perd domIdPrefix + un correctif Tailwind ; BaseImageElement revient a un pattern de resolution media plus ancien perdant une correction documentee de contamination cross-course ; chartOption.ts perd des verifications defensives ; element-fingerprint.ts perd le cas 'code' (risque de rupture d'exhaustivite de switch). |
| components/slide-renderer/components/element/hooks/useElementOutline.ts | 4 | rejected | Le diff reintroduit un import depuis l'ancien chemin @/lib/types/slides alors qu'upstream v0.3.0 a extrait ces types dans le package @openmaic/dsl (reellement present et utilise ailleurs dans le worktree). Appliquer romprait la coherence d'architecture avec le reste du code v0.3.0 non touche par cette story. Echantillon verifie manuellement (6 fichiers) : chacun perd aussi une fonctionnalite ou un correctif upstream reel au-dela du seul import (ex. SpotlightOverlay perd domIdPrefix + un correctif Tailwind ; BaseImageElement revient a un pattern de resolution media plus ancien perdant une correction documentee de contamination cross-course ; chartOption.ts perd des verifications defensives ; element-fingerprint.ts perd le cas 'code' (risque de rupture d'exhaustivite de switch). |
| components/slide-renderer/components/element/hooks/useElementShadow.ts | 4 | rejected | Le diff reintroduit un import depuis l'ancien chemin @/lib/types/slides alors qu'upstream v0.3.0 a extrait ces types dans le package @openmaic/dsl (reellement present et utilise ailleurs dans le worktree). Appliquer romprait la coherence d'architecture avec le reste du code v0.3.0 non touche par cette story. Echantillon verifie manuellement (6 fichiers) : chacun perd aussi une fonctionnalite ou un correctif upstream reel au-dela du seul import (ex. SpotlightOverlay perd domIdPrefix + un correctif Tailwind ; BaseImageElement revient a un pattern de resolution media plus ancien perdant une correction documentee de contamination cross-course ; chartOption.ts perd des verifications defensives ; element-fingerprint.ts perd le cas 'code' (risque de rupture d'exhaustivite de switch). |
| components/stage/scene-renderer.tsx | 17 | rejected | Perd le support du type de scene 'plugin' (PluginRenderer) ajoute par upstream -- perte de fonctionnalite entiere, pas une divergence cosmetique. |
| components/ui/popover.tsx | 8 | rejected | Retire les exports PopoverAnchor/PopoverClose ajoutes par upstream -- risque de casser d'autres fichiers (hors perimetre) qui les importeraient. |
| components/whiteboard/whiteboard-canvas.tsx | 14 | rejected | Le diff reintroduit un import depuis l'ancien chemin @/lib/types/slides alors qu'upstream v0.3.0 a extrait ces types dans le package @openmaic/dsl (reellement present et utilise ailleurs dans le worktree). Appliquer romprait la coherence d'architecture avec le reste du code v0.3.0 non touche par cette story. Echantillon verifie manuellement (6 fichiers) : chacun perd aussi une fonctionnalite ou un correctif upstream reel au-dela du seul import (ex. SpotlightOverlay perd domIdPrefix + un correctif Tailwind ; BaseImageElement revient a un pattern de resolution media plus ancien perdant une correction documentee de contamination cross-course ; chartOption.ts perd des verifications defensives ; element-fingerprint.ts perd le cas 'code' (risque de rupture d'exhaustivite de switch). |
| components/whiteboard/whiteboard-history.tsx | 7 | noop | contenu main deja applique au worktree lors d'une execution precedente (source=git) — idempotent, rien a refaire. Motif d'application : Coherence i18n : les traductions Qalem deja portees (lib/i18n/stage.ts) utilisent des placeholders a accolade simple '{n}' (pas '{{n}}' i18next). Le t(key,{n}) upstream ne les interpolerait pas (react-i18next attend '{{n}}') -- le .replace('{n}',...) de main est necessaire pour un affichage correct. |
| configs/animation.ts | 4 | rejected | Le diff reintroduit un import depuis l'ancien chemin @/lib/types/slides alors qu'upstream v0.3.0 a extrait ces types dans le package @openmaic/dsl (reellement present et utilise ailleurs dans le worktree). Appliquer romprait la coherence d'architecture avec le reste du code v0.3.0 non touche par cette story. Echantillon verifie manuellement (6 fichiers) : chacun perd aussi une fonctionnalite ou un correctif upstream reel au-dela du seul import (ex. SpotlightOverlay perd domIdPrefix + un correctif Tailwind ; BaseImageElement revient a un pattern de resolution media plus ancien perdant une correction documentee de contamination cross-course ; chartOption.ts perd des verifications defensives ; element-fingerprint.ts perd le cas 'code' (risque de rupture d'exhaustivite de switch). |
| configs/chart.ts | 4 | rejected | Le diff reintroduit un import depuis l'ancien chemin @/lib/types/slides alors qu'upstream v0.3.0 a extrait ces types dans le package @openmaic/dsl (reellement present et utilise ailleurs dans le worktree). Appliquer romprait la coherence d'architecture avec le reste du code v0.3.0 non touche par cette story. Echantillon verifie manuellement (6 fichiers) : chacun perd aussi une fonctionnalite ou un correctif upstream reel au-dela du seul import (ex. SpotlightOverlay perd domIdPrefix + un correctif Tailwind ; BaseImageElement revient a un pattern de resolution media plus ancien perdant une correction documentee de contamination cross-course ; chartOption.ts perd des verifications defensives ; element-fingerprint.ts perd le cas 'code' (risque de rupture d'exhaustivite de switch). |
| configs/lines.ts | 4 | rejected | Le diff reintroduit un import depuis l'ancien chemin @/lib/types/slides alors qu'upstream v0.3.0 a extrait ces types dans le package @openmaic/dsl (reellement present et utilise ailleurs dans le worktree). Appliquer romprait la coherence d'architecture avec le reste du code v0.3.0 non touche par cette story. Echantillon verifie manuellement (6 fichiers) : chacun perd aussi une fonctionnalite ou un correctif upstream reel au-dela du seul import (ex. SpotlightOverlay perd domIdPrefix + un correctif Tailwind ; BaseImageElement revient a un pattern de resolution media plus ancien perdant une correction documentee de contamination cross-course ; chartOption.ts perd des verifications defensives ; element-fingerprint.ts perd le cas 'code' (risque de rupture d'exhaustivite de switch). |
| configs/shapes.ts | 4 | rejected | Le diff reintroduit un import depuis l'ancien chemin @/lib/types/slides alors qu'upstream v0.3.0 a extrait ces types dans le package @openmaic/dsl (reellement present et utilise ailleurs dans le worktree). Appliquer romprait la coherence d'architecture avec le reste du code v0.3.0 non touche par cette story. Echantillon verifie manuellement (6 fichiers) : chacun perd aussi une fonctionnalite ou un correctif upstream reel au-dela du seul import (ex. SpotlightOverlay perd domIdPrefix + un correctif Tailwind ; BaseImageElement revient a un pattern de resolution media plus ancien perdant une correction documentee de contamination cross-course ; chartOption.ts perd des verifications defensives ; element-fingerprint.ts perd le cas 'code' (risque de rupture d'exhaustivite de switch). |
| configs/theme.ts | 4 | rejected | Le diff reintroduit un import depuis l'ancien chemin @/lib/types/slides alors qu'upstream v0.3.0 a extrait ces types dans le package @openmaic/dsl (reellement present et utilise ailleurs dans le worktree). Appliquer romprait la coherence d'architecture avec le reste du code v0.3.0 non touche par cette story. Echantillon verifie manuellement (6 fichiers) : chacun perd aussi une fonctionnalite ou un correctif upstream reel au-dela du seul import (ex. SpotlightOverlay perd domIdPrefix + un correctif Tailwind ; BaseImageElement revient a un pattern de resolution media plus ancien perdant une correction documentee de contamination cross-course ; chartOption.ts perd des verifications defensives ; element-fingerprint.ts perd le cas 'code' (risque de rupture d'exhaustivite de switch). |
| e2e/fixtures/mock-api.ts | 27 | rejected | Upstream a ajoute l'extraction automatique de stageId depuis le corps de requete (robustesse) que main n'a pas encore -- fichier de test uniquement mais retrait d'une fonctionnalite reelle sans necessite identifiee ; rejet par coherence de politique. |
| e2e/fixtures/test-data/scene-content.ts | 6 | rejected | Le diff reintroduit un import depuis l'ancien chemin @/lib/types/slides alors qu'upstream v0.3.0 a extrait ces types dans le package @openmaic/dsl (reellement present et utilise ailleurs dans le worktree). Appliquer romprait la coherence d'architecture avec le reste du code v0.3.0 non touche par cette story. Echantillon verifie manuellement (6 fichiers) : chacun perd aussi une fonctionnalite ou un correctif upstream reel au-dela du seul import (ex. SpotlightOverlay perd domIdPrefix + un correctif Tailwind ; BaseImageElement revient a un pattern de resolution media plus ancien perdant une correction documentee de contamination cross-course ; chartOption.ts perd des verifications defensives ; element-fingerprint.ts perd le cas 'code' (risque de rupture d'exhaustivite de switch). |
| e2e/fixtures/test-data/settings.ts | 3 | rejected | Perd le champ reviewOutlineEnabled ajoute par upstream (fixture de test). |
| e2e/pages/home.page.ts | 4 | noop | contenu main deja applique au worktree lors d'une execution precedente (source=git) — idempotent, rien a refaire. Motif d'application : Branding : selecteur de test cherche alt='Qalem' au lieu de alt='OpenMAIC' -- necessaire pour que les e2e trouvent le logo une fois le rebranding applique (deja fait sur layout.tsx/page.tsx via OURS_ONLY). |
| eslint.config.mjs | 15 | noop | contenu main deja applique au worktree lors d'une execution precedente (source=git) — idempotent, rien a refaire. Motif d'application : Le remplacement des ignores granulaires par 'packages/**' est un SUR-ensemble (ignore AU MOINS autant, y compris packages/@openmaic et packages/docs) -- reduit le perimetre de lint, ne peut pas introduire de nouvelle erreur. |
| lib/api/stage-api-canvas.ts | 4 | rejected | Le diff reintroduit un import depuis l'ancien chemin @/lib/types/slides alors qu'upstream v0.3.0 a extrait ces types dans le package @openmaic/dsl (reellement present et utilise ailleurs dans le worktree). Appliquer romprait la coherence d'architecture avec le reste du code v0.3.0 non touche par cette story. Echantillon verifie manuellement (6 fichiers) : chacun perd aussi une fonctionnalite ou un correctif upstream reel au-dela du seul import (ex. SpotlightOverlay perd domIdPrefix + un correctif Tailwind ; BaseImageElement revient a un pattern de resolution media plus ancien perdant une correction documentee de contamination cross-course ; chartOption.ts perd des verifications defensives ; element-fingerprint.ts perd le cas 'code' (risque de rupture d'exhaustivite de switch). |
| lib/api/stage-api-element.ts | 4 | rejected | Le diff reintroduit un import depuis l'ancien chemin @/lib/types/slides alors qu'upstream v0.3.0 a extrait ces types dans le package @openmaic/dsl (reellement present et utilise ailleurs dans le worktree). Appliquer romprait la coherence d'architecture avec le reste du code v0.3.0 non touche par cette story. Echantillon verifie manuellement (6 fichiers) : chacun perd aussi une fonctionnalite ou un correctif upstream reel au-dela du seul import (ex. SpotlightOverlay perd domIdPrefix + un correctif Tailwind ; BaseImageElement revient a un pattern de resolution media plus ancien perdant une correction documentee de contamination cross-course ; chartOption.ts perd des verifications defensives ; element-fingerprint.ts perd le cas 'code' (risque de rupture d'exhaustivite de switch). |
| lib/api/stage-api-types.ts | 4 | rejected | Le diff reintroduit un import depuis l'ancien chemin @/lib/types/slides alors qu'upstream v0.3.0 a extrait ces types dans le package @openmaic/dsl (reellement present et utilise ailleurs dans le worktree). Appliquer romprait la coherence d'architecture avec le reste du code v0.3.0 non touche par cette story. Echantillon verifie manuellement (6 fichiers) : chacun perd aussi une fonctionnalite ou un correctif upstream reel au-dela du seul import (ex. SpotlightOverlay perd domIdPrefix + un correctif Tailwind ; BaseImageElement revient a un pattern de resolution media plus ancien perdant une correction documentee de contamination cross-course ; chartOption.ts perd des verifications defensives ; element-fingerprint.ts perd le cas 'code' (risque de rupture d'exhaustivite de switch). |
| lib/api/stage-api-whiteboard.ts | 4 | rejected | Le diff reintroduit un import depuis l'ancien chemin @/lib/types/slides alors qu'upstream v0.3.0 a extrait ces types dans le package @openmaic/dsl (reellement present et utilise ailleurs dans le worktree). Appliquer romprait la coherence d'architecture avec le reste du code v0.3.0 non touche par cette story. Echantillon verifie manuellement (6 fichiers) : chacun perd aussi une fonctionnalite ou un correctif upstream reel au-dela du seul import (ex. SpotlightOverlay perd domIdPrefix + un correctif Tailwind ; BaseImageElement revient a un pattern de resolution media plus ancien perdant une correction documentee de contamination cross-course ; chartOption.ts perd des verifications defensives ; element-fingerprint.ts perd le cas 'code' (risque de rupture d'exhaustivite de switch). |
| lib/audio/browser-tts-preview.ts | 16 | noop | contenu main deja applique au worktree lors d'une execution precedente (source=git) — idempotent, rien a refaire. Motif d'application : Additif pur : main ajoute la detection de langue arabe (ar-MA) et francaise (fr-FR) a inferPreviewLang(), sans retirer aucune capacite upstream. Coeur de la mission FR/AR de Qalem. |
| lib/audio/use-tts-preview.ts | 6 | rejected | Perd modelId et providerOptions (options TTS avancees) ajoutes par upstream. |
| lib/export/svg-path-parser.ts | 24 | rejected | Upstream a ajoute une gestion defensive des chemins SVG malformes (try/catch, retour [] documente comme corrigeant un crash reel cause par des hallucinations LLM). Appliquer main REINTRODUIRAIT ce risque de crash. |
| lib/generation/generation-pipeline.ts | 6 | rejected | Renomme buildLanguageText en getLanguageName/getLanguageQualityRules (meme cluster languageDirective que scene-builder.ts/pipeline-runner.ts) et retire un re-export de types (SceneContentOptions/SceneActionsOptions) potentiellement utilise ailleurs. |
| lib/generation/interactive-post-processor.ts | 25 | rejected | Upstream corrige un bug de performance documente (O(blocs x longueur) avec l'ancienne boucle indexOf+substring, remplace par un replacer fonction qui est deja immunise contre les caracteres '$' -- le commentaire de main affirme a tort le contraire). Appliquer main REINTRODUIRAIT ce bug de perf deja corrige. |
| lib/generation/pipeline-runner.ts | 25 | rejected | CORRECTION apres re-verification rigoureuse du sens du diff : c'est en realite MAIN qui contient encore les messages de statut CODES EN DUR EN CHINOIS ('正在分析需求...', '正在生成场景内容...', '生成完成！') alors qu'UPSTREAM les a DEJA en anglais -- appliquer main REINTRODUIRAIT du chinois fige dans l'UI, contraire a l'objectif meme de Qalem. De plus main perd la destructuration 'languageDirective' (fonctionnalite upstream plus recente, cf. cluster scene-builder.ts/generation-preview/types.ts). Rejet sur les deux motifs. |
| lib/generation/scene-builder.ts | 27 | rejected | Le diff reintroduit un import depuis l'ancien chemin @/lib/types/slides alors qu'upstream v0.3.0 a extrait ces types dans le package @openmaic/dsl (reellement present et utilise ailleurs dans le worktree). Appliquer romprait la coherence d'architecture avec le reste du code v0.3.0 non touche par cette story. Echantillon verifie manuellement (6 fichiers) : chacun perd aussi une fonctionnalite ou un correctif upstream reel au-dela du seul import (ex. SpotlightOverlay perd domIdPrefix + un correctif Tailwind ; BaseImageElement revient a un pattern de resolution media plus ancien perdant une correction documentee de contamination cross-course ; chartOption.ts perd des verifications defensives ; element-fingerprint.ts perd le cas 'code' (risque de rupture d'exhaustivite de switch). |
| lib/hooks/use-browser-asr.ts | 13 | rejected | Le diff reintroduit un import depuis l'ancien chemin @/lib/types/slides alors qu'upstream v0.3.0 a extrait ces types dans le package @openmaic/dsl (reellement present et utilise ailleurs dans le worktree). Appliquer romprait la coherence d'architecture avec le reste du code v0.3.0 non touche par cette story. Echantillon verifie manuellement (6 fichiers) : chacun perd aussi une fonctionnalite ou un correctif upstream reel au-dela du seul import (ex. SpotlightOverlay perd domIdPrefix + un correctif Tailwind ; BaseImageElement revient a un pattern de resolution media plus ancien perdant une correction documentee de contamination cross-course ; chartOption.ts perd des verifications defensives ; element-fingerprint.ts perd le cas 'code' (risque de rupture d'exhaustivite de switch). |
| lib/hooks/use-canvas-operations.ts | 8 | rejected | Le diff reintroduit un import depuis l'ancien chemin @/lib/types/slides alors qu'upstream v0.3.0 a extrait ces types dans le package @openmaic/dsl (reellement present et utilise ailleurs dans le worktree). Appliquer romprait la coherence d'architecture avec le reste du code v0.3.0 non touche par cette story. Echantillon verifie manuellement (6 fichiers) : chacun perd aussi une fonctionnalite ou un correctif upstream reel au-dela du seul import (ex. SpotlightOverlay perd domIdPrefix + un correctif Tailwind ; BaseImageElement revient a un pattern de resolution media plus ancien perdant une correction documentee de contamination cross-course ; chartOption.ts perd des verifications defensives ; element-fingerprint.ts perd le cas 'code' (risque de rupture d'exhaustivite de switch). |
| lib/hooks/use-order-element.ts | 6 | rejected | Le diff reintroduit un import depuis l'ancien chemin @/lib/types/slides alors qu'upstream v0.3.0 a extrait ces types dans le package @openmaic/dsl (reellement present et utilise ailleurs dans le worktree). Appliquer romprait la coherence d'architecture avec le reste du code v0.3.0 non touche par cette story. Echantillon verifie manuellement (6 fichiers) : chacun perd aussi une fonctionnalite ou un correctif upstream reel au-dela du seul import (ex. SpotlightOverlay perd domIdPrefix + un correctif Tailwind ; BaseImageElement revient a un pattern de resolution media plus ancien perdant une correction documentee de contamination cross-course ; chartOption.ts perd des verifications defensives ; element-fingerprint.ts perd le cas 'code' (risque de rupture d'exhaustivite de switch). |
| lib/hooks/use-slide-background-style.ts | 4 | rejected | Le diff reintroduit un import depuis l'ancien chemin @/lib/types/slides alors qu'upstream v0.3.0 a extrait ces types dans le package @openmaic/dsl (reellement present et utilise ailleurs dans le worktree). Appliquer romprait la coherence d'architecture avec le reste du code v0.3.0 non touche par cette story. Echantillon verifie manuellement (6 fichiers) : chacun perd aussi une fonctionnalite ou un correctif upstream reel au-dela du seul import (ex. SpotlightOverlay perd domIdPrefix + un correctif Tailwind ; BaseImageElement revient a un pattern de resolution media plus ancien perdant une correction documentee de contamination cross-course ; chartOption.ts perd des verifications defensives ; element-fingerprint.ts perd le cas 'code' (risque de rupture d'exhaustivite de switch). |
| lib/hooks/use-streaming-text.ts | 17 | rejected | Le diff reintroduit un import depuis l'ancien chemin @/lib/types/slides alors qu'upstream v0.3.0 a extrait ces types dans le package @openmaic/dsl (reellement present et utilise ailleurs dans le worktree). Appliquer romprait la coherence d'architecture avec le reste du code v0.3.0 non touche par cette story. Echantillon verifie manuellement (6 fichiers) : chacun perd aussi une fonctionnalite ou un correctif upstream reel au-dela du seul import (ex. SpotlightOverlay perd domIdPrefix + un correctif Tailwind ; BaseImageElement revient a un pattern de resolution media plus ancien perdant une correction documentee de contamination cross-course ; chartOption.ts perd des verifications defensives ; element-fingerprint.ts perd le cas 'code' (risque de rupture d'exhaustivite de switch). |
| lib/hooks/use-theme.tsx | 6 | noop | contenu main deja applique au worktree lors d'une execution precedente (source=git) — idempotent, rien a refaire. Motif d'application : Defaut de theme 'dark'/'dark' au lieu de 'system'/'light' -- choix UX cosmetique reversible, ne retire aucune capacite upstream, ne touche aucun type/API partagee. |
| lib/i18n/types.ts | 8 | noop | contenu main deja applique au worktree lors d'une execution precedente (source=git) — idempotent, rien a refaire. Motif d'application : Necessaire : le systeme i18n deja porte (OURS_ONLY, use-i18n.tsx via react-i18next) depend du type Locale incluant fr-FR/ar-MA et de defaultLocale='fr-FR'. La version upstream derive Locale du nouveau registre lib/i18n/locales.ts (zh-CN/zh-TW/en-US/ja-JP/ru-RU/ar-SA/pt-BR/ko-KR, PAS fr-FR/ar-MA) -- la garder casserait la compilation du systeme i18n deja porte. |
| lib/media/types.ts | 22 | rejected | Retrecit drastiquement ImageProviderId/VideoProviderId (perd openai-image, minimax-image, grok-image, lemonade, minimax-video, happyhorse) -- risque concret de casser d'autres fichiers (hors perimetre des 137) qui referencent ces IDs, contraire a l'objectif tsc vert. |
| lib/orchestration/registry/store.ts | 24 | rejected | Melange : la traduction des noms d'agents par defaut (chinois -> anglais) est souhaitable (cf. Known Issues progress.md) MAIS indissociable ici de pertes reelles (type VoiceDesign, actions wb_draw_code/wb_edit_code, warm-up vocal). Renommage a refaire chirurgicalement, plus probablement dans S0-006 (branding). |
| lib/orchestration/registry/types.ts | 11 | rejected | Perd voiceDesign, modelId (config vocale par agent) et les actions wb_draw_code/wb_edit_code -- meme cluster que registry/store.ts et tool-schemas.ts/inline-action-tag.tsx. |
| lib/orchestration/stateless-generate.ts | 16 | rejected | Le diff reintroduit un import depuis l'ancien chemin @/lib/types/slides alors qu'upstream v0.3.0 a extrait ces types dans le package @openmaic/dsl (reellement present et utilise ailleurs dans le worktree). Appliquer romprait la coherence d'architecture avec le reste du code v0.3.0 non touche par cette story. Echantillon verifie manuellement (6 fichiers) : chacun perd aussi une fonctionnalite ou un correctif upstream reel au-dela du seul import (ex. SpotlightOverlay perd domIdPrefix + un correctif Tailwind ; BaseImageElement revient a un pattern de resolution media plus ancien perdant une correction documentee de contamination cross-course ; chartOption.ts perd des verifications defensives ; element-fingerprint.ts perd le cas 'code' (risque de rupture d'exhaustivite de switch). |
| lib/orchestration/tool-schemas.ts | 6 | rejected | Perd les descriptions d'outils wb_draw_code/wb_edit_code -- meme cluster que registry/types.ts et inline-action-tag.tsx. |
| lib/pbl/mcp/issueboard-mcp.ts | 12 | rejected | Renomme 'languageDirective' en 'language' -- lie au meme cluster de fonctionnalite que scene-builder.ts/pipeline-runner.ts (deja rejetes) ; incoherent d'appliquer ici isolement. |
| lib/pdf/constants.ts | 12 | rejected | Retire le provider 'mineru-cloud' entierement -- retrecissement de capacite, meme famille de risque que media/types.ts et lib/pdf/types.ts. |
| lib/pdf/types.ts | 4 | rejected | Retrecit PDFProviderId en retirant 'mineru-cloud' -- coherent avec le rejet de lib/pdf/constants.ts (meme provider). |
| lib/prosemirror/schema/marks.ts | 12 | rejected | SEVERITE ELEVEE : upstream a ajoute une protection anti-injection CSS (validation/echappement du nom de police avant insertion dans un style inline, avec commentaire documentant explicitement le risque). Appliquer main REVERTERAIT ce correctif de securite. |
| lib/server/api-response.ts | 6 | rejected | Perd plusieurs codes d'erreur ajoutes par upstream (PROVIDER_DISABLED, VOXCPM_AUTO_VOICE_REQUIRES_CONTEXT, TOO_MANY_REDIRECTS, RATE_LIMITED) -- risque concret de casser d'autres fichiers (hors perimetre) qui les referenceraient. |
| lib/server/classroom-job-store.ts | 4 | applied | CORRECTION apres re-verification : c'est MAIN qui AJOUTE le champ 'language' (suivi multilingue des jobs), absent d'upstream -- additif, aucune perte, aligne avec l'objectif FR/AR/EN de Qalem. (source=git, aucune derive detectee) |
| lib/store/canvas.ts | 12 | rejected | Perd la fonctionnalite 'pickTarget' (mode timeline 'choisir un element sur le canevas') ajoutee par upstream. |
| lib/store/whiteboard-history.ts | 4 | rejected | Le diff reintroduit un import depuis l'ancien chemin @/lib/types/slides alors qu'upstream v0.3.0 a extrait ces types dans le package @openmaic/dsl (reellement present et utilise ailleurs dans le worktree). Appliquer romprait la coherence d'architecture avec le reste du code v0.3.0 non touche par cette story. Echantillon verifie manuellement (6 fichiers) : chacun perd aussi une fonctionnalite ou un correctif upstream reel au-dela du seul import (ex. SpotlightOverlay perd domIdPrefix + un correctif Tailwind ; BaseImageElement revient a un pattern de resolution media plus ancien perdant une correction documentee de contamination cross-course ; chartOption.ts perd des verifications defensives ; element-fingerprint.ts perd le cas 'code' (risque de rupture d'exhaustivite de switch). |
| lib/types/chat.ts | 19 | rejected | Melange complexe : perd ThinkingConfig, SessionStatus='error', maxTurns/currentTurn (fonctionnalites upstream) ; gagnerait 'requiresApiKey' (utile, cf. use-pbl-chat.ts) ; change aussi l'import de WhiteboardActionRecord vers un chemin obsolete (./types au lieu de ./director-prompt). Remplacement integral disproportionnellement risque pour le present perimetre mecanique. |
| lib/types/edit.ts | 9 | rejected | Le diff reintroduit un import depuis l'ancien chemin @/lib/types/slides alors qu'upstream v0.3.0 a extrait ces types dans le package @openmaic/dsl (reellement present et utilise ailleurs dans le worktree). Appliquer romprait la coherence d'architecture avec le reste du code v0.3.0 non touche par cette story. Echantillon verifie manuellement (6 fichiers) : chacun perd aussi une fonctionnalite ou un correctif upstream reel au-dela du seul import (ex. SpotlightOverlay perd domIdPrefix + un correctif Tailwind ; BaseImageElement revient a un pattern de resolution media plus ancien perdant une correction documentee de contamination cross-course ; chartOption.ts perd des verifications defensives ; element-fingerprint.ts perd le cas 'code' (risque de rupture d'exhaustivite de switch). |
| lib/types/settings.ts | 10 | rejected | Perd le champ serverBaseUrl (override d'URL cote serveur) ajoute par upstream -- risque de casser d'autres fichiers hors perimetre qui le referenceraient. |
| lib/utils/audio-player.ts | 13 | rejected | Le diff reintroduit un import depuis l'ancien chemin @/lib/types/slides alors qu'upstream v0.3.0 a extrait ces types dans le package @openmaic/dsl (reellement present et utilise ailleurs dans le worktree). Appliquer romprait la coherence d'architecture avec le reste du code v0.3.0 non touche par cette story. Echantillon verifie manuellement (6 fichiers) : chacun perd aussi une fonctionnalite ou un correctif upstream reel au-dela du seul import (ex. SpotlightOverlay perd domIdPrefix + un correctif Tailwind ; BaseImageElement revient a un pattern de resolution media plus ancien perdant une correction documentee de contamination cross-course ; chartOption.ts perd des verifications defensives ; element-fingerprint.ts perd le cas 'code' (risque de rupture d'exhaustivite de switch). |
| lib/utils/element-fingerprint.ts | 6 | rejected | Le diff reintroduit un import depuis l'ancien chemin @/lib/types/slides alors qu'upstream v0.3.0 a extrait ces types dans le package @openmaic/dsl (reellement present et utilise ailleurs dans le worktree). Appliquer romprait la coherence d'architecture avec le reste du code v0.3.0 non touche par cette story. Echantillon verifie manuellement (6 fichiers) : chacun perd aussi une fonctionnalite ou un correctif upstream reel au-dela du seul import (ex. SpotlightOverlay perd domIdPrefix + un correctif Tailwind ; BaseImageElement revient a un pattern de resolution media plus ancien perdant une correction documentee de contamination cross-course ; chartOption.ts perd des verifications defensives ; element-fingerprint.ts perd le cas 'code' (risque de rupture d'exhaustivite de switch). |
| lib/utils/element.ts | 4 | rejected | Le diff reintroduit un import depuis l'ancien chemin @/lib/types/slides alors qu'upstream v0.3.0 a extrait ces types dans le package @openmaic/dsl (reellement present et utilise ailleurs dans le worktree). Appliquer romprait la coherence d'architecture avec le reste du code v0.3.0 non touche par cette story. Echantillon verifie manuellement (6 fichiers) : chacun perd aussi une fonctionnalite ou un correctif upstream reel au-dela du seul import (ex. SpotlightOverlay perd domIdPrefix + un correctif Tailwind ; BaseImageElement revient a un pattern de resolution media plus ancien perdant une correction documentee de contamination cross-course ; chartOption.ts perd des verifications defensives ; element-fingerprint.ts perd le cas 'code' (risque de rupture d'exhaustivite de switch). |
| lib/utils/geometry.ts | 4 | rejected | Le diff reintroduit un import depuis l'ancien chemin @/lib/types/slides alors qu'upstream v0.3.0 a extrait ces types dans le package @openmaic/dsl (reellement present et utilise ailleurs dans le worktree). Appliquer romprait la coherence d'architecture avec le reste du code v0.3.0 non touche par cette story. Echantillon verifie manuellement (6 fichiers) : chacun perd aussi une fonctionnalite ou un correctif upstream reel au-dela du seul import (ex. SpotlightOverlay perd domIdPrefix + un correctif Tailwind ; BaseImageElement revient a un pattern de resolution media plus ancien perdant une correction documentee de contamination cross-course ; chartOption.ts perd des verifications defensives ; element-fingerprint.ts perd le cas 'code' (risque de rupture d'exhaustivite de switch). |
| lib/utils/model-config.ts | 15 | rejected | Le diff reintroduit un import depuis l'ancien chemin @/lib/types/slides alors qu'upstream v0.3.0 a extrait ces types dans le package @openmaic/dsl (reellement present et utilise ailleurs dans le worktree). Appliquer romprait la coherence d'architecture avec le reste du code v0.3.0 non touche par cette story. Echantillon verifie manuellement (6 fichiers) : chacun perd aussi une fonctionnalite ou un correctif upstream reel au-dela du seul import (ex. SpotlightOverlay perd domIdPrefix + un correctif Tailwind ; BaseImageElement revient a un pattern de resolution media plus ancien perdant une correction documentee de contamination cross-course ; chartOption.ts perd des verifications defensives ; element-fingerprint.ts perd le cas 'code' (risque de rupture d'exhaustivite de switch). |
| lib/web-search/types.ts | 14 | rejected | Le diff reintroduit un import depuis l'ancien chemin @/lib/types/slides alors qu'upstream v0.3.0 a extrait ces types dans le package @openmaic/dsl (reellement present et utilise ailleurs dans le worktree). Appliquer romprait la coherence d'architecture avec le reste du code v0.3.0 non touche par cette story. Echantillon verifie manuellement (6 fichiers) : chacun perd aussi une fonctionnalite ou un correctif upstream reel au-dela du seul import (ex. SpotlightOverlay perd domIdPrefix + un correctif Tailwind ; BaseImageElement revient a un pattern de resolution media plus ancien perdant une correction documentee de contamination cross-course ; chartOption.ts perd des verifications defensives ; element-fingerprint.ts perd le cas 'code' (risque de rupture d'exhaustivite de switch). |
| packages/pptxgenjs/rollup.config.mjs | 8 | rejected | Modifie la configuration de build du package pptxgenjs dont le postinstall est deja verifie vert par S0-001 -- risque de regression sur une chaine de build deja prouvee, sans benefice identifie. |
| packages/pptxgenjs/src/gen-media.ts | 4 | rejected | Retire les commentaires magiques '/* webpackIgnore: true */' presents dans la version v0.3.0 dont le build (postinstall) est deja verifie vert par S0-001 -- pas de raison de toucher a un etat deja valide. |
| packages/pptxgenjs/src/pptxgen.ts | 4 | rejected | Meme raison que gen-media.ts (meme paire de commentaires webpackIgnore, meme package deja valide par S0-001). |
| playwright.config.ts | 7 | rejected | Perd le flag NEXT_PUBLIC_MAIC_EDITOR_ENABLED necessaire aux e2e de l'Editor Pro mode ajoute par upstream -- perte de couverture e2e reelle, pas cosmetique. |
| pnpm-workspace.yaml | 4 | rejected | main ne declare pas 'packages/@openmaic/*' comme membre du workspace pnpm, alors que packages/@openmaic/{dsl,importer,renderer} existent reellement et sont deja necessaires au postinstall valide vert par S0-001 (pnpm build). Appliquer casserait cette chaine de build deja verifiee. |
| public/logos/openai.svg | 13 | noop | contenu main deja applique au worktree lors d'une execution precedente (source=git) — idempotent, rien a refaire. Motif d'application : Asset SVG pur (fill via <style>/prefers-color-scheme au lieu de fill='#000' fixe) -- ajout d'un support dark-mode au logo, aucun impact build/type. |
| tsconfig.json | 4 | rejected | main exclut 'packages/docs' de tsc alors que ce dossier EXISTE reellement dans le worktree v0.3.0 (app Fumadocs avec son propre toolchain) -- retirer cette exclusion ferait typechecker un sous-projet non concu pour ca, risque concret de regression sur l'acceptance 'npx tsc --noEmit vert'. |
| vitest.config.ts | 3 | rejected | Perd setupFiles:['tests/setup-env.ts'] ajoute par upstream -- ce fichier existe reellement dans le worktree v0.3.0, le retirer changerait le comportement des tests sans necessite identifiee. |

## Patch chirurgical S0-004 (hors script mecanique)

- `app/api/azure-voices/route.ts` : ajout manuel de `requireAuth(req)` (import `@/lib/api/auth`, gain de securite reel absent d'upstream sur cette route) tout en **conservant** le `await validateUrlForSSRF(baseUrl)` deja present (upstream) — main perdait ce `await` sur une fonction pourtant `async` (`lib/server/ssrf-guard.ts:172`), ce qui aurait rendu `ssrfError` toujours une Promise (donc toujours *truthy*) et cassé systematiquement la validation SSRF. Le reste du fichier (destructuration, message de log) est reste celui d'upstream, deja correct.

## Note methodologique importante (corrections apportees en cours de story)

Une premiere lecture rapide des diffs main-vs-upstream a mal interprete le sens
`+`/`-` sur plusieurs fichiers (confondant "ce que main ajoute" et "ce
qu'upstream ajoute"), menant a des rejets initialement motives a tort pour :
`app/api/azure-voices/route.ts`, `app/api/classroom-media/[classroomId]/[...path]/route.ts`,
`app/api/generate-classroom/[jobId]/route.ts` (les 3 routes gagnent en realite
un `requireAuth(...)` absent d'upstream — un gain de securite, pas une perte),
`components/scene-renderers/pbl/use-pbl-chat.ts` (gagne un en-tete
`x-requires-api-key` + une interpolation `.replace()` necessaire aux
traductions Qalem deja portees), `lib/server/classroom-job-store.ts` (gagne un
champ `language` de suivi), et `lib/generation/pipeline-runner.ts` (main
contient en realite ENCORE des messages de statut code en dur en CHINOIS, pas
en anglais comme initialement cru — le rejet reste justifie mais pour la
bonne raison). Toutes les 137 decisions ont ensuite ete RE-VERIFIEES une par
une avec des diffs explicitement labellises UPSTREAM(-)/MAIN(+) avant
finalisation — voir `refork/s0-004-decisions.json` pour le detail motive de
chaque fichier.

## Execution S0-004 (revue manuelle) 2026-07-10T14:43:00

Application semi-automatique des divergences BOTH_DIFFER < 30 lignes, apres REVUE MANUELLE DU CONTENU de chacune (pas seulement du nombre de lignes — voir docstring de `refork/apply_both_differ_small.py` et `refork/s0-004-decisions.json` pour la justification complete).

- Inventaire : `C:\projets\Qalem\refork\inventaire.json`
- Decisions manuelles : `C:\projets\Qalem\refork-v030-wt\refork\s0-004-decisions.json`
- Repo carriere (main) : `C:\projets\Qalem\OpenMAIC`
- Archive upstream v0.3.0 : `C:\projets\Qalem\upstream-v030`
- Worktree cible : `C:\projets\Qalem\refork-v030-wt`
- Total fichiers BOTH_DIFFER < 30 lignes : 137
- Appliques : 0
- Rejetes : 124
- No-op (deja a jour) : 13

### Detail fichier par fichier

| Fichier | Lignes diff | Decision | Motif |
|---|---|---|---|
| .github/ISSUE_TEMPLATE/config.yml | 4 | noop | contenu main deja applique au worktree lors d'une execution precedente (source=git) — idempotent, rien a refaire. Motif d'application : Lien Discord mis a jour -- cosmetique, zero risque technique. |
| .github/workflows/ci.yml | 28 | noop | contenu main deja applique au worktree lors d'une execution precedente (source=git) — idempotent, rien a refaire. Motif d'application : Config CI uniquement (n'affecte aucune commande locale tsc/lint/test/build). Retire un step 'i18n Key Alignment' + 'Unit Tests (importer)' absents de main, ajoute un step Build -- cosmetique, zero risque sur le code source. |
| .prettierignore | 10 | rejected | Meme famille que pnpm-workspace.yaml/tsconfig.json : retire les ignores de packages/@openmaic/*/dist, importer/src1 (vendor) et packages/docs sans les remplacer par un ignore plus large -- regression par rapport a la structure v0.3.0 reelle du worktree. |
| CONTRIBUTING.md | 11 | rejected | FR-3 / convention soft-fork Qalem : README/CONTRIBUTING/CHANGELOG herites de l'upstream ne sont JAMAIS modifies (regle absolue, independante du seuil de lignes). |
| app/api/azure-voices/route.ts | 15 | rejected | MELANGE (rejete du remplacement mecanique, traite CHIRURGICALEMENT par edition manuelle directe hors de ce script) : main AJOUTE 'requireAuth(...)' (gain de securite reel, absent d'upstream) MAIS retire aussi le 'await' sur 'validateUrlForSSRF(baseUrl)' qui est une fonction ASYNC (verifie : lib/server/ssrf-guard.ts:172 'export async function validateUrlForSSRF') -- sans await, 'ssrfError' serait toujours une Promise (donc toujours truthy), cassant systematiquement la validation SSRF et donc la fonctionnalite entiere. Patch chirurgical applique manuellement : ajout de requireAuth, conservation de l'await existant. |
| app/api/classroom-media/[classroomId]/[...path]/route.ts | 13 | noop | contenu main deja applique au worktree lors d'une execution precedente (source=git) — idempotent, rien a refaire. Motif d'application : CORRECTION apres re-verification rigoureuse du sens du diff : c'est MAIN qui AJOUTE 'requireAuth(...)' (authentification), ABSENTE d'upstream sur cette route -- gain de securite reel. Perte associee : logging d'erreur detaille (classroomId/path) -- acceptable, aucune fonctionnalite utilisateur perdue. |
| app/api/generate-classroom/[jobId]/route.ts | 12 | noop | contenu main deja applique au worktree lors d'une execution precedente (source=git) — idempotent, rien a refaire. Motif d'application : CORRECTION apres re-verification rigoureuse du sens du diff : c'est MAIN qui AJOUTE 'requireAuth(...)', ABSENTE d'upstream sur cette route -- gain de securite reel. Perte associee : logging d'erreur detaille (jobId) -- acceptable. |
| app/api/health/route.ts | 4 | rejected | Regression mineure de precision (verifie Object.keys().length au lieu de values().some(!disabled)) -- pas de gain identifie, rejet par coherence de politique (main en retard, aucune necessite Qalem). |
| app/api/server-providers/route.ts | 6 | rejected | Perd le champ 'generation.parallelSceneConcurrency' ajoute par upstream -- risque de casser un type client qui l'attendrait. |
| app/generation-preview/types.ts | 23 | rejected | Perd previewPhase, languageDirective, courseTitle, taskEngineMode (cluster de fonctionnalites upstream plus recentes) et reordonne le tableau d'etapes -- perte de structure, pas une simple divergence textuelle. |
| components/agent/agent-config-panel.tsx | 8 | rejected | Perd la fonctionnalite PublishAgentDialog (publication d'agent) ajoutee par upstream. |
| components/ai-elements/prompt-input.tsx | 7 | rejected | RISQUE CONCRET DE CASSER TSC : le commentaire upstream explique que le '?' optionnel sur Window.SpeechRecognition doit rester identique aux modificateurs de l'augmentation globale de @assistant-ui/core (declaration merging TypeScript exige des modificateurs identiques). Le retirer peut provoquer un conflit de declaration globale. |
| components/canvas/canvas-area.tsx | 21 | rejected | Perd la fonctionnalite ClassroomCompletePageConnected / isCourseComplete (ecran de fin de cours) ajoutee par upstream. |
| components/chat/inline-action-tag.tsx | 6 | rejected | Perd les icones/labels wb_draw_code et wb_edit_code -- meme cluster que registry/types.ts et tool-schemas.ts. |
| components/chat/lecture-notes-view.tsx | 4 | noop | contenu main deja applique au worktree lors d'une execution precedente (source=git) — idempotent, rien a refaire. Motif d'application : Meme raison que whiteboard-history.tsx : interpolation par .replace('{n}',...) necessaire pour les traductions Qalem a accolade simple. |
| components/chat/session-list.tsx | 6 | rejected | Perd le cas SessionStatus='error' (icone AlertCircle) -- coherent avec le rejet de lib/types/chat.ts (meme enum). |
| components/scene-renderers/pbl/use-pbl-chat.ts | 24 | noop | contenu main deja applique au worktree lors d'une execution precedente (source=git) — idempotent, rien a refaire. Motif d'application : CORRECTION apres re-verification rigoureuse du sens du diff (voir note methodologique) : main AJOUTE l'en-tete 'x-requires-api-key' (absent d'upstream, additif) ET bascule sur t(key).replace('{title}'/'{questions}',...) qui est NECESSAIRE pour utiliser les traductions Qalem deja portees de welcomeMessage/issueCompleteMessage (accolade simple '{title}'/'{questions}', verifie dans lib/i18n/stage.ts) -- sans ce changement ces traductions FR/AR/EN entierement redigees ne seraient jamais affichees. Aucune fonctionnalite upstream perdue ici. |
| components/settings/provider-list.tsx | 8 | rejected | Perd la logique MONO_LOGO_PROVIDERS (inversion dark-mode des logos monochromes) ajoutee par upstream. |
| components/slide-renderer/Editor/Canvas/EditableElement.tsx | 4 | rejected | Le diff reintroduit un import depuis l'ancien chemin @/lib/types/slides alors qu'upstream v0.3.0 a extrait ces types dans le package @openmaic/dsl (reellement present et utilise ailleurs dans le worktree). Appliquer romprait la coherence d'architecture avec le reste du code v0.3.0 non touche par cette story. Echantillon verifie manuellement (6 fichiers) : chacun perd aussi une fonctionnalite ou un correctif upstream reel au-dela du seul import (ex. SpotlightOverlay perd domIdPrefix + un correctif Tailwind ; BaseImageElement revient a un pattern de resolution media plus ancien perdant une correction documentee de contamination cross-course ; chartOption.ts perd des verifications defensives ; element-fingerprint.ts perd le cas 'code' (risque de rupture d'exhaustivite de switch). |
| components/slide-renderer/Editor/Canvas/ElementCreateSelection.tsx | 10 | rejected | Perd la gestion dediee de creation d'un element texte (comportement de taille par defaut adapte) ajoutee par upstream. |
| components/slide-renderer/Editor/Canvas/GridLines.tsx | 4 | rejected | Le diff reintroduit un import depuis l'ancien chemin @/lib/types/slides alors qu'upstream v0.3.0 a extrait ces types dans le package @openmaic/dsl (reellement present et utilise ailleurs dans le worktree). Appliquer romprait la coherence d'architecture avec le reste du code v0.3.0 non touche par cette story. Echantillon verifie manuellement (6 fichiers) : chacun perd aussi une fonctionnalite ou un correctif upstream reel au-dela du seul import (ex. SpotlightOverlay perd domIdPrefix + un correctif Tailwind ; BaseImageElement revient a un pattern de resolution media plus ancien perdant une correction documentee de contamination cross-course ; chartOption.ts perd des verifications defensives ; element-fingerprint.ts perd le cas 'code' (risque de rupture d'exhaustivite de switch). |
| components/slide-renderer/Editor/Canvas/Operate/CommonElementOperate.tsx | 4 | rejected | Le diff reintroduit un import depuis l'ancien chemin @/lib/types/slides alors qu'upstream v0.3.0 a extrait ces types dans le package @openmaic/dsl (reellement present et utilise ailleurs dans le worktree). Appliquer romprait la coherence d'architecture avec le reste du code v0.3.0 non touche par cette story. Echantillon verifie manuellement (6 fichiers) : chacun perd aussi une fonctionnalite ou un correctif upstream reel au-dela du seul import (ex. SpotlightOverlay perd domIdPrefix + un correctif Tailwind ; BaseImageElement revient a un pattern de resolution media plus ancien perdant une correction documentee de contamination cross-course ; chartOption.ts perd des verifications defensives ; element-fingerprint.ts perd le cas 'code' (risque de rupture d'exhaustivite de switch). |
| components/slide-renderer/Editor/Canvas/Operate/ImageElementOperate.tsx | 4 | rejected | Le diff reintroduit un import depuis l'ancien chemin @/lib/types/slides alors qu'upstream v0.3.0 a extrait ces types dans le package @openmaic/dsl (reellement present et utilise ailleurs dans le worktree). Appliquer romprait la coherence d'architecture avec le reste du code v0.3.0 non touche par cette story. Echantillon verifie manuellement (6 fichiers) : chacun perd aussi une fonctionnalite ou un correctif upstream reel au-dela du seul import (ex. SpotlightOverlay perd domIdPrefix + un correctif Tailwind ; BaseImageElement revient a un pattern de resolution media plus ancien perdant une correction documentee de contamination cross-course ; chartOption.ts perd des verifications defensives ; element-fingerprint.ts perd le cas 'code' (risque de rupture d'exhaustivite de switch). |
| components/slide-renderer/Editor/Canvas/Operate/LineElementOperate.tsx | 4 | rejected | Le diff reintroduit un import depuis l'ancien chemin @/lib/types/slides alors qu'upstream v0.3.0 a extrait ces types dans le package @openmaic/dsl (reellement present et utilise ailleurs dans le worktree). Appliquer romprait la coherence d'architecture avec le reste du code v0.3.0 non touche par cette story. Echantillon verifie manuellement (6 fichiers) : chacun perd aussi une fonctionnalite ou un correctif upstream reel au-dela du seul import (ex. SpotlightOverlay perd domIdPrefix + un correctif Tailwind ; BaseImageElement revient a un pattern de resolution media plus ancien perdant une correction documentee de contamination cross-course ; chartOption.ts perd des verifications defensives ; element-fingerprint.ts perd le cas 'code' (risque de rupture d'exhaustivite de switch). |
| components/slide-renderer/Editor/Canvas/Operate/MultiSelectOperate.tsx | 4 | rejected | Le diff reintroduit un import depuis l'ancien chemin @/lib/types/slides alors qu'upstream v0.3.0 a extrait ces types dans le package @openmaic/dsl (reellement present et utilise ailleurs dans le worktree). Appliquer romprait la coherence d'architecture avec le reste du code v0.3.0 non touche par cette story. Echantillon verifie manuellement (6 fichiers) : chacun perd aussi une fonctionnalite ou un correctif upstream reel au-dela du seul import (ex. SpotlightOverlay perd domIdPrefix + un correctif Tailwind ; BaseImageElement revient a un pattern de resolution media plus ancien perdant une correction documentee de contamination cross-course ; chartOption.ts perd des verifications defensives ; element-fingerprint.ts perd le cas 'code' (risque de rupture d'exhaustivite de switch). |
| components/slide-renderer/Editor/Canvas/Operate/ShapeElementOperate.tsx | 4 | rejected | Le diff reintroduit un import depuis l'ancien chemin @/lib/types/slides alors qu'upstream v0.3.0 a extrait ces types dans le package @openmaic/dsl (reellement present et utilise ailleurs dans le worktree). Appliquer romprait la coherence d'architecture avec le reste du code v0.3.0 non touche par cette story. Echantillon verifie manuellement (6 fichiers) : chacun perd aussi une fonctionnalite ou un correctif upstream reel au-dela du seul import (ex. SpotlightOverlay perd domIdPrefix + un correctif Tailwind ; BaseImageElement revient a un pattern de resolution media plus ancien perdant une correction documentee de contamination cross-course ; chartOption.ts perd des verifications defensives ; element-fingerprint.ts perd le cas 'code' (risque de rupture d'exhaustivite de switch). |
| components/slide-renderer/Editor/Canvas/Operate/TableElementOperate.tsx | 4 | rejected | Le diff reintroduit un import depuis l'ancien chemin @/lib/types/slides alors qu'upstream v0.3.0 a extrait ces types dans le package @openmaic/dsl (reellement present et utilise ailleurs dans le worktree). Appliquer romprait la coherence d'architecture avec le reste du code v0.3.0 non touche par cette story. Echantillon verifie manuellement (6 fichiers) : chacun perd aussi une fonctionnalite ou un correctif upstream reel au-dela du seul import (ex. SpotlightOverlay perd domIdPrefix + un correctif Tailwind ; BaseImageElement revient a un pattern de resolution media plus ancien perdant une correction documentee de contamination cross-course ; chartOption.ts perd des verifications defensives ; element-fingerprint.ts perd le cas 'code' (risque de rupture d'exhaustivite de switch). |
| components/slide-renderer/Editor/Canvas/Operate/index.tsx | 4 | rejected | Le diff reintroduit un import depuis l'ancien chemin @/lib/types/slides alors qu'upstream v0.3.0 a extrait ces types dans le package @openmaic/dsl (reellement present et utilise ailleurs dans le worktree). Appliquer romprait la coherence d'architecture avec le reste du code v0.3.0 non touche par cette story. Echantillon verifie manuellement (6 fichiers) : chacun perd aussi une fonctionnalite ou un correctif upstream reel au-dela du seul import (ex. SpotlightOverlay perd domIdPrefix + un correctif Tailwind ; BaseImageElement revient a un pattern de resolution media plus ancien perdant une correction documentee de contamination cross-course ; chartOption.ts perd des verifications defensives ; element-fingerprint.ts perd le cas 'code' (risque de rupture d'exhaustivite de switch). |
| components/slide-renderer/Editor/Canvas/Ruler.tsx | 4 | rejected | Le diff reintroduit un import depuis l'ancien chemin @/lib/types/slides alors qu'upstream v0.3.0 a extrait ces types dans le package @openmaic/dsl (reellement present et utilise ailleurs dans le worktree). Appliquer romprait la coherence d'architecture avec le reste du code v0.3.0 non touche par cette story. Echantillon verifie manuellement (6 fichiers) : chacun perd aussi une fonctionnalite ou un correctif upstream reel au-dela du seul import (ex. SpotlightOverlay perd domIdPrefix + un correctif Tailwind ; BaseImageElement revient a un pattern de resolution media plus ancien perdant une correction documentee de contamination cross-course ; chartOption.ts perd des verifications defensives ; element-fingerprint.ts perd le cas 'code' (risque de rupture d'exhaustivite de switch). |
| components/slide-renderer/Editor/Canvas/ShapeCreateCanvas.tsx | 4 | rejected | Le diff reintroduit un import depuis l'ancien chemin @/lib/types/slides alors qu'upstream v0.3.0 a extrait ces types dans le package @openmaic/dsl (reellement present et utilise ailleurs dans le worktree). Appliquer romprait la coherence d'architecture avec le reste du code v0.3.0 non touche par cette story. Echantillon verifie manuellement (6 fichiers) : chacun perd aussi une fonctionnalite ou un correctif upstream reel au-dela du seul import (ex. SpotlightOverlay perd domIdPrefix + un correctif Tailwind ; BaseImageElement revient a un pattern de resolution media plus ancien perdant une correction documentee de contamination cross-course ; chartOption.ts perd des verifications defensives ; element-fingerprint.ts perd le cas 'code' (risque de rupture d'exhaustivite de switch). |
| components/slide-renderer/Editor/Canvas/ViewportBackground.tsx | 4 | rejected | Le diff reintroduit un import depuis l'ancien chemin @/lib/types/slides alors qu'upstream v0.3.0 a extrait ces types dans le package @openmaic/dsl (reellement present et utilise ailleurs dans le worktree). Appliquer romprait la coherence d'architecture avec le reste du code v0.3.0 non touche par cette story. Echantillon verifie manuellement (6 fichiers) : chacun perd aussi une fonctionnalite ou un correctif upstream reel au-dela du seul import (ex. SpotlightOverlay perd domIdPrefix + un correctif Tailwind ; BaseImageElement revient a un pattern de resolution media plus ancien perdant une correction documentee de contamination cross-course ; chartOption.ts perd des verifications defensives ; element-fingerprint.ts perd le cas 'code' (risque de rupture d'exhaustivite de switch). |
| components/slide-renderer/Editor/Canvas/hooks/useDragElement.ts | 4 | rejected | Le diff reintroduit un import depuis l'ancien chemin @/lib/types/slides alors qu'upstream v0.3.0 a extrait ces types dans le package @openmaic/dsl (reellement present et utilise ailleurs dans le worktree). Appliquer romprait la coherence d'architecture avec le reste du code v0.3.0 non touche par cette story. Echantillon verifie manuellement (6 fichiers) : chacun perd aussi une fonctionnalite ou un correctif upstream reel au-dela du seul import (ex. SpotlightOverlay perd domIdPrefix + un correctif Tailwind ; BaseImageElement revient a un pattern de resolution media plus ancien perdant une correction documentee de contamination cross-course ; chartOption.ts perd des verifications defensives ; element-fingerprint.ts perd le cas 'code' (risque de rupture d'exhaustivite de switch). |
| components/slide-renderer/Editor/Canvas/hooks/useDragLineElement.ts | 4 | rejected | Le diff reintroduit un import depuis l'ancien chemin @/lib/types/slides alors qu'upstream v0.3.0 a extrait ces types dans le package @openmaic/dsl (reellement present et utilise ailleurs dans le worktree). Appliquer romprait la coherence d'architecture avec le reste du code v0.3.0 non touche par cette story. Echantillon verifie manuellement (6 fichiers) : chacun perd aussi une fonctionnalite ou un correctif upstream reel au-dela du seul import (ex. SpotlightOverlay perd domIdPrefix + un correctif Tailwind ; BaseImageElement revient a un pattern de resolution media plus ancien perdant une correction documentee de contamination cross-course ; chartOption.ts perd des verifications defensives ; element-fingerprint.ts perd le cas 'code' (risque de rupture d'exhaustivite de switch). |
| components/slide-renderer/Editor/Canvas/hooks/useMouseSelection.ts | 4 | rejected | Le diff reintroduit un import depuis l'ancien chemin @/lib/types/slides alors qu'upstream v0.3.0 a extrait ces types dans le package @openmaic/dsl (reellement present et utilise ailleurs dans le worktree). Appliquer romprait la coherence d'architecture avec le reste du code v0.3.0 non touche par cette story. Echantillon verifie manuellement (6 fichiers) : chacun perd aussi une fonctionnalite ou un correctif upstream reel au-dela du seul import (ex. SpotlightOverlay perd domIdPrefix + un correctif Tailwind ; BaseImageElement revient a un pattern de resolution media plus ancien perdant une correction documentee de contamination cross-course ; chartOption.ts perd des verifications defensives ; element-fingerprint.ts perd le cas 'code' (risque de rupture d'exhaustivite de switch). |
| components/slide-renderer/Editor/Canvas/hooks/useMoveShapeKeypoint.ts | 4 | rejected | Le diff reintroduit un import depuis l'ancien chemin @/lib/types/slides alors qu'upstream v0.3.0 a extrait ces types dans le package @openmaic/dsl (reellement present et utilise ailleurs dans le worktree). Appliquer romprait la coherence d'architecture avec le reste du code v0.3.0 non touche par cette story. Echantillon verifie manuellement (6 fichiers) : chacun perd aussi une fonctionnalite ou un correctif upstream reel au-dela du seul import (ex. SpotlightOverlay perd domIdPrefix + un correctif Tailwind ; BaseImageElement revient a un pattern de resolution media plus ancien perdant une correction documentee de contamination cross-course ; chartOption.ts perd des verifications defensives ; element-fingerprint.ts perd le cas 'code' (risque de rupture d'exhaustivite de switch). |
| components/slide-renderer/Editor/Canvas/hooks/useRotateElement.ts | 4 | rejected | Le diff reintroduit un import depuis l'ancien chemin @/lib/types/slides alors qu'upstream v0.3.0 a extrait ces types dans le package @openmaic/dsl (reellement present et utilise ailleurs dans le worktree). Appliquer romprait la coherence d'architecture avec le reste du code v0.3.0 non touche par cette story. Echantillon verifie manuellement (6 fichiers) : chacun perd aussi une fonctionnalite ou un correctif upstream reel au-dela du seul import (ex. SpotlightOverlay perd domIdPrefix + un correctif Tailwind ; BaseImageElement revient a un pattern de resolution media plus ancien perdant une correction documentee de contamination cross-course ; chartOption.ts perd des verifications defensives ; element-fingerprint.ts perd le cas 'code' (risque de rupture d'exhaustivite de switch). |
| components/slide-renderer/Editor/Canvas/hooks/useScaleElement.ts | 9 | rejected | Le diff reintroduit un import depuis l'ancien chemin @/lib/types/slides alors qu'upstream v0.3.0 a extrait ces types dans le package @openmaic/dsl (reellement present et utilise ailleurs dans le worktree). Appliquer romprait la coherence d'architecture avec le reste du code v0.3.0 non touche par cette story. Echantillon verifie manuellement (6 fichiers) : chacun perd aussi une fonctionnalite ou un correctif upstream reel au-dela du seul import (ex. SpotlightOverlay perd domIdPrefix + un correctif Tailwind ; BaseImageElement revient a un pattern de resolution media plus ancien perdant une correction documentee de contamination cross-course ; chartOption.ts perd des verifications defensives ; element-fingerprint.ts perd le cas 'code' (risque de rupture d'exhaustivite de switch). |
| components/slide-renderer/Editor/Canvas/hooks/useSelectElement.ts | 4 | rejected | Le diff reintroduit un import depuis l'ancien chemin @/lib/types/slides alors qu'upstream v0.3.0 a extrait ces types dans le package @openmaic/dsl (reellement present et utilise ailleurs dans le worktree). Appliquer romprait la coherence d'architecture avec le reste du code v0.3.0 non touche par cette story. Echantillon verifie manuellement (6 fichiers) : chacun perd aussi une fonctionnalite ou un correctif upstream reel au-dela du seul import (ex. SpotlightOverlay perd domIdPrefix + un correctif Tailwind ; BaseImageElement revient a un pattern de resolution media plus ancien perdant une correction documentee de contamination cross-course ; chartOption.ts perd des verifications defensives ; element-fingerprint.ts perd le cas 'code' (risque de rupture d'exhaustivite de switch). |
| components/slide-renderer/Editor/Canvas/index.tsx | 4 | rejected | Le diff reintroduit un import depuis l'ancien chemin @/lib/types/slides alors qu'upstream v0.3.0 a extrait ces types dans le package @openmaic/dsl (reellement present et utilise ailleurs dans le worktree). Appliquer romprait la coherence d'architecture avec le reste du code v0.3.0 non touche par cette story. Echantillon verifie manuellement (6 fichiers) : chacun perd aussi une fonctionnalite ou un correctif upstream reel au-dela du seul import (ex. SpotlightOverlay perd domIdPrefix + un correctif Tailwind ; BaseImageElement revient a un pattern de resolution media plus ancien perdant une correction documentee de contamination cross-course ; chartOption.ts perd des verifications defensives ; element-fingerprint.ts perd le cas 'code' (risque de rupture d'exhaustivite de switch). |
| components/slide-renderer/Editor/HighlightOverlay.tsx | 4 | rejected | Le diff reintroduit un import depuis l'ancien chemin @/lib/types/slides alors qu'upstream v0.3.0 a extrait ces types dans le package @openmaic/dsl (reellement present et utilise ailleurs dans le worktree). Appliquer romprait la coherence d'architecture avec le reste du code v0.3.0 non touche par cette story. Echantillon verifie manuellement (6 fichiers) : chacun perd aussi une fonctionnalite ou un correctif upstream reel au-dela du seul import (ex. SpotlightOverlay perd domIdPrefix + un correctif Tailwind ; BaseImageElement revient a un pattern de resolution media plus ancien perdant une correction documentee de contamination cross-course ; chartOption.ts perd des verifications defensives ; element-fingerprint.ts perd le cas 'code' (risque de rupture d'exhaustivite de switch). |
| components/slide-renderer/Editor/ScreenCanvas.tsx | 4 | rejected | Le diff reintroduit un import depuis l'ancien chemin @/lib/types/slides alors qu'upstream v0.3.0 a extrait ces types dans le package @openmaic/dsl (reellement present et utilise ailleurs dans le worktree). Appliquer romprait la coherence d'architecture avec le reste du code v0.3.0 non touche par cette story. Echantillon verifie manuellement (6 fichiers) : chacun perd aussi une fonctionnalite ou un correctif upstream reel au-dela du seul import (ex. SpotlightOverlay perd domIdPrefix + un correctif Tailwind ; BaseImageElement revient a un pattern de resolution media plus ancien perdant une correction documentee de contamination cross-course ; chartOption.ts perd des verifications defensives ; element-fingerprint.ts perd le cas 'code' (risque de rupture d'exhaustivite de switch). |
| components/slide-renderer/Editor/ScreenElement.tsx | 6 | rejected | Le diff reintroduit un import depuis l'ancien chemin @/lib/types/slides alors qu'upstream v0.3.0 a extrait ces types dans le package @openmaic/dsl (reellement present et utilise ailleurs dans le worktree). Appliquer romprait la coherence d'architecture avec le reste du code v0.3.0 non touche par cette story. Echantillon verifie manuellement (6 fichiers) : chacun perd aussi une fonctionnalite ou un correctif upstream reel au-dela du seul import (ex. SpotlightOverlay perd domIdPrefix + un correctif Tailwind ; BaseImageElement revient a un pattern de resolution media plus ancien perdant une correction documentee de contamination cross-course ; chartOption.ts perd des verifications defensives ; element-fingerprint.ts perd le cas 'code' (risque de rupture d'exhaustivite de switch). |
| components/slide-renderer/Editor/SpotlightOverlay.tsx | 28 | rejected | Le diff reintroduit un import depuis l'ancien chemin @/lib/types/slides alors qu'upstream v0.3.0 a extrait ces types dans le package @openmaic/dsl (reellement present et utilise ailleurs dans le worktree). Appliquer romprait la coherence d'architecture avec le reste du code v0.3.0 non touche par cette story. Echantillon verifie manuellement (6 fichiers) : chacun perd aussi une fonctionnalite ou un correctif upstream reel au-dela du seul import (ex. SpotlightOverlay perd domIdPrefix + un correctif Tailwind ; BaseImageElement revient a un pattern de resolution media plus ancien perdant une correction documentee de contamination cross-course ; chartOption.ts perd des verifications defensives ; element-fingerprint.ts perd le cas 'code' (risque de rupture d'exhaustivite de switch). |
| components/slide-renderer/components/element/ChartElement/BaseChartElement.tsx | 4 | rejected | Le diff reintroduit un import depuis l'ancien chemin @/lib/types/slides alors qu'upstream v0.3.0 a extrait ces types dans le package @openmaic/dsl (reellement present et utilise ailleurs dans le worktree). Appliquer romprait la coherence d'architecture avec le reste du code v0.3.0 non touche par cette story. Echantillon verifie manuellement (6 fichiers) : chacun perd aussi une fonctionnalite ou un correctif upstream reel au-dela du seul import (ex. SpotlightOverlay perd domIdPrefix + un correctif Tailwind ; BaseImageElement revient a un pattern de resolution media plus ancien perdant une correction documentee de contamination cross-course ; chartOption.ts perd des verifications defensives ; element-fingerprint.ts perd le cas 'code' (risque de rupture d'exhaustivite de switch). |
| components/slide-renderer/components/element/ChartElement/Chart.tsx | 4 | rejected | Le diff reintroduit un import depuis l'ancien chemin @/lib/types/slides alors qu'upstream v0.3.0 a extrait ces types dans le package @openmaic/dsl (reellement present et utilise ailleurs dans le worktree). Appliquer romprait la coherence d'architecture avec le reste du code v0.3.0 non touche par cette story. Echantillon verifie manuellement (6 fichiers) : chacun perd aussi une fonctionnalite ou un correctif upstream reel au-dela du seul import (ex. SpotlightOverlay perd domIdPrefix + un correctif Tailwind ; BaseImageElement revient a un pattern de resolution media plus ancien perdant une correction documentee de contamination cross-course ; chartOption.ts perd des verifications defensives ; element-fingerprint.ts perd le cas 'code' (risque de rupture d'exhaustivite de switch). |
| components/slide-renderer/components/element/ChartElement/chartOption.ts | 25 | rejected | Le diff reintroduit un import depuis l'ancien chemin @/lib/types/slides alors qu'upstream v0.3.0 a extrait ces types dans le package @openmaic/dsl (reellement present et utilise ailleurs dans le worktree). Appliquer romprait la coherence d'architecture avec le reste du code v0.3.0 non touche par cette story. Echantillon verifie manuellement (6 fichiers) : chacun perd aussi une fonctionnalite ou un correctif upstream reel au-dela du seul import (ex. SpotlightOverlay perd domIdPrefix + un correctif Tailwind ; BaseImageElement revient a un pattern de resolution media plus ancien perdant une correction documentee de contamination cross-course ; chartOption.ts perd des verifications defensives ; element-fingerprint.ts perd le cas 'code' (risque de rupture d'exhaustivite de switch). |
| components/slide-renderer/components/element/ChartElement/index.tsx | 4 | rejected | Le diff reintroduit un import depuis l'ancien chemin @/lib/types/slides alors qu'upstream v0.3.0 a extrait ces types dans le package @openmaic/dsl (reellement present et utilise ailleurs dans le worktree). Appliquer romprait la coherence d'architecture avec le reste du code v0.3.0 non touche par cette story. Echantillon verifie manuellement (6 fichiers) : chacun perd aussi une fonctionnalite ou un correctif upstream reel au-dela du seul import (ex. SpotlightOverlay perd domIdPrefix + un correctif Tailwind ; BaseImageElement revient a un pattern de resolution media plus ancien perdant une correction documentee de contamination cross-course ; chartOption.ts perd des verifications defensives ; element-fingerprint.ts perd le cas 'code' (risque de rupture d'exhaustivite de switch). |
| components/slide-renderer/components/element/ElementOutline.tsx | 4 | rejected | Le diff reintroduit un import depuis l'ancien chemin @/lib/types/slides alors qu'upstream v0.3.0 a extrait ces types dans le package @openmaic/dsl (reellement present et utilise ailleurs dans le worktree). Appliquer romprait la coherence d'architecture avec le reste du code v0.3.0 non touche par cette story. Echantillon verifie manuellement (6 fichiers) : chacun perd aussi une fonctionnalite ou un correctif upstream reel au-dela du seul import (ex. SpotlightOverlay perd domIdPrefix + un correctif Tailwind ; BaseImageElement revient a un pattern de resolution media plus ancien perdant une correction documentee de contamination cross-course ; chartOption.ts perd des verifications defensives ; element-fingerprint.ts perd le cas 'code' (risque de rupture d'exhaustivite de switch). |
| components/slide-renderer/components/element/ImageElement/BaseImageElement.tsx | 26 | rejected | Le diff reintroduit un import depuis l'ancien chemin @/lib/types/slides alors qu'upstream v0.3.0 a extrait ces types dans le package @openmaic/dsl (reellement present et utilise ailleurs dans le worktree). Appliquer romprait la coherence d'architecture avec le reste du code v0.3.0 non touche par cette story. Echantillon verifie manuellement (6 fichiers) : chacun perd aussi une fonctionnalite ou un correctif upstream reel au-dela du seul import (ex. SpotlightOverlay perd domIdPrefix + un correctif Tailwind ; BaseImageElement revient a un pattern de resolution media plus ancien perdant une correction documentee de contamination cross-course ; chartOption.ts perd des verifications defensives ; element-fingerprint.ts perd le cas 'code' (risque de rupture d'exhaustivite de switch). |
| components/slide-renderer/components/element/ImageElement/ImageClipHandler.tsx | 4 | rejected | Le diff reintroduit un import depuis l'ancien chemin @/lib/types/slides alors qu'upstream v0.3.0 a extrait ces types dans le package @openmaic/dsl (reellement present et utilise ailleurs dans le worktree). Appliquer romprait la coherence d'architecture avec le reste du code v0.3.0 non touche par cette story. Echantillon verifie manuellement (6 fichiers) : chacun perd aussi une fonctionnalite ou un correctif upstream reel au-dela du seul import (ex. SpotlightOverlay perd domIdPrefix + un correctif Tailwind ; BaseImageElement revient a un pattern de resolution media plus ancien perdant une correction documentee de contamination cross-course ; chartOption.ts perd des verifications defensives ; element-fingerprint.ts perd le cas 'code' (risque de rupture d'exhaustivite de switch). |
| components/slide-renderer/components/element/ImageElement/ImageOutline/image-ellipse-outline.tsx | 4 | rejected | Le diff reintroduit un import depuis l'ancien chemin @/lib/types/slides alors qu'upstream v0.3.0 a extrait ces types dans le package @openmaic/dsl (reellement present et utilise ailleurs dans le worktree). Appliquer romprait la coherence d'architecture avec le reste du code v0.3.0 non touche par cette story. Echantillon verifie manuellement (6 fichiers) : chacun perd aussi une fonctionnalite ou un correctif upstream reel au-dela du seul import (ex. SpotlightOverlay perd domIdPrefix + un correctif Tailwind ; BaseImageElement revient a un pattern de resolution media plus ancien perdant une correction documentee de contamination cross-course ; chartOption.ts perd des verifications defensives ; element-fingerprint.ts perd le cas 'code' (risque de rupture d'exhaustivite de switch). |
| components/slide-renderer/components/element/ImageElement/ImageOutline/image-polygon-outline.tsx | 4 | rejected | Le diff reintroduit un import depuis l'ancien chemin @/lib/types/slides alors qu'upstream v0.3.0 a extrait ces types dans le package @openmaic/dsl (reellement present et utilise ailleurs dans le worktree). Appliquer romprait la coherence d'architecture avec le reste du code v0.3.0 non touche par cette story. Echantillon verifie manuellement (6 fichiers) : chacun perd aussi une fonctionnalite ou un correctif upstream reel au-dela du seul import (ex. SpotlightOverlay perd domIdPrefix + un correctif Tailwind ; BaseImageElement revient a un pattern de resolution media plus ancien perdant une correction documentee de contamination cross-course ; chartOption.ts perd des verifications defensives ; element-fingerprint.ts perd le cas 'code' (risque de rupture d'exhaustivite de switch). |
| components/slide-renderer/components/element/ImageElement/ImageOutline/image-rect-outline.tsx | 4 | rejected | Le diff reintroduit un import depuis l'ancien chemin @/lib/types/slides alors qu'upstream v0.3.0 a extrait ces types dans le package @openmaic/dsl (reellement present et utilise ailleurs dans le worktree). Appliquer romprait la coherence d'architecture avec le reste du code v0.3.0 non touche par cette story. Echantillon verifie manuellement (6 fichiers) : chacun perd aussi une fonctionnalite ou un correctif upstream reel au-dela du seul import (ex. SpotlightOverlay perd domIdPrefix + un correctif Tailwind ; BaseImageElement revient a un pattern de resolution media plus ancien perdant une correction documentee de contamination cross-course ; chartOption.ts perd des verifications defensives ; element-fingerprint.ts perd le cas 'code' (risque de rupture d'exhaustivite de switch). |
| components/slide-renderer/components/element/ImageElement/ImageOutline/index.tsx | 4 | rejected | Le diff reintroduit un import depuis l'ancien chemin @/lib/types/slides alors qu'upstream v0.3.0 a extrait ces types dans le package @openmaic/dsl (reellement present et utilise ailleurs dans le worktree). Appliquer romprait la coherence d'architecture avec le reste du code v0.3.0 non touche par cette story. Echantillon verifie manuellement (6 fichiers) : chacun perd aussi une fonctionnalite ou un correctif upstream reel au-dela du seul import (ex. SpotlightOverlay perd domIdPrefix + un correctif Tailwind ; BaseImageElement revient a un pattern de resolution media plus ancien perdant une correction documentee de contamination cross-course ; chartOption.ts perd des verifications defensives ; element-fingerprint.ts perd le cas 'code' (risque de rupture d'exhaustivite de switch). |
| components/slide-renderer/components/element/ImageElement/index.tsx | 15 | rejected | Le diff reintroduit un import depuis l'ancien chemin @/lib/types/slides alors qu'upstream v0.3.0 a extrait ces types dans le package @openmaic/dsl (reellement present et utilise ailleurs dans le worktree). Appliquer romprait la coherence d'architecture avec le reste du code v0.3.0 non touche par cette story. Echantillon verifie manuellement (6 fichiers) : chacun perd aussi une fonctionnalite ou un correctif upstream reel au-dela du seul import (ex. SpotlightOverlay perd domIdPrefix + un correctif Tailwind ; BaseImageElement revient a un pattern de resolution media plus ancien perdant une correction documentee de contamination cross-course ; chartOption.ts perd des verifications defensives ; element-fingerprint.ts perd le cas 'code' (risque de rupture d'exhaustivite de switch). |
| components/slide-renderer/components/element/ImageElement/useClipImage.ts | 4 | rejected | Le diff reintroduit un import depuis l'ancien chemin @/lib/types/slides alors qu'upstream v0.3.0 a extrait ces types dans le package @openmaic/dsl (reellement present et utilise ailleurs dans le worktree). Appliquer romprait la coherence d'architecture avec le reste du code v0.3.0 non touche par cette story. Echantillon verifie manuellement (6 fichiers) : chacun perd aussi une fonctionnalite ou un correctif upstream reel au-dela du seul import (ex. SpotlightOverlay perd domIdPrefix + un correctif Tailwind ; BaseImageElement revient a un pattern de resolution media plus ancien perdant une correction documentee de contamination cross-course ; chartOption.ts perd des verifications defensives ; element-fingerprint.ts perd le cas 'code' (risque de rupture d'exhaustivite de switch). |
| components/slide-renderer/components/element/ImageElement/useFilter.ts | 4 | rejected | Le diff reintroduit un import depuis l'ancien chemin @/lib/types/slides alors qu'upstream v0.3.0 a extrait ces types dans le package @openmaic/dsl (reellement present et utilise ailleurs dans le worktree). Appliquer romprait la coherence d'architecture avec le reste du code v0.3.0 non touche par cette story. Echantillon verifie manuellement (6 fichiers) : chacun perd aussi une fonctionnalite ou un correctif upstream reel au-dela du seul import (ex. SpotlightOverlay perd domIdPrefix + un correctif Tailwind ; BaseImageElement revient a un pattern de resolution media plus ancien perdant une correction documentee de contamination cross-course ; chartOption.ts perd des verifications defensives ; element-fingerprint.ts perd le cas 'code' (risque de rupture d'exhaustivite de switch). |
| components/slide-renderer/components/element/LatexElement/BaseLatexElement.tsx | 4 | rejected | Le diff reintroduit un import depuis l'ancien chemin @/lib/types/slides alors qu'upstream v0.3.0 a extrait ces types dans le package @openmaic/dsl (reellement present et utilise ailleurs dans le worktree). Appliquer romprait la coherence d'architecture avec le reste du code v0.3.0 non touche par cette story. Echantillon verifie manuellement (6 fichiers) : chacun perd aussi une fonctionnalite ou un correctif upstream reel au-dela du seul import (ex. SpotlightOverlay perd domIdPrefix + un correctif Tailwind ; BaseImageElement revient a un pattern de resolution media plus ancien perdant une correction documentee de contamination cross-course ; chartOption.ts perd des verifications defensives ; element-fingerprint.ts perd le cas 'code' (risque de rupture d'exhaustivite de switch). |
| components/slide-renderer/components/element/LatexElement/index.tsx | 4 | rejected | Le diff reintroduit un import depuis l'ancien chemin @/lib/types/slides alors qu'upstream v0.3.0 a extrait ces types dans le package @openmaic/dsl (reellement present et utilise ailleurs dans le worktree). Appliquer romprait la coherence d'architecture avec le reste du code v0.3.0 non touche par cette story. Echantillon verifie manuellement (6 fichiers) : chacun perd aussi une fonctionnalite ou un correctif upstream reel au-dela du seul import (ex. SpotlightOverlay perd domIdPrefix + un correctif Tailwind ; BaseImageElement revient a un pattern de resolution media plus ancien perdant une correction documentee de contamination cross-course ; chartOption.ts perd des verifications defensives ; element-fingerprint.ts perd le cas 'code' (risque de rupture d'exhaustivite de switch). |
| components/slide-renderer/components/element/LineElement/BaseLineElement.tsx | 4 | rejected | Le diff reintroduit un import depuis l'ancien chemin @/lib/types/slides alors qu'upstream v0.3.0 a extrait ces types dans le package @openmaic/dsl (reellement present et utilise ailleurs dans le worktree). Appliquer romprait la coherence d'architecture avec le reste du code v0.3.0 non touche par cette story. Echantillon verifie manuellement (6 fichiers) : chacun perd aussi une fonctionnalite ou un correctif upstream reel au-dela du seul import (ex. SpotlightOverlay perd domIdPrefix + un correctif Tailwind ; BaseImageElement revient a un pattern de resolution media plus ancien perdant une correction documentee de contamination cross-course ; chartOption.ts perd des verifications defensives ; element-fingerprint.ts perd le cas 'code' (risque de rupture d'exhaustivite de switch). |
| components/slide-renderer/components/element/LineElement/LinePointMarker.tsx | 4 | rejected | Le diff reintroduit un import depuis l'ancien chemin @/lib/types/slides alors qu'upstream v0.3.0 a extrait ces types dans le package @openmaic/dsl (reellement present et utilise ailleurs dans le worktree). Appliquer romprait la coherence d'architecture avec le reste du code v0.3.0 non touche par cette story. Echantillon verifie manuellement (6 fichiers) : chacun perd aussi une fonctionnalite ou un correctif upstream reel au-dela du seul import (ex. SpotlightOverlay perd domIdPrefix + un correctif Tailwind ; BaseImageElement revient a un pattern de resolution media plus ancien perdant une correction documentee de contamination cross-course ; chartOption.ts perd des verifications defensives ; element-fingerprint.ts perd le cas 'code' (risque de rupture d'exhaustivite de switch). |
| components/slide-renderer/components/element/LineElement/index.tsx | 4 | rejected | Le diff reintroduit un import depuis l'ancien chemin @/lib/types/slides alors qu'upstream v0.3.0 a extrait ces types dans le package @openmaic/dsl (reellement present et utilise ailleurs dans le worktree). Appliquer romprait la coherence d'architecture avec le reste du code v0.3.0 non touche par cette story. Echantillon verifie manuellement (6 fichiers) : chacun perd aussi une fonctionnalite ou un correctif upstream reel au-dela du seul import (ex. SpotlightOverlay perd domIdPrefix + un correctif Tailwind ; BaseImageElement revient a un pattern de resolution media plus ancien perdant une correction documentee de contamination cross-course ; chartOption.ts perd des verifications defensives ; element-fingerprint.ts perd le cas 'code' (risque de rupture d'exhaustivite de switch). |
| components/slide-renderer/components/element/ShapeElement/BaseShapeElement.tsx | 4 | rejected | Le diff reintroduit un import depuis l'ancien chemin @/lib/types/slides alors qu'upstream v0.3.0 a extrait ces types dans le package @openmaic/dsl (reellement present et utilise ailleurs dans le worktree). Appliquer romprait la coherence d'architecture avec le reste du code v0.3.0 non touche par cette story. Echantillon verifie manuellement (6 fichiers) : chacun perd aussi une fonctionnalite ou un correctif upstream reel au-dela du seul import (ex. SpotlightOverlay perd domIdPrefix + un correctif Tailwind ; BaseImageElement revient a un pattern de resolution media plus ancien perdant une correction documentee de contamination cross-course ; chartOption.ts perd des verifications defensives ; element-fingerprint.ts perd le cas 'code' (risque de rupture d'exhaustivite de switch). |
| components/slide-renderer/components/element/ShapeElement/GradientDefs.tsx | 4 | rejected | Le diff reintroduit un import depuis l'ancien chemin @/lib/types/slides alors qu'upstream v0.3.0 a extrait ces types dans le package @openmaic/dsl (reellement present et utilise ailleurs dans le worktree). Appliquer romprait la coherence d'architecture avec le reste du code v0.3.0 non touche par cette story. Echantillon verifie manuellement (6 fichiers) : chacun perd aussi une fonctionnalite ou un correctif upstream reel au-dela du seul import (ex. SpotlightOverlay perd domIdPrefix + un correctif Tailwind ; BaseImageElement revient a un pattern de resolution media plus ancien perdant une correction documentee de contamination cross-course ; chartOption.ts perd des verifications defensives ; element-fingerprint.ts perd le cas 'code' (risque de rupture d'exhaustivite de switch). |
| components/slide-renderer/components/element/ShapeElement/index.tsx | 4 | rejected | Le diff reintroduit un import depuis l'ancien chemin @/lib/types/slides alors qu'upstream v0.3.0 a extrait ces types dans le package @openmaic/dsl (reellement present et utilise ailleurs dans le worktree). Appliquer romprait la coherence d'architecture avec le reste du code v0.3.0 non touche par cette story. Echantillon verifie manuellement (6 fichiers) : chacun perd aussi une fonctionnalite ou un correctif upstream reel au-dela du seul import (ex. SpotlightOverlay perd domIdPrefix + un correctif Tailwind ; BaseImageElement revient a un pattern de resolution media plus ancien perdant une correction documentee de contamination cross-course ; chartOption.ts perd des verifications defensives ; element-fingerprint.ts perd le cas 'code' (risque de rupture d'exhaustivite de switch). |
| components/slide-renderer/components/element/TableElement/BaseTableElement.tsx | 4 | rejected | Le diff reintroduit un import depuis l'ancien chemin @/lib/types/slides alors qu'upstream v0.3.0 a extrait ces types dans le package @openmaic/dsl (reellement present et utilise ailleurs dans le worktree). Appliquer romprait la coherence d'architecture avec le reste du code v0.3.0 non touche par cette story. Echantillon verifie manuellement (6 fichiers) : chacun perd aussi une fonctionnalite ou un correctif upstream reel au-dela du seul import (ex. SpotlightOverlay perd domIdPrefix + un correctif Tailwind ; BaseImageElement revient a un pattern de resolution media plus ancien perdant une correction documentee de contamination cross-course ; chartOption.ts perd des verifications defensives ; element-fingerprint.ts perd le cas 'code' (risque de rupture d'exhaustivite de switch). |
| components/slide-renderer/components/element/TableElement/StaticTable.tsx | 4 | rejected | Le diff reintroduit un import depuis l'ancien chemin @/lib/types/slides alors qu'upstream v0.3.0 a extrait ces types dans le package @openmaic/dsl (reellement present et utilise ailleurs dans le worktree). Appliquer romprait la coherence d'architecture avec le reste du code v0.3.0 non touche par cette story. Echantillon verifie manuellement (6 fichiers) : chacun perd aussi une fonctionnalite ou un correctif upstream reel au-dela du seul import (ex. SpotlightOverlay perd domIdPrefix + un correctif Tailwind ; BaseImageElement revient a un pattern de resolution media plus ancien perdant une correction documentee de contamination cross-course ; chartOption.ts perd des verifications defensives ; element-fingerprint.ts perd le cas 'code' (risque de rupture d'exhaustivite de switch). |
| components/slide-renderer/components/element/TableElement/index.tsx | 4 | rejected | Le diff reintroduit un import depuis l'ancien chemin @/lib/types/slides alors qu'upstream v0.3.0 a extrait ces types dans le package @openmaic/dsl (reellement present et utilise ailleurs dans le worktree). Appliquer romprait la coherence d'architecture avec le reste du code v0.3.0 non touche par cette story. Echantillon verifie manuellement (6 fichiers) : chacun perd aussi une fonctionnalite ou un correctif upstream reel au-dela du seul import (ex. SpotlightOverlay perd domIdPrefix + un correctif Tailwind ; BaseImageElement revient a un pattern de resolution media plus ancien perdant une correction documentee de contamination cross-course ; chartOption.ts perd des verifications defensives ; element-fingerprint.ts perd le cas 'code' (risque de rupture d'exhaustivite de switch). |
| components/slide-renderer/components/element/TableElement/tableUtils.ts | 4 | rejected | Le diff reintroduit un import depuis l'ancien chemin @/lib/types/slides alors qu'upstream v0.3.0 a extrait ces types dans le package @openmaic/dsl (reellement present et utilise ailleurs dans le worktree). Appliquer romprait la coherence d'architecture avec le reste du code v0.3.0 non touche par cette story. Echantillon verifie manuellement (6 fichiers) : chacun perd aussi une fonctionnalite ou un correctif upstream reel au-dela du seul import (ex. SpotlightOverlay perd domIdPrefix + un correctif Tailwind ; BaseImageElement revient a un pattern de resolution media plus ancien perdant une correction documentee de contamination cross-course ; chartOption.ts perd des verifications defensives ; element-fingerprint.ts perd le cas 'code' (risque de rupture d'exhaustivite de switch). |
| components/slide-renderer/components/element/TextElement/BaseTextElement.tsx | 4 | rejected | Le diff reintroduit un import depuis l'ancien chemin @/lib/types/slides alors qu'upstream v0.3.0 a extrait ces types dans le package @openmaic/dsl (reellement present et utilise ailleurs dans le worktree). Appliquer romprait la coherence d'architecture avec le reste du code v0.3.0 non touche par cette story. Echantillon verifie manuellement (6 fichiers) : chacun perd aussi une fonctionnalite ou un correctif upstream reel au-dela du seul import (ex. SpotlightOverlay perd domIdPrefix + un correctif Tailwind ; BaseImageElement revient a un pattern de resolution media plus ancien perdant une correction documentee de contamination cross-course ; chartOption.ts perd des verifications defensives ; element-fingerprint.ts perd le cas 'code' (risque de rupture d'exhaustivite de switch). |
| components/slide-renderer/components/element/TextElement/index.tsx | 4 | rejected | Le diff reintroduit un import depuis l'ancien chemin @/lib/types/slides alors qu'upstream v0.3.0 a extrait ces types dans le package @openmaic/dsl (reellement present et utilise ailleurs dans le worktree). Appliquer romprait la coherence d'architecture avec le reste du code v0.3.0 non touche par cette story. Echantillon verifie manuellement (6 fichiers) : chacun perd aussi une fonctionnalite ou un correctif upstream reel au-dela du seul import (ex. SpotlightOverlay perd domIdPrefix + un correctif Tailwind ; BaseImageElement revient a un pattern de resolution media plus ancien perdant une correction documentee de contamination cross-course ; chartOption.ts perd des verifications defensives ; element-fingerprint.ts perd le cas 'code' (risque de rupture d'exhaustivite de switch). |
| components/slide-renderer/components/element/VideoElement/index.tsx | 4 | rejected | Le diff reintroduit un import depuis l'ancien chemin @/lib/types/slides alors qu'upstream v0.3.0 a extrait ces types dans le package @openmaic/dsl (reellement present et utilise ailleurs dans le worktree). Appliquer romprait la coherence d'architecture avec le reste du code v0.3.0 non touche par cette story. Echantillon verifie manuellement (6 fichiers) : chacun perd aussi une fonctionnalite ou un correctif upstream reel au-dela du seul import (ex. SpotlightOverlay perd domIdPrefix + un correctif Tailwind ; BaseImageElement revient a un pattern de resolution media plus ancien perdant une correction documentee de contamination cross-course ; chartOption.ts perd des verifications defensives ; element-fingerprint.ts perd le cas 'code' (risque de rupture d'exhaustivite de switch). |
| components/slide-renderer/components/element/hooks/useElementFill.ts | 4 | rejected | Le diff reintroduit un import depuis l'ancien chemin @/lib/types/slides alors qu'upstream v0.3.0 a extrait ces types dans le package @openmaic/dsl (reellement present et utilise ailleurs dans le worktree). Appliquer romprait la coherence d'architecture avec le reste du code v0.3.0 non touche par cette story. Echantillon verifie manuellement (6 fichiers) : chacun perd aussi une fonctionnalite ou un correctif upstream reel au-dela du seul import (ex. SpotlightOverlay perd domIdPrefix + un correctif Tailwind ; BaseImageElement revient a un pattern de resolution media plus ancien perdant une correction documentee de contamination cross-course ; chartOption.ts perd des verifications defensives ; element-fingerprint.ts perd le cas 'code' (risque de rupture d'exhaustivite de switch). |
| components/slide-renderer/components/element/hooks/useElementOutline.ts | 4 | rejected | Le diff reintroduit un import depuis l'ancien chemin @/lib/types/slides alors qu'upstream v0.3.0 a extrait ces types dans le package @openmaic/dsl (reellement present et utilise ailleurs dans le worktree). Appliquer romprait la coherence d'architecture avec le reste du code v0.3.0 non touche par cette story. Echantillon verifie manuellement (6 fichiers) : chacun perd aussi une fonctionnalite ou un correctif upstream reel au-dela du seul import (ex. SpotlightOverlay perd domIdPrefix + un correctif Tailwind ; BaseImageElement revient a un pattern de resolution media plus ancien perdant une correction documentee de contamination cross-course ; chartOption.ts perd des verifications defensives ; element-fingerprint.ts perd le cas 'code' (risque de rupture d'exhaustivite de switch). |
| components/slide-renderer/components/element/hooks/useElementShadow.ts | 4 | rejected | Le diff reintroduit un import depuis l'ancien chemin @/lib/types/slides alors qu'upstream v0.3.0 a extrait ces types dans le package @openmaic/dsl (reellement present et utilise ailleurs dans le worktree). Appliquer romprait la coherence d'architecture avec le reste du code v0.3.0 non touche par cette story. Echantillon verifie manuellement (6 fichiers) : chacun perd aussi une fonctionnalite ou un correctif upstream reel au-dela du seul import (ex. SpotlightOverlay perd domIdPrefix + un correctif Tailwind ; BaseImageElement revient a un pattern de resolution media plus ancien perdant une correction documentee de contamination cross-course ; chartOption.ts perd des verifications defensives ; element-fingerprint.ts perd le cas 'code' (risque de rupture d'exhaustivite de switch). |
| components/stage/scene-renderer.tsx | 17 | rejected | Perd le support du type de scene 'plugin' (PluginRenderer) ajoute par upstream -- perte de fonctionnalite entiere, pas une divergence cosmetique. |
| components/ui/popover.tsx | 8 | rejected | Retire les exports PopoverAnchor/PopoverClose ajoutes par upstream -- risque de casser d'autres fichiers (hors perimetre) qui les importeraient. |
| components/whiteboard/whiteboard-canvas.tsx | 14 | rejected | Le diff reintroduit un import depuis l'ancien chemin @/lib/types/slides alors qu'upstream v0.3.0 a extrait ces types dans le package @openmaic/dsl (reellement present et utilise ailleurs dans le worktree). Appliquer romprait la coherence d'architecture avec le reste du code v0.3.0 non touche par cette story. Echantillon verifie manuellement (6 fichiers) : chacun perd aussi une fonctionnalite ou un correctif upstream reel au-dela du seul import (ex. SpotlightOverlay perd domIdPrefix + un correctif Tailwind ; BaseImageElement revient a un pattern de resolution media plus ancien perdant une correction documentee de contamination cross-course ; chartOption.ts perd des verifications defensives ; element-fingerprint.ts perd le cas 'code' (risque de rupture d'exhaustivite de switch). |
| components/whiteboard/whiteboard-history.tsx | 7 | noop | contenu main deja applique au worktree lors d'une execution precedente (source=git) — idempotent, rien a refaire. Motif d'application : Coherence i18n : les traductions Qalem deja portees (lib/i18n/stage.ts) utilisent des placeholders a accolade simple '{n}' (pas '{{n}}' i18next). Le t(key,{n}) upstream ne les interpolerait pas (react-i18next attend '{{n}}') -- le .replace('{n}',...) de main est necessaire pour un affichage correct. |
| configs/animation.ts | 4 | rejected | Le diff reintroduit un import depuis l'ancien chemin @/lib/types/slides alors qu'upstream v0.3.0 a extrait ces types dans le package @openmaic/dsl (reellement present et utilise ailleurs dans le worktree). Appliquer romprait la coherence d'architecture avec le reste du code v0.3.0 non touche par cette story. Echantillon verifie manuellement (6 fichiers) : chacun perd aussi une fonctionnalite ou un correctif upstream reel au-dela du seul import (ex. SpotlightOverlay perd domIdPrefix + un correctif Tailwind ; BaseImageElement revient a un pattern de resolution media plus ancien perdant une correction documentee de contamination cross-course ; chartOption.ts perd des verifications defensives ; element-fingerprint.ts perd le cas 'code' (risque de rupture d'exhaustivite de switch). |
| configs/chart.ts | 4 | rejected | Le diff reintroduit un import depuis l'ancien chemin @/lib/types/slides alors qu'upstream v0.3.0 a extrait ces types dans le package @openmaic/dsl (reellement present et utilise ailleurs dans le worktree). Appliquer romprait la coherence d'architecture avec le reste du code v0.3.0 non touche par cette story. Echantillon verifie manuellement (6 fichiers) : chacun perd aussi une fonctionnalite ou un correctif upstream reel au-dela du seul import (ex. SpotlightOverlay perd domIdPrefix + un correctif Tailwind ; BaseImageElement revient a un pattern de resolution media plus ancien perdant une correction documentee de contamination cross-course ; chartOption.ts perd des verifications defensives ; element-fingerprint.ts perd le cas 'code' (risque de rupture d'exhaustivite de switch). |
| configs/lines.ts | 4 | rejected | Le diff reintroduit un import depuis l'ancien chemin @/lib/types/slides alors qu'upstream v0.3.0 a extrait ces types dans le package @openmaic/dsl (reellement present et utilise ailleurs dans le worktree). Appliquer romprait la coherence d'architecture avec le reste du code v0.3.0 non touche par cette story. Echantillon verifie manuellement (6 fichiers) : chacun perd aussi une fonctionnalite ou un correctif upstream reel au-dela du seul import (ex. SpotlightOverlay perd domIdPrefix + un correctif Tailwind ; BaseImageElement revient a un pattern de resolution media plus ancien perdant une correction documentee de contamination cross-course ; chartOption.ts perd des verifications defensives ; element-fingerprint.ts perd le cas 'code' (risque de rupture d'exhaustivite de switch). |
| configs/shapes.ts | 4 | rejected | Le diff reintroduit un import depuis l'ancien chemin @/lib/types/slides alors qu'upstream v0.3.0 a extrait ces types dans le package @openmaic/dsl (reellement present et utilise ailleurs dans le worktree). Appliquer romprait la coherence d'architecture avec le reste du code v0.3.0 non touche par cette story. Echantillon verifie manuellement (6 fichiers) : chacun perd aussi une fonctionnalite ou un correctif upstream reel au-dela du seul import (ex. SpotlightOverlay perd domIdPrefix + un correctif Tailwind ; BaseImageElement revient a un pattern de resolution media plus ancien perdant une correction documentee de contamination cross-course ; chartOption.ts perd des verifications defensives ; element-fingerprint.ts perd le cas 'code' (risque de rupture d'exhaustivite de switch). |
| configs/theme.ts | 4 | rejected | Le diff reintroduit un import depuis l'ancien chemin @/lib/types/slides alors qu'upstream v0.3.0 a extrait ces types dans le package @openmaic/dsl (reellement present et utilise ailleurs dans le worktree). Appliquer romprait la coherence d'architecture avec le reste du code v0.3.0 non touche par cette story. Echantillon verifie manuellement (6 fichiers) : chacun perd aussi une fonctionnalite ou un correctif upstream reel au-dela du seul import (ex. SpotlightOverlay perd domIdPrefix + un correctif Tailwind ; BaseImageElement revient a un pattern de resolution media plus ancien perdant une correction documentee de contamination cross-course ; chartOption.ts perd des verifications defensives ; element-fingerprint.ts perd le cas 'code' (risque de rupture d'exhaustivite de switch). |
| e2e/fixtures/mock-api.ts | 27 | rejected | Upstream a ajoute l'extraction automatique de stageId depuis le corps de requete (robustesse) que main n'a pas encore -- fichier de test uniquement mais retrait d'une fonctionnalite reelle sans necessite identifiee ; rejet par coherence de politique. |
| e2e/fixtures/test-data/scene-content.ts | 6 | rejected | Le diff reintroduit un import depuis l'ancien chemin @/lib/types/slides alors qu'upstream v0.3.0 a extrait ces types dans le package @openmaic/dsl (reellement present et utilise ailleurs dans le worktree). Appliquer romprait la coherence d'architecture avec le reste du code v0.3.0 non touche par cette story. Echantillon verifie manuellement (6 fichiers) : chacun perd aussi une fonctionnalite ou un correctif upstream reel au-dela du seul import (ex. SpotlightOverlay perd domIdPrefix + un correctif Tailwind ; BaseImageElement revient a un pattern de resolution media plus ancien perdant une correction documentee de contamination cross-course ; chartOption.ts perd des verifications defensives ; element-fingerprint.ts perd le cas 'code' (risque de rupture d'exhaustivite de switch). |
| e2e/fixtures/test-data/settings.ts | 3 | rejected | Perd le champ reviewOutlineEnabled ajoute par upstream (fixture de test). |
| e2e/pages/home.page.ts | 4 | noop | contenu main deja applique au worktree lors d'une execution precedente (source=git) — idempotent, rien a refaire. Motif d'application : Branding : selecteur de test cherche alt='Qalem' au lieu de alt='OpenMAIC' -- necessaire pour que les e2e trouvent le logo une fois le rebranding applique (deja fait sur layout.tsx/page.tsx via OURS_ONLY). |
| eslint.config.mjs | 15 | noop | contenu main deja applique au worktree lors d'une execution precedente (source=git) — idempotent, rien a refaire. Motif d'application : Le remplacement des ignores granulaires par 'packages/**' est un SUR-ensemble (ignore AU MOINS autant, y compris packages/@openmaic et packages/docs) -- reduit le perimetre de lint, ne peut pas introduire de nouvelle erreur. |
| lib/api/stage-api-canvas.ts | 4 | rejected | Le diff reintroduit un import depuis l'ancien chemin @/lib/types/slides alors qu'upstream v0.3.0 a extrait ces types dans le package @openmaic/dsl (reellement present et utilise ailleurs dans le worktree). Appliquer romprait la coherence d'architecture avec le reste du code v0.3.0 non touche par cette story. Echantillon verifie manuellement (6 fichiers) : chacun perd aussi une fonctionnalite ou un correctif upstream reel au-dela du seul import (ex. SpotlightOverlay perd domIdPrefix + un correctif Tailwind ; BaseImageElement revient a un pattern de resolution media plus ancien perdant une correction documentee de contamination cross-course ; chartOption.ts perd des verifications defensives ; element-fingerprint.ts perd le cas 'code' (risque de rupture d'exhaustivite de switch). |
| lib/api/stage-api-element.ts | 4 | rejected | Le diff reintroduit un import depuis l'ancien chemin @/lib/types/slides alors qu'upstream v0.3.0 a extrait ces types dans le package @openmaic/dsl (reellement present et utilise ailleurs dans le worktree). Appliquer romprait la coherence d'architecture avec le reste du code v0.3.0 non touche par cette story. Echantillon verifie manuellement (6 fichiers) : chacun perd aussi une fonctionnalite ou un correctif upstream reel au-dela du seul import (ex. SpotlightOverlay perd domIdPrefix + un correctif Tailwind ; BaseImageElement revient a un pattern de resolution media plus ancien perdant une correction documentee de contamination cross-course ; chartOption.ts perd des verifications defensives ; element-fingerprint.ts perd le cas 'code' (risque de rupture d'exhaustivite de switch). |
| lib/api/stage-api-types.ts | 4 | rejected | Le diff reintroduit un import depuis l'ancien chemin @/lib/types/slides alors qu'upstream v0.3.0 a extrait ces types dans le package @openmaic/dsl (reellement present et utilise ailleurs dans le worktree). Appliquer romprait la coherence d'architecture avec le reste du code v0.3.0 non touche par cette story. Echantillon verifie manuellement (6 fichiers) : chacun perd aussi une fonctionnalite ou un correctif upstream reel au-dela du seul import (ex. SpotlightOverlay perd domIdPrefix + un correctif Tailwind ; BaseImageElement revient a un pattern de resolution media plus ancien perdant une correction documentee de contamination cross-course ; chartOption.ts perd des verifications defensives ; element-fingerprint.ts perd le cas 'code' (risque de rupture d'exhaustivite de switch). |
| lib/api/stage-api-whiteboard.ts | 4 | rejected | Le diff reintroduit un import depuis l'ancien chemin @/lib/types/slides alors qu'upstream v0.3.0 a extrait ces types dans le package @openmaic/dsl (reellement present et utilise ailleurs dans le worktree). Appliquer romprait la coherence d'architecture avec le reste du code v0.3.0 non touche par cette story. Echantillon verifie manuellement (6 fichiers) : chacun perd aussi une fonctionnalite ou un correctif upstream reel au-dela du seul import (ex. SpotlightOverlay perd domIdPrefix + un correctif Tailwind ; BaseImageElement revient a un pattern de resolution media plus ancien perdant une correction documentee de contamination cross-course ; chartOption.ts perd des verifications defensives ; element-fingerprint.ts perd le cas 'code' (risque de rupture d'exhaustivite de switch). |
| lib/audio/browser-tts-preview.ts | 16 | noop | contenu main deja applique au worktree lors d'une execution precedente (source=git) — idempotent, rien a refaire. Motif d'application : Additif pur : main ajoute la detection de langue arabe (ar-MA) et francaise (fr-FR) a inferPreviewLang(), sans retirer aucune capacite upstream. Coeur de la mission FR/AR de Qalem. |
| lib/audio/use-tts-preview.ts | 6 | rejected | Perd modelId et providerOptions (options TTS avancees) ajoutes par upstream. |
| lib/export/svg-path-parser.ts | 24 | rejected | Upstream a ajoute une gestion defensive des chemins SVG malformes (try/catch, retour [] documente comme corrigeant un crash reel cause par des hallucinations LLM). Appliquer main REINTRODUIRAIT ce risque de crash. |
| lib/generation/generation-pipeline.ts | 6 | rejected | Renomme buildLanguageText en getLanguageName/getLanguageQualityRules (meme cluster languageDirective que scene-builder.ts/pipeline-runner.ts) et retire un re-export de types (SceneContentOptions/SceneActionsOptions) potentiellement utilise ailleurs. |
| lib/generation/interactive-post-processor.ts | 25 | rejected | Upstream corrige un bug de performance documente (O(blocs x longueur) avec l'ancienne boucle indexOf+substring, remplace par un replacer fonction qui est deja immunise contre les caracteres '$' -- le commentaire de main affirme a tort le contraire). Appliquer main REINTRODUIRAIT ce bug de perf deja corrige. |
| lib/generation/pipeline-runner.ts | 25 | rejected | CORRECTION apres re-verification rigoureuse du sens du diff : c'est en realite MAIN qui contient encore les messages de statut CODES EN DUR EN CHINOIS ('正在分析需求...', '正在生成场景内容...', '生成完成！') alors qu'UPSTREAM les a DEJA en anglais -- appliquer main REINTRODUIRAIT du chinois fige dans l'UI, contraire a l'objectif meme de Qalem. De plus main perd la destructuration 'languageDirective' (fonctionnalite upstream plus recente, cf. cluster scene-builder.ts/generation-preview/types.ts). Rejet sur les deux motifs. |
| lib/generation/scene-builder.ts | 27 | rejected | Le diff reintroduit un import depuis l'ancien chemin @/lib/types/slides alors qu'upstream v0.3.0 a extrait ces types dans le package @openmaic/dsl (reellement present et utilise ailleurs dans le worktree). Appliquer romprait la coherence d'architecture avec le reste du code v0.3.0 non touche par cette story. Echantillon verifie manuellement (6 fichiers) : chacun perd aussi une fonctionnalite ou un correctif upstream reel au-dela du seul import (ex. SpotlightOverlay perd domIdPrefix + un correctif Tailwind ; BaseImageElement revient a un pattern de resolution media plus ancien perdant une correction documentee de contamination cross-course ; chartOption.ts perd des verifications defensives ; element-fingerprint.ts perd le cas 'code' (risque de rupture d'exhaustivite de switch). |
| lib/hooks/use-browser-asr.ts | 13 | rejected | Le diff reintroduit un import depuis l'ancien chemin @/lib/types/slides alors qu'upstream v0.3.0 a extrait ces types dans le package @openmaic/dsl (reellement present et utilise ailleurs dans le worktree). Appliquer romprait la coherence d'architecture avec le reste du code v0.3.0 non touche par cette story. Echantillon verifie manuellement (6 fichiers) : chacun perd aussi une fonctionnalite ou un correctif upstream reel au-dela du seul import (ex. SpotlightOverlay perd domIdPrefix + un correctif Tailwind ; BaseImageElement revient a un pattern de resolution media plus ancien perdant une correction documentee de contamination cross-course ; chartOption.ts perd des verifications defensives ; element-fingerprint.ts perd le cas 'code' (risque de rupture d'exhaustivite de switch). |
| lib/hooks/use-canvas-operations.ts | 8 | rejected | Le diff reintroduit un import depuis l'ancien chemin @/lib/types/slides alors qu'upstream v0.3.0 a extrait ces types dans le package @openmaic/dsl (reellement present et utilise ailleurs dans le worktree). Appliquer romprait la coherence d'architecture avec le reste du code v0.3.0 non touche par cette story. Echantillon verifie manuellement (6 fichiers) : chacun perd aussi une fonctionnalite ou un correctif upstream reel au-dela du seul import (ex. SpotlightOverlay perd domIdPrefix + un correctif Tailwind ; BaseImageElement revient a un pattern de resolution media plus ancien perdant une correction documentee de contamination cross-course ; chartOption.ts perd des verifications defensives ; element-fingerprint.ts perd le cas 'code' (risque de rupture d'exhaustivite de switch). |
| lib/hooks/use-order-element.ts | 6 | rejected | Le diff reintroduit un import depuis l'ancien chemin @/lib/types/slides alors qu'upstream v0.3.0 a extrait ces types dans le package @openmaic/dsl (reellement present et utilise ailleurs dans le worktree). Appliquer romprait la coherence d'architecture avec le reste du code v0.3.0 non touche par cette story. Echantillon verifie manuellement (6 fichiers) : chacun perd aussi une fonctionnalite ou un correctif upstream reel au-dela du seul import (ex. SpotlightOverlay perd domIdPrefix + un correctif Tailwind ; BaseImageElement revient a un pattern de resolution media plus ancien perdant une correction documentee de contamination cross-course ; chartOption.ts perd des verifications defensives ; element-fingerprint.ts perd le cas 'code' (risque de rupture d'exhaustivite de switch). |
| lib/hooks/use-slide-background-style.ts | 4 | rejected | Le diff reintroduit un import depuis l'ancien chemin @/lib/types/slides alors qu'upstream v0.3.0 a extrait ces types dans le package @openmaic/dsl (reellement present et utilise ailleurs dans le worktree). Appliquer romprait la coherence d'architecture avec le reste du code v0.3.0 non touche par cette story. Echantillon verifie manuellement (6 fichiers) : chacun perd aussi une fonctionnalite ou un correctif upstream reel au-dela du seul import (ex. SpotlightOverlay perd domIdPrefix + un correctif Tailwind ; BaseImageElement revient a un pattern de resolution media plus ancien perdant une correction documentee de contamination cross-course ; chartOption.ts perd des verifications defensives ; element-fingerprint.ts perd le cas 'code' (risque de rupture d'exhaustivite de switch). |
| lib/hooks/use-streaming-text.ts | 17 | rejected | Le diff reintroduit un import depuis l'ancien chemin @/lib/types/slides alors qu'upstream v0.3.0 a extrait ces types dans le package @openmaic/dsl (reellement present et utilise ailleurs dans le worktree). Appliquer romprait la coherence d'architecture avec le reste du code v0.3.0 non touche par cette story. Echantillon verifie manuellement (6 fichiers) : chacun perd aussi une fonctionnalite ou un correctif upstream reel au-dela du seul import (ex. SpotlightOverlay perd domIdPrefix + un correctif Tailwind ; BaseImageElement revient a un pattern de resolution media plus ancien perdant une correction documentee de contamination cross-course ; chartOption.ts perd des verifications defensives ; element-fingerprint.ts perd le cas 'code' (risque de rupture d'exhaustivite de switch). |
| lib/hooks/use-theme.tsx | 6 | noop | contenu main deja applique au worktree lors d'une execution precedente (source=git) — idempotent, rien a refaire. Motif d'application : Defaut de theme 'dark'/'dark' au lieu de 'system'/'light' -- choix UX cosmetique reversible, ne retire aucune capacite upstream, ne touche aucun type/API partagee. |
| lib/i18n/types.ts | 8 | noop | contenu main deja applique au worktree lors d'une execution precedente (source=git) — idempotent, rien a refaire. Motif d'application : Necessaire : le systeme i18n deja porte (OURS_ONLY, use-i18n.tsx via react-i18next) depend du type Locale incluant fr-FR/ar-MA et de defaultLocale='fr-FR'. La version upstream derive Locale du nouveau registre lib/i18n/locales.ts (zh-CN/zh-TW/en-US/ja-JP/ru-RU/ar-SA/pt-BR/ko-KR, PAS fr-FR/ar-MA) -- la garder casserait la compilation du systeme i18n deja porte. |
| lib/media/types.ts | 22 | rejected | Retrecit drastiquement ImageProviderId/VideoProviderId (perd openai-image, minimax-image, grok-image, lemonade, minimax-video, happyhorse) -- risque concret de casser d'autres fichiers (hors perimetre des 137) qui referencent ces IDs, contraire a l'objectif tsc vert. |
| lib/orchestration/registry/store.ts | 24 | rejected | Melange : la traduction des noms d'agents par defaut (chinois -> anglais) est souhaitable (cf. Known Issues progress.md) MAIS indissociable ici de pertes reelles (type VoiceDesign, actions wb_draw_code/wb_edit_code, warm-up vocal). Renommage a refaire chirurgicalement, plus probablement dans S0-006 (branding). |
| lib/orchestration/registry/types.ts | 11 | rejected | Perd voiceDesign, modelId (config vocale par agent) et les actions wb_draw_code/wb_edit_code -- meme cluster que registry/store.ts et tool-schemas.ts/inline-action-tag.tsx. |
| lib/orchestration/stateless-generate.ts | 16 | rejected | Le diff reintroduit un import depuis l'ancien chemin @/lib/types/slides alors qu'upstream v0.3.0 a extrait ces types dans le package @openmaic/dsl (reellement present et utilise ailleurs dans le worktree). Appliquer romprait la coherence d'architecture avec le reste du code v0.3.0 non touche par cette story. Echantillon verifie manuellement (6 fichiers) : chacun perd aussi une fonctionnalite ou un correctif upstream reel au-dela du seul import (ex. SpotlightOverlay perd domIdPrefix + un correctif Tailwind ; BaseImageElement revient a un pattern de resolution media plus ancien perdant une correction documentee de contamination cross-course ; chartOption.ts perd des verifications defensives ; element-fingerprint.ts perd le cas 'code' (risque de rupture d'exhaustivite de switch). |
| lib/orchestration/tool-schemas.ts | 6 | rejected | Perd les descriptions d'outils wb_draw_code/wb_edit_code -- meme cluster que registry/types.ts et inline-action-tag.tsx. |
| lib/pbl/mcp/issueboard-mcp.ts | 12 | rejected | Renomme 'languageDirective' en 'language' -- lie au meme cluster de fonctionnalite que scene-builder.ts/pipeline-runner.ts (deja rejetes) ; incoherent d'appliquer ici isolement. |
| lib/pdf/constants.ts | 12 | rejected | Retire le provider 'mineru-cloud' entierement -- retrecissement de capacite, meme famille de risque que media/types.ts et lib/pdf/types.ts. |
| lib/pdf/types.ts | 4 | rejected | Retrecit PDFProviderId en retirant 'mineru-cloud' -- coherent avec le rejet de lib/pdf/constants.ts (meme provider). |
| lib/prosemirror/schema/marks.ts | 12 | rejected | SEVERITE ELEVEE : upstream a ajoute une protection anti-injection CSS (validation/echappement du nom de police avant insertion dans un style inline, avec commentaire documentant explicitement le risque). Appliquer main REVERTERAIT ce correctif de securite. |
| lib/server/api-response.ts | 6 | rejected | Perd plusieurs codes d'erreur ajoutes par upstream (PROVIDER_DISABLED, VOXCPM_AUTO_VOICE_REQUIRES_CONTEXT, TOO_MANY_REDIRECTS, RATE_LIMITED) -- risque concret de casser d'autres fichiers (hors perimetre) qui les referenceraient. |
| lib/server/classroom-job-store.ts | 4 | rejected | REVERTE apres test reel : main ajoute un champ 'language' (gain identifie, additif) MAIS son application introduit une VRAIE erreur tsc (TS2339 : 'Property language does not exist on type GenerateClassroomInput') car ce type est defini dans lib/server/classroom-generation.ts, hors du perimetre des 137 fichiers de cette story -- l'ajouter la aurait exige une modification hors-scope. Applique puis reverte a l'etat upstream d'origine (aucun changement net) pour ne pas introduire de dette tsc nouvelle ; a reprendre dans une story ulterieure qui couvre lib/server/classroom-generation.ts. |
| lib/store/canvas.ts | 12 | rejected | Perd la fonctionnalite 'pickTarget' (mode timeline 'choisir un element sur le canevas') ajoutee par upstream. |
| lib/store/whiteboard-history.ts | 4 | rejected | Le diff reintroduit un import depuis l'ancien chemin @/lib/types/slides alors qu'upstream v0.3.0 a extrait ces types dans le package @openmaic/dsl (reellement present et utilise ailleurs dans le worktree). Appliquer romprait la coherence d'architecture avec le reste du code v0.3.0 non touche par cette story. Echantillon verifie manuellement (6 fichiers) : chacun perd aussi une fonctionnalite ou un correctif upstream reel au-dela du seul import (ex. SpotlightOverlay perd domIdPrefix + un correctif Tailwind ; BaseImageElement revient a un pattern de resolution media plus ancien perdant une correction documentee de contamination cross-course ; chartOption.ts perd des verifications defensives ; element-fingerprint.ts perd le cas 'code' (risque de rupture d'exhaustivite de switch). |
| lib/types/chat.ts | 19 | rejected | Melange complexe : perd ThinkingConfig, SessionStatus='error', maxTurns/currentTurn (fonctionnalites upstream) ; gagnerait 'requiresApiKey' (utile, cf. use-pbl-chat.ts) ; change aussi l'import de WhiteboardActionRecord vers un chemin obsolete (./types au lieu de ./director-prompt). Remplacement integral disproportionnellement risque pour le present perimetre mecanique. |
| lib/types/edit.ts | 9 | rejected | Le diff reintroduit un import depuis l'ancien chemin @/lib/types/slides alors qu'upstream v0.3.0 a extrait ces types dans le package @openmaic/dsl (reellement present et utilise ailleurs dans le worktree). Appliquer romprait la coherence d'architecture avec le reste du code v0.3.0 non touche par cette story. Echantillon verifie manuellement (6 fichiers) : chacun perd aussi une fonctionnalite ou un correctif upstream reel au-dela du seul import (ex. SpotlightOverlay perd domIdPrefix + un correctif Tailwind ; BaseImageElement revient a un pattern de resolution media plus ancien perdant une correction documentee de contamination cross-course ; chartOption.ts perd des verifications defensives ; element-fingerprint.ts perd le cas 'code' (risque de rupture d'exhaustivite de switch). |
| lib/types/settings.ts | 10 | rejected | Perd le champ serverBaseUrl (override d'URL cote serveur) ajoute par upstream -- risque de casser d'autres fichiers hors perimetre qui le referenceraient. |
| lib/utils/audio-player.ts | 13 | rejected | Le diff reintroduit un import depuis l'ancien chemin @/lib/types/slides alors qu'upstream v0.3.0 a extrait ces types dans le package @openmaic/dsl (reellement present et utilise ailleurs dans le worktree). Appliquer romprait la coherence d'architecture avec le reste du code v0.3.0 non touche par cette story. Echantillon verifie manuellement (6 fichiers) : chacun perd aussi une fonctionnalite ou un correctif upstream reel au-dela du seul import (ex. SpotlightOverlay perd domIdPrefix + un correctif Tailwind ; BaseImageElement revient a un pattern de resolution media plus ancien perdant une correction documentee de contamination cross-course ; chartOption.ts perd des verifications defensives ; element-fingerprint.ts perd le cas 'code' (risque de rupture d'exhaustivite de switch). |
| lib/utils/element-fingerprint.ts | 6 | rejected | Le diff reintroduit un import depuis l'ancien chemin @/lib/types/slides alors qu'upstream v0.3.0 a extrait ces types dans le package @openmaic/dsl (reellement present et utilise ailleurs dans le worktree). Appliquer romprait la coherence d'architecture avec le reste du code v0.3.0 non touche par cette story. Echantillon verifie manuellement (6 fichiers) : chacun perd aussi une fonctionnalite ou un correctif upstream reel au-dela du seul import (ex. SpotlightOverlay perd domIdPrefix + un correctif Tailwind ; BaseImageElement revient a un pattern de resolution media plus ancien perdant une correction documentee de contamination cross-course ; chartOption.ts perd des verifications defensives ; element-fingerprint.ts perd le cas 'code' (risque de rupture d'exhaustivite de switch). |
| lib/utils/element.ts | 4 | rejected | Le diff reintroduit un import depuis l'ancien chemin @/lib/types/slides alors qu'upstream v0.3.0 a extrait ces types dans le package @openmaic/dsl (reellement present et utilise ailleurs dans le worktree). Appliquer romprait la coherence d'architecture avec le reste du code v0.3.0 non touche par cette story. Echantillon verifie manuellement (6 fichiers) : chacun perd aussi une fonctionnalite ou un correctif upstream reel au-dela du seul import (ex. SpotlightOverlay perd domIdPrefix + un correctif Tailwind ; BaseImageElement revient a un pattern de resolution media plus ancien perdant une correction documentee de contamination cross-course ; chartOption.ts perd des verifications defensives ; element-fingerprint.ts perd le cas 'code' (risque de rupture d'exhaustivite de switch). |
| lib/utils/geometry.ts | 4 | rejected | Le diff reintroduit un import depuis l'ancien chemin @/lib/types/slides alors qu'upstream v0.3.0 a extrait ces types dans le package @openmaic/dsl (reellement present et utilise ailleurs dans le worktree). Appliquer romprait la coherence d'architecture avec le reste du code v0.3.0 non touche par cette story. Echantillon verifie manuellement (6 fichiers) : chacun perd aussi une fonctionnalite ou un correctif upstream reel au-dela du seul import (ex. SpotlightOverlay perd domIdPrefix + un correctif Tailwind ; BaseImageElement revient a un pattern de resolution media plus ancien perdant une correction documentee de contamination cross-course ; chartOption.ts perd des verifications defensives ; element-fingerprint.ts perd le cas 'code' (risque de rupture d'exhaustivite de switch). |
| lib/utils/model-config.ts | 15 | rejected | Le diff reintroduit un import depuis l'ancien chemin @/lib/types/slides alors qu'upstream v0.3.0 a extrait ces types dans le package @openmaic/dsl (reellement present et utilise ailleurs dans le worktree). Appliquer romprait la coherence d'architecture avec le reste du code v0.3.0 non touche par cette story. Echantillon verifie manuellement (6 fichiers) : chacun perd aussi une fonctionnalite ou un correctif upstream reel au-dela du seul import (ex. SpotlightOverlay perd domIdPrefix + un correctif Tailwind ; BaseImageElement revient a un pattern de resolution media plus ancien perdant une correction documentee de contamination cross-course ; chartOption.ts perd des verifications defensives ; element-fingerprint.ts perd le cas 'code' (risque de rupture d'exhaustivite de switch). |
| lib/web-search/types.ts | 14 | rejected | Le diff reintroduit un import depuis l'ancien chemin @/lib/types/slides alors qu'upstream v0.3.0 a extrait ces types dans le package @openmaic/dsl (reellement present et utilise ailleurs dans le worktree). Appliquer romprait la coherence d'architecture avec le reste du code v0.3.0 non touche par cette story. Echantillon verifie manuellement (6 fichiers) : chacun perd aussi une fonctionnalite ou un correctif upstream reel au-dela du seul import (ex. SpotlightOverlay perd domIdPrefix + un correctif Tailwind ; BaseImageElement revient a un pattern de resolution media plus ancien perdant une correction documentee de contamination cross-course ; chartOption.ts perd des verifications defensives ; element-fingerprint.ts perd le cas 'code' (risque de rupture d'exhaustivite de switch). |
| packages/pptxgenjs/rollup.config.mjs | 8 | rejected | Modifie la configuration de build du package pptxgenjs dont le postinstall est deja verifie vert par S0-001 -- risque de regression sur une chaine de build deja prouvee, sans benefice identifie. |
| packages/pptxgenjs/src/gen-media.ts | 4 | rejected | Retire les commentaires magiques '/* webpackIgnore: true */' presents dans la version v0.3.0 dont le build (postinstall) est deja verifie vert par S0-001 -- pas de raison de toucher a un etat deja valide. |
| packages/pptxgenjs/src/pptxgen.ts | 4 | rejected | Meme raison que gen-media.ts (meme paire de commentaires webpackIgnore, meme package deja valide par S0-001). |
| playwright.config.ts | 7 | rejected | Perd le flag NEXT_PUBLIC_MAIC_EDITOR_ENABLED necessaire aux e2e de l'Editor Pro mode ajoute par upstream -- perte de couverture e2e reelle, pas cosmetique. |
| pnpm-workspace.yaml | 4 | rejected | main ne declare pas 'packages/@openmaic/*' comme membre du workspace pnpm, alors que packages/@openmaic/{dsl,importer,renderer} existent reellement et sont deja necessaires au postinstall valide vert par S0-001 (pnpm build). Appliquer casserait cette chaine de build deja verifiee. |
| public/logos/openai.svg | 13 | noop | contenu main deja applique au worktree lors d'une execution precedente (source=git) — idempotent, rien a refaire. Motif d'application : Asset SVG pur (fill via <style>/prefers-color-scheme au lieu de fill='#000' fixe) -- ajout d'un support dark-mode au logo, aucun impact build/type. |
| tsconfig.json | 4 | rejected | main exclut 'packages/docs' de tsc alors que ce dossier EXISTE reellement dans le worktree v0.3.0 (app Fumadocs avec son propre toolchain) -- retirer cette exclusion ferait typechecker un sous-projet non concu pour ca, risque concret de regression sur l'acceptance 'npx tsc --noEmit vert'. |
| vitest.config.ts | 3 | rejected | Perd setupFiles:['tests/setup-env.ts'] ajoute par upstream -- ce fichier existe reellement dans le worktree v0.3.0, le retirer changerait le comportement des tests sans necessite identifiee. |

## S0-006 -- Copie-adaptation branding + providers souverains (LLM/TTS/ASR)

Contexte : app/globals.css, lib/ai/providers.ts, lib/audio/constants.ts, lib/audio/tts-providers.ts, lib/audio/asr-providers.ts, lib/audio/voice-resolver.ts, lib/audio/types.ts, lib/audio/use-tts-preview.ts sont tous des fichiers both_differ HORS perimetre des 137 diffs < 30 lignes de S0-004 (changed_lines de 199 a 1689) -- jamais touches jusqu'ici. Methode : diff complet main vs upstream-v030 fichier par fichier, classification systematique de chaque changement en (a) rattrapage upstream a garder tel quel ou (b) personnalisation Qalem a re-porter.

| Fichier | Verdict | Motif |
|---|---|---|
| app/layout.tsx | applique (partiel) | Titre OpenMAIC -> Qalem, description Qalem, head avec link rel=manifest + meta theme-color=#722ed1, police Manrope (--font-display) restauree -- necessaire aux pages marketing deja portees (OURS_ONLY : app/features, app/institutions, app/legal/*, components/landing/*) qui utilisent la classe Tailwind font-display. Le reste du layout main (SidebarLayout, ErrorBoundary, PwaInstallBanner, TelemetryConsentBanner, ServiceWorkerRegistrar, OfflineIndicator) n'est PAS wire ici : composants deja portes OURS_ONLY (S0-002) mais leur integration au shell applicatif deborde le perimetre branding/providers de S0-006 (PWA push/service worker relevent plutot de S3-002). |
| app/globals.css | applique | Palette complete Illuminated Scholar (light) / Digital Madrasa (dark) restauree dans :root/.dark (le worktree v0.3.0 avait ecrase ces tokens par la palette shadcn grise oklch generique, ne conservant que --primary en light). Utilitaires de design system Qalem re-ajoutes (.glass-nav, .hero-gradient, .purple-glow, .surface-0..4, .pricing-card-glow, .purple-glow-text, .waveform-bar/wave-bounce, .qalam-spark) -- verifies utilises reellement (components/landing/*, navigation-sidebar.tsx). --font-display restaure dans theme inline. Le reste des ajouts upstream (streamdown source, PBL v2 scrollbars/markdown, editor list styles, color-picker) conserve intact. |
| lib/audio/types.ts | applique | BuiltInTTSProviderId : re-ajout de fish-audio / cartesia / edge-tts (presents sur main, absents du worktree v0.3.0 -- 3 providers TTS Qalem entierement supprimes par la restructuration upstream, pas seulement deplaces). Doc d'en-tete corrigee en consequence. |
| lib/audio/constants.ts | applique | Re-ajout des 3 entrees TTS_PROVIDERS completes (fish-audio, cartesia, edge-tts) adaptees au nouveau schema v0.3.0 (models: [], defaultModelId: vide -- champs desormais requis par TTSProviderConfig). Re-ajout des voix FR/AR supprimees par upstream sur azure-tts (Denise, Henri, Mouna, Zariyah) et elevenlabs-tts (Antoine, Charlotte, Arnaud, Emilie, Ghizlane, Fatima, Youssef). Entrees DEFAULT_TTS_VOICES/DEFAULT_TTS_MODELS completees pour les 3 providers reintroduits (Record exhaustif, sinon erreur tsc). |
| lib/audio/tts-providers.ts | applique | Re-ajout de generateFishAudioTTS/generateCartesiaTTS/generateEdgeTTS (logique main inchangee, wiring throwIfTtsRateLimited ajoute par coherence avec les autres providers du fichier v0.3.0) + cases switch correspondants. Bug corrige : generateAzureTTS codait en dur la locale chinoise dans le SSML upstream (regression par rapport a main qui derive la locale depuis la voix -- Known Issue deja documente dans progress.md) ; restaure le calcul dynamique de la locale depuis voiceEntry.language ou le prefixe de l'id de voix, essentiel pour que les voix FR/AR fonctionnent correctement sur Azure. |
| lib/audio/voice-resolver.ts | applique | resolveAgentVoice upstream (v0.3.0) a perdu TOUTE selection de voix consciente de la locale (S-015, ancienne story pre-refork sur main) lors de sa refonte (gestion d'activation par provider, groupes de modeles, providers custom). Re-ajout d'un 5e parametre optionnel locale (string) avec filtrage des voix par prefixe de langue (fr depuis fr-FR, ar depuis ar-MA) en 3e etape de fallback (avant le choix deterministe locale-agnostique), sans toucher aux etapes 1/2 (overrides utilisateur) ni a la signature existante (retro-compatible, tests voice-resolver test files inchanges). Cable dans les 2 call sites reels (components/agent/agent-bar.tsx, lib/hooks/use-discussion-tts.ts) qui exposaient deja locale via useI18n(). |
| lib/audio/asr-providers.ts | verifie, aucun changement | Diff = additions upstream pures (azure-asr, lemonade-asr, custom-provider fallback) ; aucune personnalisation Qalem perdue. |
| lib/audio/use-tts-preview.ts | verifie, aucun changement | Diff = additions upstream pures (modelId, providerOptions) ; aucune personnalisation Qalem perdue. |
| lib/ai/providers.ts | verifie, aucun changement | Diff de 1542 lignes analyse par extraction des IDs de provider/modele (main vs worktree) : churn pur de catalogue de modeles (upstream a renouvele les modeles OpenAI/Claude/Grok/Kimi/Qwen vers des versions plus recentes, ajoute ollama/openrouter/lemonade/xiaomi/tencent-hunyuan, etc.), zero structure/branding Qalem detectee (pas de provider souverain nomme, pas d'override d'icone/couleur). Revert vers main REGRESSERAIT le catalogue (meme categorie d'erreur que S0-004 : main en retard, pas main personnalise). La souverainete LLM de Qalem passe par la config (variables d'environnement/server-providers.yml, hors perimetre S0-006/S0-007), pas par ce fichier. |
| public/logos/{fish-audio,cartesia,edge}.svg | gap pre-existant, non corrige | Ces 3 icones referencees par constants.ts sont ABSENTES du disque sur refork-v030 ET sur main (verification via git cat-file, echec pour les 3) -- gap deja present sur la carriere avant le re-fork, pas une regression introduite ici. Non fabrique (aucune source pour ces assets) ; le champ icon reste optionnel dans TTSProviderConfig, degradation cosmetique (image cassee) sans impact fonctionnel. A signaler si un audit d'assets est fait plus tard. |

### Verification reelle Higgs TTS (HTTP 200), depuis serveuria uniquement

Adresse deduite par recoupement documentaire (dernier octet ".7" mentionne dans 5 fichiers de la documentation produit, meme sous-reseau que serveuria) : 192.168.100.7 port 7861. Aucune adresse complete n'etait documentee ni dans le coffre de secrets -- deduction du dernier octet confirmee a posteriori par le contenu reel de la reponse du service.

1. Requete de sante sur le service -> reponse HTTP 200, avec un statut "ok", un sample_rate de 24000, une liste de voix incluant des noms tels que amine, anasdarija, bilal, david, hamza, hanae, khalid, layla, maryam, mehdi, omar, rim, salma, sana, tariq, younes, et un indicateur de support du tachkil actif -- confirme qu'il s'agit bien du studio Higgs (la voix "anasdarija" evoque la Darija, l'indicateur tachkil est pertinent pour la future story S1-009).
2. La description de l'API (schema OpenAPI standard expose par le service) confirme un framework FastAPI avec des routes de sante, de synthese simple, et une route de synthese compatible OpenAI recevant un objet avec un champ modele (valeur par defaut "higgs"), un texte d'entree requis, une voix optionnelle et une langue optionnelle.
3. Un appel de synthese reel, envoye avec exactement la forme de requete que le code existant (fonction de generation TTS OpenAI deja implementee dans le repo) produit -- teste avec le nom de modele par defaut du service ET avec le nom de modele code en dur cote Qalem -- a chaque fois retourne HTTP 200, avec un type de contenu audio WAV, et un fichier audio valide (format RIFF/WAVE, PCM 16 bits, mono, 24kHz, taille de l'ordre de 150 a 200 Ko selon le texte), synthese reelle en francais aboutie en environ 4 a 5 secondes.
4. Conclusion : le service Higgs est deja compatible avec le code existant sans aucune modification necessaire -- il suffit de configurer la variable d'environnement de l'URL de base du provider TTS OpenAI existant pour pointer vers ce service (sans cle API, aucune cle n'etant requise) afin que le provider deja implemente route vers Higgs. Aucun nouveau provider code en dur n'est necessaire, ce qui respecte la regle documentaire "aucun nouveau service au chantier 0". Le calage effectif de cette configuration en production (fichier d'environnement ou fichier de configuration des providers serveur) releve d'une story ulterieure dediee a cette configuration, hors perimetre de la presente story.
5. Note de suivi (hors perimetre, a tracker) : le champ "cle API requise" du provider TTS OpenAI existant est actuellement code en dur a vrai dans le registre des providers ; si Higgs doit fonctionner sans cle en dehors d'un contexte de configuration cote serveur explicite, une story ulterieure devra verifier que la validation cote client ne bloque pas ce cas de figure (un appel client direct sans cle pourrait etre refuse meme si le serveur route deja vers un endpoint qui n'en a pas besoin).
